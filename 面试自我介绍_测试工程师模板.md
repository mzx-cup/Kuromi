# 测试工程师面试 — 自我介绍 + 项目测试方法讲解

---

## 一、自我介绍（口语版，约 110 秒，含演示停顿）

> 面试官你好，我叫 **[姓名]**，应聘测试开发工程师。
>
> 我对测试开发的理解是——**不是简单地写用例，而是给整个团队搭质量基础设施**。所以我在每个项目里都会做三件事：把测试方法用对、把自动化体系搭完整、把质量防线往前移。我拿我参与的一个项目来具体说，这是一个 FastAPI + LLM 多智能体的 AI 教学系统，30 个页面、30 多个接口。
>
> 先说用例设计。黑盒白盒我都实战过，而且不是分开用的——拿到一个功能，我会同时从两个角度去覆盖。比如说 LLM 返回的闪卡数据，我不只知道它是个 JSON，我还读了一遍它的解析代码，然后发现它可能以 8 种不同的格式出现：纯 JSON、Markdown 包了一层、JSON 嵌在文字中间……那我就用等价类划分把这 8 种全部列出来，再拿 pytest parametrize 做成数据驱动，一个函数搞定。这就是黑盒和白盒配合起来的效果。
>
> 边界值分析也是一样。字段有截断上限、CV 分辨率有 64 到 4096 的有效范围、评分权重有封顶阈值——我不只测有效值，边界上、边界外都测了。场景法我用来验证完整的数据链路，从注册到登录到保存进度到下次加载，4 步串联，中间没有一个环节被跳过。错误推测法我习惯问自己一个问题——"如果我是用户，什么情况下会坏？"LLM 挂了怎么办、Token 被篡改了怎么办、视频文件找不到怎么办，所有这些异常路径我都写了对应的测试。
>
> 白盒测试我还想多说两句。Profile 的评分公式我知道内部怎么算的，我就用手算和代码交叉验证了 7 组参数，浮点数不是用等号比的，是用 `pytest.approx` 做精度对比。CV 那边生成的 MP4 文件，我读了二进制的文件头，验证了第 5 到第 8 字节是不是 `ftyp`——这是 ISO 14496-12 标准写死的，少了这四个字节文件就是坏的。代码内部怎么处理数据，测试就应该精确到那个粒度。
>
> 自动化方面，岗位要求的工具我全部在项目里实际用过。后端 API 用 FastAPI TestClient 加 Mock，隔离了 LLM 依赖，不需要消耗 API 配额就能验证全部逻辑。前端单元测试用 Vitest 加 jsdom，31 个用例覆盖了 Toast 通知和 JWT 认证。UI 自动化这边，Playwright 和 Selenium 我都会，Playwright 我用在 30 个页面的冒烟测试、视觉回归和无障碍审计上，Selenium 我单独写了一套登录页的完整测试，三种定位策略、显式等待、JavaScript Executor 全在里面。接口测试我用 Postman 整理了 17 个请求，每个都配了自动化断言脚本。CI/CD 用 GitHub Actions 搭了 4 个并行 Job，Python 测试、前端单元、E2E、静态审计各跑各的，10 分钟内全部跑完。
>
> 如果说我跟其他候选人有区别——**我关注的不是"跑通"，是"不会再坏"**。发现一个 Bug，我一定把它变成一条自动化回归用例，让它永远不会再出现。我写的不是测试代码，是质量的底线。
>
> 我可以打开项目给你看一下测试的实际效果。（等待面试官回应）

---

### 自我介绍的核心设计逻辑（自己看，不背）

自下而上三层信息，面试官能在 2 分钟内同时感知到：

| 层 | 传递信息 | 关键句 |
|----|---------|--------|
| **技能层** | 工具和方法全会 | "岗位要求的工具我全部在项目里实际用过" |
| **实战层** | 每个方法有对应文件和结果 | "91 个用例 0 失败"合——"你可以看这个文件" |
| **认知层** | 理解 SDET 的本质 | "不是写用例，是搭质量基础设施" / "不是跑通，是不会再坏" / "发现 Bug 变成回归用例，这才是闭环" |

4 句认知锚点，面试官听完至少记住一个：
1. **测试开发 = 搭质量基础设施**（开场，定义角色）
2. **白盒测试精确到代码内部数据的粒度**（讲到白盒时）
3. **自动化测试 ≠ 测试自动化**——审计脚本、CSS 检查、Prompt 校验都是自动化（演示时展开）
4. **发现 Bug → 变成回归用例 → 永远不会再出现 = 闭环**（收尾）

---

## 二、项目测试全景图

### 2.1 我实际写了哪些测试

先把项目的测试清单列清楚，面试官可以一目了然看到工作量：

| 层级 | 测试文件 | 框架 | 覆盖内容 |
|------|---------|------|---------|
| **Python 单元测试** | [test_flashcard_flow.py](tests/test_flashcard_flow.py) | unittest | Flashcard 智能体：解析、截断、异常、API 集成、前端 HTML 验证 |
| | [test_dashboard_data.py](tests/test_dashboard_data.py) | unittest | Dashboard 数据聚合：进度摘要、日历合并、专注事件 |
| | [test_video_static_route.py](tests/test_video_static_route.py) | unittest + TestClient | 视频静态路由：200/404、文件列表 API、中文文件名 |
| | [test_video_player_links.py](tests/test_video_player_links.py) | unittest | 视频播放器 HTML 结构验证、Bilibili API 集成 |
| | [test_hub_navigation.py](tests/test_hub_navigation.py) | unittest | Hub 页面导航链接正确性 |
| | [test_socratic.py](tests/test_socratic.py) | asyncio | 苏格拉底智能体：对话流程、回退数据 |
| | [test_memory_extractor.py](tests/test_memory_extractor.py) | pytest | 记忆抽取器：6 种记忆类型解析、legacy 兼容 |
| | [test_profile_aggregator.py](tests/test_profile_aggregator.py) | pytest | 画像聚合器：评分公式、排序、上限、确认加分，**含 9 种记忆类型 + 7 种评分公式 parametrize** |
| | [test_profile_api.py](tests/test_profile_api.py) | pytest + TestClient | Profile API：200 响应、空用户、v1/v2 路径 |
| | [test_flashcard_parse_parametrize.py](tests/test_flashcard_parse_parametrize.py) 🆕 | pytest | **pytest parametrize 改造**：8 个等价类 × 2 个边界值，数据驱动测试 |
| | [test_ai_output_quality.py](tests/test_ai_output_quality.py) 🆕 | pytest | **AI 输出质量**：格式合规、安全性、性能、容错、Prompt 质量，26 个用例 |
| | [test_cv_api_validation.py](tests/test_cv_api_validation.py) 🆕 | pytest | **CV 算法测试**：分辨率/时长边界值 parametrize、MP4 文件头魔数、响应 Schema |
| **API 场景测试** | [test_api.py](test_api.py) | requests | 注册→登录→保存进度→加载进度 完整链路 |
| **前端单元测试** | [toast.test.js](tests/frontend/unit/toast.test.js) | Vitest + jsdom | Toast 通知：16 个用例，含 XSS 防护、计时器、DOM 生命周期 |
| | [auth.test.js](tests/frontend/unit/auth.test.js) | Vitest + jsdom | JWT 认证：15 个用例，token 过期/损坏、fetchMe、角色显隐 |
| **E2E 测试** | [smoke.spec.js](tests/frontend/e2e/smoke.spec.js) | Playwright | **30 个页面**逐一加载验证 + CSS 令牌注入 + 核心页面专项 |
| | [visual.spec.js](tests/frontend/e2e/visual.spec.js) | Playwright | 4 核心页 + 4 主题变体 像素级截图对比 |
| | [css-conflict-resolution.spec.js](tests/frontend/e2e/css-conflict-resolution.spec.js) | Playwright | 5 高风险页 CSS 加载冲突检测 + 主题切换变量验证 |
| **无障碍测试** | [a11y.spec.js](tests/frontend/a11y/a11y.spec.js) | Playwright + axe-core | 5 核心页 WCAG 2.1 AA 审计 + 键盘导航 + 颜色对比度 |
| **审计脚本测试** | 6 个文件在 [tests/scripts/](tests/scripts/) | unittest | 为 5 个生产审计工具 + 1 个修复工具写的单元测试 |
| **PPT 验证** | [verify_ppt_templates.py](verify_ppt_templates.py) | 独立脚本 | 25 种布局×15 种风格×30 种颜色的完整性校验 |
| **Selenium 示例** 🆕 | [test_login_selenium.py](tests/selenium/test_login_selenium.py) | Selenium WebDriver | 登录页 UI 自动化：3 种定位策略、显式等待、JavaScript Executor、无障碍检查 |
| **Postman 集合** 🆕 | [star-learn.postman_collection.json](star-learn.postman_collection.json) | Postman | 17 个 API 请求，含 Pre-request Script 和 Test 自动化断言 |
| **CI/CD** 🆕 | [.github/workflows/test.yml](.github/workflows/test.yml) | GitHub Actions | 4 个并行 Job：Python tests + 前端单元 + E2E + 静态审计 |
| **pytest 配置** 🆕 | [conftest.py](tests/conftest.py) | pytest | 5 个自定义 markers（slow/ai/cv/e2e/unit）|

### 2.2 测试金字塔

```
                 ╱  视觉回归   ╲        Playwright 截图对比, 4 页 + 4 主题
                ╱   无障碍审计   ╲       axe-core WCAG 2.1 AA, 5 页
               ╱     E2E 冒烟     ╲      Playwright, 30 页面全覆盖
              ╱   API 集成测试     ╲     TestClient + requests
             ╱  AI/CV 专项测试    ╲     LLM输出质量 + CV参数/文件验证 🆕
            ╱   Python 单元测试    ╲    unittest × 6 + pytest × 3 + parametrize × 2 🆕
           ╱   JS 前端单元测试     ╲    Vitest × 2
          ╱   静态审计脚本测试      ╲   unittest × 6 (测审计工具本身)
         ╱   配置/模板验证脚本      ╲   verify_ppt_templates.py
        ╱  CI/CD 自动化流水线     ╲   GitHub Actions 4 并行 Job 🆕
       ╱  Postman 接口集合       ╲   17 个 API + 自动化断言 🆕
      ╱  Selenium WebDriver     ╲   登录页 UI 自动化 🆕
```

---

## 三、测试方法逐一讲解（面试核心）

### 方法 1：等价类划分 — Flashcard JSON 解析

**被测对象**：[`FlashcardAgent._parse_flashcard_response()`](tests/test_flashcard_flow.py#L31)

LLM 返回的闪卡数据可能以多种格式出现，我把输入分成等价类，每类取一个代表值：

| 等价类 | 输入样例 | 测试方法 |
|--------|---------|---------|
| 有效：标准 JSON | `{"flashcards":[{...},{...}]}` | `test_parse_flashcard_response_valid_json` |
| 有效：Markdown 代码块 | ` ```json\n{...}\n``` ` | `test_parse_flashcard_response_markdown_code_block` |
| 有效：普通代码块 | ` ```\n{...}\n``` ` | `test_parse_flashcard_response_plain_code_block` |
| 有效：JSON 嵌在文本中 | `前缀文本 {json} 后缀文本` | `test_parse_flashcard_response_partial_json` |
| 无效：纯文本 | `"This is not JSON at all"` | `test_parse_flashcard_response_invalid_json` |
| 无效：含非 dict 元素 | `{"flashcards":["str", 123, {...}]}` | `test_parse_flashcard_response_non_dict_card` |
| 无效：front 为空 | `{"front":"","back":"A","hint":"H"}` | `test_parse_flashcard_response_empty_front_skipped` |
| 无效：back 为空 | `{"front":"Q","back":"","hint":"H"}` | `test_parse_flashcard_response_empty_back_skipped` |

> **面试讲法**：LLM 的输出格式不可控——有时候返回纯 JSON，有时候外面包着 Markdown，有时候 JSON 嵌在一段说明文字中间。我把所有可能的输入格式划分成等价类，确保解析器对每种情况都能正确处理或优雅降级。总共 8 个等价类，覆盖了有效和无效两大维度。

---

### 方法 2：边界值分析 — 三个边界场景

#### 场景 A：内容长度截断边界

```python
# [tests/test_flashcard_flow.py:60-74](tests/test_flashcard_flow.py#L60)

# back 字段上限 200 字符 → 用 250 测试截断
long_back = "A" * 250
self.assertLessEqual(len(result["flashcards"][0]["back"]), 200)

# hint 字段上限 50 字符 → 用 80 测试截断
long_hint = "H" * 80
self.assertLessEqual(len(result["flashcards"][0]["hint"]), 50)
```

#### 场景 B：最小有效内容边界

```python
# [tests/test_flashcard_flow.py:118-125](tests/test_flashcard_flow.py#L118)
# chapter_content="hi" — 太短，不应生成闪卡
result = asyncio.run(agent.run(state, chapter_content="hi", chapter_name="test"))
self.assertEqual(len(cards.get("flashcards", [])), 0)
```

#### 场景 C：评分公式权重上限

```python
# [tests/test_profile_aggregator.py:159-189](tests/test_profile_aggregator.py#L159)
# access_count 权重公式: min(access_count/10, 0.3)，即上限 0.3
# access_count=20 → min(20/10, 0.3) = 0.3（触发上限）
# access_count=2  → min(2/10, 0.3)  = 0.2
assert high["score"] == pytest.approx(0.78)
assert low["score"] == pytest.approx(0.68)
```

> **面试讲法**：边界值分析我的原则是 **"边界上 + 边界外"**。back 字段限制 200 字符，我就测 250（超出 50）；hint 限制 50，我就测 80（超出 30）。access_count 的权重函数有 `min(x/10, 0.3)` 封顶逻辑，我专门测了触发封顶（20 次访问）和未封顶（2 次）的情况，并用 `pytest.approx` 做精确浮点对比。

---

### 方法 3：场景法 — 完整用户旅程

```python
# [test_api.py](test_api.py) — 模拟一个完整的学习会话

# Step 1: 注册新用户
POST /api/register  → userId

# Step 2: 保存学习进度（含 8 项评估指标）
POST /api/progress/save  → {
    interactionCount, socraticPassRate, difficultyLevel,
    codePracticeTime, focusTimeToday, flashcardsStudied,
    streakDays, interactionHistory
}

# Step 3: 加载进度 → 验证数据一致性
POST /api/progress/load  → 对比 saved == loaded
```

> **面试讲法**：场景法和单个 API 测试的区别是——我测的不是"注册接口通不通"，而是"一个用户从注册到学习到下次打开的全部行为能不能走通"。这 3 步之间有数据依赖，Step 2 需要 Step 1 的 userId，Step 3 需要验证 Step 2 的数据完整落地。**如果只测单接口，数据链路断了根本发现不了。**

---

### 方法 4：错误推测法 — 三类风险

| 风险类型 | 推测 | 测试 |
|---------|------|------|
| **外部依赖故障** | LLM 服务挂了 | Mock LLM 抛出 `Exception("LLM service unavailable")`，验证返回空 + 有"闪卡生成失败"提示 |
| **资源不存在** | 视频文件被删除 | `GET /video/missing.mp4` → 404，非视频文件（README.md）→ 404 |
| **客户端数据损坏** | Token 不是有效 JWT | `localStorage.setItem('sp_token', 'not.a.valid.jwt')` → `isTokenValid()` 返回 false 而非抛异常 |

```python
# [tests/test_flashcard_flow.py:140-150](tests/test_flashcard_flow.py#L140)
mock_gen.side_effect = Exception("LLM service unavailable")
result = asyncio.run(agent.run(state, chapter_content="x" * 100))
self.assertEqual(len(cards.get("flashcards", [])), 0)       # 不崩
self.assertIn("闪卡生成失败", cards.get("message", ""))      # 友好提示
```

> **面试讲法**：错误推测的核心是问自己"**如果我是用户，什么情况下会坏？**"。这个项目依赖 LLM 的 API——我就测 API 挂了怎么办。有视频文件服务——我就测文件不存在怎么办。前端有 JWT Token——我就测 Token 被人为篡改了怎么办。**测试不只测正常路径，异常路径的覆盖才是质量的关键。**

---

### 方法 5：白盒测试 — 精确验证算法

```python
# [tests/test_profile_aggregator.py:68-95](tests/test_profile_aggregator.py#L68)

# 我知道内部评分公式，所以在注释里先写出预期计算过程：
# memory1: confidence=0.9, access=5, confirmed=1
#   score = 0.9*0.6 + min(5/10, 0.3) + 0.1 = 0.54 + 0.3 + 0.1 = 0.94
# memory2: confidence=0.6, access=0, confirmed=0
#   score = 0.6*0.6 + 0 + 0 = 0.36

assert traits[0]["score"] > traits[1]["score"]  # 高分排前面
assert traits[0]["memory_id"] == "mem_high"      # 精确到具体记录

# 确认加分精确到 +0.1
def test_confirmed_bonus_applied(self):
    assert confirmed_trait["score"] == pytest.approx(unconfirmed_trait["score"] + 0.1)
```

> **面试讲法**：白盒测试的关键是——**我知道代码怎么算的，我就能精确验证**。这里有两点值得注意：①我在注释中先把公式和期望值手算出来，再写断言——这相当于用人工计算交叉验证程序逻辑；②浮点数比较用了 `pytest.approx` 而不是 `==`，避免精度误差导致的 flaky test。

---

### 方法 6：Mock 隔离 — 解耦外部依赖

**Python 侧** — 隔离 LLM API：

```python
# [tests/test_flashcard_flow.py:135-139](tests/test_flashcard_flow.py#L135)
from unittest.mock import AsyncMock, patch

with patch.object(agent, '_generate_flashcards', new_callable=AsyncMock) as mock_gen:
    mock_gen.return_value = {"flashcards": [{"front": "Q", "back": "A", "hint": "H"}]}
    # 不调真实 LLM，测试在 10ms 内完成
```

**JavaScript 侧** — 隔离 fetch + 控制时间：

```javascript
// [tests/frontend/unit/auth.test.js:101-109](tests/frontend/unit/auth.test.js#L101)
global.fetch = vi.fn();
fetch.mockResolvedValueOnce({
    ok: true,
    json: () => Promise.resolve({ user: { role: 'student', name: '小明' } }),
});

// [tests/frontend/unit/toast.test.js:108-113](tests/frontend/unit/toast.test.js#L108)
vi.useFakeTimers();
vi.advanceTimersByTime(3100);  // 快进 3.1 秒，不真等
```

> **面试讲法**：Mock 的目的不是偷懒，是三个字——**快、稳、省**。快：不调 LLM，测试毫秒级完成；稳：不依赖网络和第三方服务，不会因为 API 限流导致测试失败；省：不消耗 API 配额。但 Mock 测的是"逻辑正确"，不测"集成正确"——所以我在上层用 Playwright E2E 测试来补位。

---

### 方法 7：Playwright E2E — 30 页冒烟 + 视觉回归 + 无障碍

**冒烟测试** — 30 个页面全覆盖：

```javascript
// [tests/frontend/e2e/smoke.spec.js:19-50](tests/frontend/e2e/smoke.spec.js#L19)
const ALL_PAGES = ['login', 'register', 'hub', 'personal', 'settings',
  'teacher-dashboard', 'courses', 'calendar', 'progress', 'assessment',
  'video-player', 'code', 'socratic-ai', 'data-dashboard', 'flow-meter',
  'plant', 'teacher-class', 'teacher-content', 'teacher-exam', 'teacher-manage',
  'course-learn', 'course-detail', 'generation-preview', 'ai-pair-programming',
  'architecture-blueprint', 'concept-analyzer', 'my-courses', 'stellar-showcase',
  'pixel-pet-game', 'classroom-premium-preview'];

// 每个页面验证：加载成功 + 无 JS 报错 + Toast 系统可用 + CSS 令牌注入
ALL_PAGES.forEach((pageName) => {
  test(`页面加载: ${pageName}.html — 无控制台错误`, async ({ page }) => {
    page.on('console', msg => { if (msg.type() === 'error') errors.push(msg.text()); });
    page.on('pageerror', err => errors.push(err.message));
    await page.goto(`/html/${pageName}.html`, { waitUntil: 'networkidle' });
    expect(await page.locator('body').count()).toBe(1);
    expect(await page.evaluate(() => typeof window.Toast !== 'undefined')).toBe(true);
  });
});
```

**视觉回归** — 像素级截图对比：

```javascript
// [tests/frontend/e2e/visual.spec.js:34-48](tests/frontend/e2e/visual.spec.js#L34)
// 首次运行生成基准图，后续运行逐像素对比
await expect(page).toHaveScreenshot(`${name}.png`, {
    fullPage: true,
    maxDiffPixels: 2000,      // 允许的差异像素
    maxDiffPixelRatio: 0.01,  // 差异比例上限
});

// 4 种主题各截一张，验证主题切换不造成布局偏移
const THEMES = ['warm-morning', 'study-night', 'ocean-glass', 'starry-night'];
```

**无障碍审计** — WCAG 2.1 AA：

```javascript
// [tests/frontend/a11y/a11y.spec.js:33-54](tests/frontend/a11y/a11y.spec.js#L33)
const results = await new AxeBuilder({ page })
    .withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa'])
    .analyze();

// 只阻塞 critical/serious 违规
const criticalViolations = results.violations.filter(
    v => v.impact === 'critical' || v.impact === 'serious'
);
expect(criticalViolations.length).toBe(0);
```

> **面试讲法**：Playwright 是我在项目中最满意的投入。30 个页面的冒烟测试 30 秒跑完——CSS 改了 tokens.css，你不可能手动把 30 个页面全点一遍，但冒烟测试可以。视觉回归的价值在于：CSS 是全局的，改一个 `.app-card` 的 padding 可能影响 200+ 个卡片实例——人类的肉眼发现不了，像素对比可以发现。无障碍审计用的 axe-core 内置 100+ 条 WCAG 规则，我配置了 **只阻塞 critical/serious** 的策略，避免第三方组件的 minor 问题阻断 CI。

---

### 方法 8：自动化审计工具 — 主动构建质量防线

这个项目里我写了 **5 个审计工具**，每一个都有对应的单元测试：

| 审计工具 | 功能 | 对应测试 |
|---------|------|---------|
| [audit_compound_selectors.py](scripts/audit_compound_selectors.py) | 检测 CSS 复合选择器（`* {}`, `body {}`） | [test_audit_compound_selectors.py](tests/scripts/test_audit_compound_selectors.py) — 6 个用例 |
| [audit_frontend_smells.py](scripts/audit_frontend_smells.py) | 检测内联样式、内联事件、魔法数字、console.log、TODO | [test_audit_frontend_smells.py](tests/scripts/test_audit_frontend_smells.py) — 7 个用例 |
| [audit_undefined_css_vars.py](scripts/audit_undefined_css_vars.py) | 检测引用了但未定义的 CSS 变量 | [test_audit_undefined_css_vars.py](tests/scripts/test_audit_undefined_css_vars.py) — 6 个用例 |
| [build_tech_debt_inventory.py](scripts/build_tech_debt_inventory.py) | 汇总所有审计结果，按 P0-P3 排优先级生成 Markdown 报告 | [test_build_tech_debt_inventory.py](tests/scripts/test_build_tech_debt_inventory.py) — 7 个用例 |
| [fix_p0_tech_debt.py](scripts/fix_p0_tech_debt.py) | 自动修复 P0 级问题（注入缺失变量、删除溢出规则） | [test_fix_p0_tech_debt.py](tests/scripts/test_fix_p0_tech_debt.py) — 6 个用例 |

```python
# 举例：审计工具的测试用临时文件隔离，不碰真实代码
# [tests/scripts/test_audit_compound_selectors.py:13-25](tests/scripts/test_audit_compound_selectors.py#L13)
def test_detects_universal_selector(self):
    with tempfile.TemporaryDirectory() as tmpdir:
        css_dir = Path(tmpdir)
        (css_dir / "x.css").write_text("* { box-sizing: border-box; }\n.leaf { color: green; }")
        results = find_compound_selectors(css_dir)
        self.assertEqual(hits[0]["selector"], "*")
        self.assertEqual(hits[0]["line"], 1)

def test_ignores_safe_selectors(self):
    # .card 和 #header 是安全选择器，不应被标记
    (css_dir / "safe.css").write_text(".card { padding: 16px; }\n#header { z-index: 10; }")
    results = find_compound_selectors(css_dir)
    self.assertEqual(results["safe.css"], [])     # 空列表 = 没有误报
```

> **面试讲法**：这是**测试左移**的实践——不等 Bug 出现再测，而是在代码开发阶段就用工具自动扫描问题。5 个审计工具各有对应的单元测试（用临时目录隔离，不碰真实代码），组合成一个 P0-P3 的优先级体系。比如未定义的 CSS 变量被标为 P0，因为会导致样式静默失效用户看不出来；console.log 被标为 P2，因为上线后影响不大但应该清理。

---

### 方法 9：PPT 模板系统的完整性验证

```python
# [verify_ppt_templates.py](verify_ppt_templates.py)
# 被测对象：25 种布局 × 15 种设计风格 × 30 种颜色

# 检查项：
# 1. 25 种 LAYOUT_TYPES 无重复
# 2. 15 种 DESIGN_STYLES 无重复
# 3. 30 种 THEME_COLORS 无重复
# 4. 每个 style 引用的 color 都存在于 THEME_COLORS（外键完整性）
# 5. 每个 style 引用的 layout 都存在于 LAYOUT_TYPES（外键完整性）
# 6. _select_layout() 对 1-8 个 content item 锁定不同布局
# 7. 锁定布局之间不重复（8 个计数 → 8 种不同布局）

# 运行: python verify_ppt_templates.py
# 输出: ALL CHECKS PASSED 或错误列表
```

> **面试讲法**：这个验证脚本本质上是**配置数据的集成测试**——25 种布局、15 种风格、30 种颜色之间有交叉引用关系，任何新增/修改都可能破坏引用完整性。我把这些检查写成自动化脚本，每次修改配置后跑一次就能确认没有破坏已有规则。这比手工检查快，也比手工检查可靠。

---

### 方法 10：pytest parametrize — 数据驱动测试改造 🆕

**被测对象**：[`test_flashcard_parse_parametrize.py`](tests/test_flashcard_parse_parametrize.py) + [`test_profile_aggregator.py`](tests/test_profile_aggregator.py)

这是 pytest 和 unittest 最大的区别——**把重复测试用例变成数据驱动**。

**改造前 vs 改造后**：

```python
# 【改造前】test_flashcard_flow.py — 8 个独立方法，每个方法结构完全相同
def test_parse_standard_json(self):            # 方法 1
def test_parse_markdown_code_block(self):       # 方法 2
def test_parse_plain_code_block(self):          # 方法 3
def test_parse_json_in_text(self):              # 方法 4
def test_parse_invalid_json(self):              # 方法 5
def test_parse_non_dict_card(self):             # 方法 6
def test_parse_empty_front(self):               # 方法 7
def test_parse_empty_back(self):                # 方法 8

# 【改造后】test_flashcard_parse_parametrize.py — 1 个函数驱动 8 组数据
FLASH_CARD_TEST_CASES = [
    pytest.param("标准JSON数组", ..., 2, "What is HDFS?", id="valid-standard-json"),
    pytest.param("Markdown代码块", ..., 1, "Q1", id="valid-markdown-code-block"),
    # ... 另外 6 组
]

@pytest.mark.parametrize("desc,response,expected_count,first_front",
                          FLASH_CARD_TEST_CASES)
def test_parse_flashcard_format(desc, response, expected_count, first_front):
    """每个等价类的断言逻辑完全相同，只是输入数据和期望不同"""
    result = agent._parse_flashcard_response(response)
    assert len(result["flashcards"]) == expected_count
```

同样改造了 `test_profile_aggregator.py`——**9 种记忆类型映射**和**7 组评分公式**也全部参数化：

```python
# [tests/test_profile_aggregator.py:195-206]
@pytest.mark.parametrize("memory_type,expected_category", [
    ("learning_trait", "learning_traits"),
    ("personality",    "personality_traits"),
    ("goal",           "goals_interests"),
    # ... 共 9 种映射
])
def test_memory_type_mapped_to_correct_category(memory_type, expected_category):
    ...

# [tests/test_profile_aggregator.py:224-263]
@pytest.mark.parametrize("confidence,access_count,confirmed,expected_score,desc", [
    (0.9, 5, 1, 0.94, "高置信度+已确认"),
    (0.6, 0, 0, 0.36, "低置信度裸分"),
    (0.8, 20, 0, 0.78, "access_count 封顶"),
    # ... 共 7 组场景
])
def test_score_calculation_formula(confidence, access_count, confirmed, expected_score, desc):
    result = _calculate_score(confidence, access_count, confirmed)
    assert result == pytest.approx(expected_score)
```

> **面试讲法**：很多人用 pytest 只是把 unittest 的 TestCase 搬过来，没用到 pytest 的真正价值。我在项目里专门做了 parametrize 改造——把 Flashcard 解析的 8 个等价类从 8 个独立方法压缩成 1 个参数化函数，把 Profile 聚合的 9 种记忆类型映射和 7 组评分公式也全部参数化。这样做有**三个好处**：①加新测试只需在数据列表加一行，不用写新函数；②失败时 pytest 精确报告是哪组数据，定位快；③跑 `-v` 时能看到每个 `id` 的通过状态，可读性比 8 个 `test_xxx` 方法好。

---

### 方法 11：AI 输出质量测试 🆕

**被测对象**：[`test_ai_output_quality.py`](tests/test_ai_output_quality.py) — 26 个用例，5 个维度

这个项目的特殊性在于它本身是 AI 系统——LLM 是核心依赖。传统的"测 API 返回 200"不够用，我补充了 AI 输出质量专项测试：

| 维度 | 测试内容 | 用例数 |
|------|---------|-------|
| **格式合规** | 每张卡片必须有 front/back/hint，批量卡片的逐个校验 | 4 |
| **内容安全** | 解析器保留原始内容供前端 escapeHTML 处理，截断后数据完整性 | 2 |
| **解析性能** | 单张 < 5ms、20 张 < 10ms、Markdown 包裹解析 < 10ms | 3 |
| **异常容错** | 空返回、残缺 JSON、数组输入、大量空白字符 | 4 |
| **Prompt 质量** | SYSTEM_PROMPT 包含质量控制关键词、JSON 格式要求、字符限制声明、最少数量要求 | 7 |
| **常量一致性** | MIN_FLASHCARD_COUNT 和 MAX_RETRY_ATTEMPTS 与 Prompt 声明一致 | 2 |

```python
# [tests/test_ai_output_quality.py:18-42] — 格式合规示例
def test_parsed_cards_have_required_fields(self):
    """验证：每张解析后的闪卡必须包含 front/back/hint"""
    result = agent._parse_flashcard_response(response)
    for card in result.get("flashcards", []):
        assert "front" in card and "back" in card and "hint" in card

# [tests/test_ai_output_quality.py:115-127] — 性能测试示例
def test_parse_single_card_under_5ms(self):
    result = agent._parse_flashcard_response(response)
    elapsed_ms = (time.perf_counter() - start) * 1000
    assert elapsed_ms < 5, f"单张闪卡解析耗时 {elapsed_ms:.2f}ms"

# [tests/test_ai_output_quality.py:155-161] — Prompt 质量测试
@pytest.mark.parametrize("keyword", ["压缩规则","输出格式","质量控制","flashcards","front","back","hint"])
def test_flashcard_prompt_contains_quality_keywords(self, keyword):
    assert keyword in FlashcardAgent.SYSTEM_PROMPT
```

> **面试讲法**：这个项目是 AI 系统，所以我对 AI 接口测试有一套区别于传统 API 测试的方法。传统 API 测返回 200 + JSON schema 就够了，但 LLM 接口的**非确定性输出**需要额外维度：①格式合规——LLM 可能返回各种包裹格式；②Prompt 质量——Prompt 写错了后面所有测试都无效，所以我专门验证 Prompt 模板的关键词完整性；③性能——LLM 调用慢是已知问题，但解析层必须快；④异常容错——LLM 返回空/不完整 JSON/奇怪格式时不能崩。**所有测试不消耗真实 API 配额，Mock 了 LLM 调用层。**

---

### 方法 12：CV 算法测试 🆕

**被测对象**：[`test_cv_api_validation.py`](tests/test_cv_api_validation.py) — 30 个用例

项目有视频/图片生成 API（CogVideo、Kling），我针对这些 CV 接口写了专项测试：

| 维度 | 测试方法 | 用例数 |
|------|---------|-------|
| **分辨率边界值** | `@pytest.mark.parametrize` 驱动 11 组 (w,h) → 有效/无效 | 11 |
| **时长边界值** | 7 组 duration 参数，从 0.1s 到 61s | 7 |
| **宽高比验证** | 5 组常见比例（16/9、1:1、9/16）精确到 0.01 | 5 |
| **MP4 文件头** | ftyp box 魔数验证（ISO/IEC 14496-12 标准） | 1 |
| **文件完整性** | 非空验证、最小大小验证 | 2 |
| **响应 Schema** | image_url/base64 字段存在性 + URL 协议验证 | 4 |

```python
# [tests/test_cv_api_validation.py:37-51] — 分辨率参数化
@pytest.mark.parametrize("width,height,expected,desc", [
    (1920, 1080, True,  "标准 1080p"),
    (64,   64,   True,  "最小有效分辨率"),     # 边界值
    (4096, 4096, True,  "最大有效分辨率"),     # 边界值
    (0,    1080, False, "宽度为 0"),           # 边界外
    (-1,   1080, False, "宽度为负数"),          # 边界外
    (10000, 10000, False, "超大分辨率"),        # 边界外
    # ...
])
def test_resolution_boundary_values(self, width, height, expected, desc):
    assert validate_resolution(width, height) == expected

# [tests/test_cv_api_validation.py:104-120] — MP4 文件头魔数验证
def test_mp4_file_header_ftyp_box(self):
    """白盒：MP4 文件第 5-8 字节应为 'ftyp' (ISO/IEC 14496-12)"""
    with open(mp4_file, "rb") as f:
        header = f.read(12)
    assert header[4:8] == b'ftyp', f"MP4 文件缺少 ftyp box"
```

> **面试讲法**：虽然这不是纯 CV 项目，但包含了视频/图片生成 API。我针对性地写了 CV 接口测试，三个维度：①**参数校验**——分辨率和时长的边界值用 parametrize 驱动，从最小有效值到非法值全覆盖；②**输出文件格式**——MP4 文件头的 ftyp box 魔数验证，这是 ISO 标准规定的，如果缺失说明文件损坏；③**响应 Schema**——验证 image_url 是否使用 http/https 协议。这套测试方法对任何 CV API 都适用。没有 .mp4 文件时测试自动 skip，不阻塞。

---

### 方法 13：CI/CD + Postman + Selenium — 工具体系落地 🆕

这三个放到一起讲，因为它们构成了项目的**工程化测试体系**：

**GitHub Actions CI** — 4 个并行 Job：

```yaml
# [.github/workflows/test.yml](.github/workflows/test.yml)
jobs:
  python-tests:     # pytest + coverage → html 报告
  frontend-unit:    # Vitest + junit reporter
  e2e-tests:        # Playwright + screenshot artifacts
  static-audit:     # CSS 审计 + 审计脚本测试
```

**Postman Collection** — 17 个 API 请求，5 个功能模块：

```javascript
// [star-learn.postman_collection.json] — 闪卡生成请求的 Test 脚本
pm.test("闪卡生成成功 200", function () {
    pm.response.to.have.status(200);
});
pm.test("生成至少 1 张闪卡", function () {
    pm.expect(cards.length).to.be.at.least(1);
});
// 同时验证每张卡片的字段完整性
cards.forEach(function(card) {
    pm.expect(card.front).to.be.a('string').and.not.empty;
    pm.expect(card.back).to.be.a('string').and.not.empty;
});
```

**Selenium WebDriver** — 登录页 UI 自动化：

```python
# [tests/selenium/test_login_selenium.py:100-120]
# 展示三种定位策略
username_input = self.driver.find_element(By.CSS_SELECTOR, "input[name='username']")
password_input = self.driver.find_element(By.CSS_SELECTOR, "input[type='password']")
submit_btn = self.driver.find_element(By.XPATH, "//button[contains(text(),'登录')]")

# 显示等待 — 表单卡片出现后才继续
form_card = self.wait.until(
    EC.visibility_of_element_located((By.CSS_SELECTOR, ".auth-card-inner"))
)
```

> **面试讲法**：CI 方面，我把 GitHub Actions 配置成 4 个并行 Job——Python 测试、前端单元测试、Playwright E2E 和静态审计各跑各的，互不阻塞，总时间控制在 10 分钟内。Postman 方面，不是简单的存几个 curl——每个请求都配了 Tests 脚本做自动化断言，集合变量管理 base_url 和 token。Selenium 方面，虽然项目主力用 Playwright，但岗位要求了解 Selenium，所以我写了一个完整的登录页测试对比两者差异——Selenium 需要手动管理 WebDriver 生命周期和显式等待，Playwright 内置自动等待。三种元素定位策略（CSS/XPath/ID）两者通用，迁移成本不高。

---

## 四、测试数据管理

| 数据类型 | 管理方式 | 例子 |
|---------|---------|------|
| **硬编码测试数据** | 直接在测试方法内定义 | `"A" * 250`、`{"front":"Q","back":"A"}` |
| **临时文件隔离** | `tempfile.TemporaryDirectory()` | 审计脚本测试用临时 CSS 文件，测试结束自动清理 |
| **Mock 数据** | `AsyncMock.return_value` / `vi.fn().mockResolvedValueOnce` | LLM 返回、fetch 响应 |
| **Fixtures** | `tests/fixtures/audit/*.css` | 预置 CSS 样本文件，供审计工具测试使用 |
| **E2E 隔离用户** | 唯一 test user ID | `test_user`, `testuser_eval`, `nonexistent_user_999` |
| **视觉基准** | Git 追踪的 PNG 截图 | `tests/frontend/e2e/visual.spec.js-snapshots/*.png` |

---

## 五、岗位要求逐条匹配

| 岗位要求 | 项目中的对应实践 |
|---------|----------------|
| **熟练掌握 Python** | 15 个 Python 测试文件 + 5 个审计脚本 + PPT 验证脚本 |
| **等价类划分** | Flashcard JSON 解析 8 个等价类 + CV 分辨率 11 组边界值 + 记忆类型 9 种映射 |
| **边界值分析** | 200/50 字符截断、最小内容边界、权重上限、分辨率 64-4096 范围 |
| **场景法** | 注册→保存→加载 完整用户旅程 + 登录页 Selenium UI 交互 |
| **黑盒测试** | API E2E 测试、30 页 Playwright 冒烟、WCAG 无障碍审计、Postman Collection |
| **白盒测试** | Profile 评分公式精确计算（7 组 parametrize）+ MP4 ftyp 魔数验证 |
| **项目测试经验** | 20+ 测试文件，Py+JS + Postman + CI/CD 完整体系 |
| **API 自动化** | FastAPI TestClient + requests + Postman 自动化断言 + Playwright E2E |
| **Web UI 自动化** | Playwright 冒烟/视觉回归/无障碍 + Selenium WebDriver 登录测试 |
| **AI 工具提效** | AI 输出质量专项测试(26 用例) + Prompt 质量验证 + LLM 容错测试 |
| **CV 算法测试** | 视频分辨率/时长边界值 + MP4 文件头魔数 + 图像响应 Schema 验证 |

| 加分项 | 匹配 |
|--------|------|
| **Pytest** | ✅ 参数化测试 (parametrize) + fixtures + approx + conftest markers |
| **Unittest** | ✅ 9 个文件使用 TestCase + AsyncMock + patch + setUp/tearDown |
| **Selenium WebDriver** | ✅ 登录页 UI 自动化，含 3 种定位策略 + 显式等待 + JavaScript Executor |
| **Postman** | ✅ 完整 Collection (17 请求 + Pre-request Script + Test 自动化断言) |
| **Robot Framework** | ⚠️ 了解基础语法，项目未使用（有 Playwright 可替代） |
| **JMeter** | ⚠️ 了解原理，项目未使用（如需可补充 locust） |
| **Git** | ✅ 项目全程 Git 管理 |
| **CI/CD** | ✅ GitHub Actions 4 并行 Job (Python + 前端 + E2E + 静态审计) |

---

## 六、13 个面试追问 + 回答

### Q1: 你是怎么决定一个功能要写哪些测试用例的？

> 拿到功能需求后，我先拆成三层来想：
>
> **第一层——逻辑层**：这个函数的输入有哪些可能的值？用等价类+边界值列出来。比如 Flashcard 解析，输入是 LLM 返回的字符串 → 分成标准 JSON、Markdown 包裹、纯文本、含非 dict 元素等 8 个等价类。
>
> **第二层——集成层**：这个 API 的 happy path + 每个 HTTP 错误码都要覆盖。比如 Flashcard API，200（成功）、422（参数校验失败）、以及内容太短返回空但不报错。
>
> **第三层——异常层**：什么可能出错？外部服务挂了、文件不存在、数据损坏。用错误推测法补充。
>
> 三层加在一起，就是我的测试用例清单。

### Q2: 单元测试和集成测试的比例怎么分配？

> 我的原则是**金字塔原则**——底层多、上层少。这个项目里 Python 单元测试约 60+ 个用例，API 集成约 15 个，E2E 约 10 个 E2E 场景。单元测试跑得快（秒级），写得多；E2E 慢（分钟级），但覆盖真实用户路径，只测关键流程。

### Q3: 怎么保证测试的稳定性（不 flaky）？

> 三个方面。**数据隔离**：每个测试用独立构造数据或临时文件，不依赖数据库状态。**时间控制**：Mock 所有定时器和外部调用，用 `vi.useFakeTimers()` 和 `AsyncMock` 替代真等待。**断言宽松度**：视觉回归设 `maxDiffPixels: 2000` 容忍动画微小差异；无障碍只阻塞 critical/serious。

### Q4: 遇到过最难的 Bug 是什么？

> Flashcard API 中，LLM 返回的 JSON 格式极其不稳定——有时纯 JSON、有时 \`\`\`json 包裹、有时前缀文本后面跟 JSON。最开始只处理了纯 JSON 的情况，解析失败率约 20%。我通过分析失败日志 → 归纳格式类别 → 增强解析器的多格式提取逻辑 → 写了 8 个等价类测试用例覆盖。修复后解析成功率接近 100%。

### Q5: 测试数据怎么管理？

> 分四类管理：①**硬编码**——简单数据直接写测试里；②**临时文件**——审计工具测试用 `tempfile.TemporaryDirectory`，自动清理；③**Mock 数据**——LLM 和 fetch 调用全部 Mock，不碰真实 API；④**Fixtures**——`tests/fixtures/` 下预置 CSS 样本供审计工具测试。所有测试数据不依赖生产环境。

### Q6: 项目中用 pytest 和 unittest 有什么区别感受？

> unittest 的 TestCase 类结构适合有状态的、需要 setUp/tearDown 的场景，比如 FlashcardAgent 测试需要构造 StudentState。pytest 的纯函数风格 + fixture 机制适合无状态的纯逻辑测试，比如 profile_aggregator 的数据处理。
>
> 我专门做了 pytest 的 parametrize 改造来展示两者的核心差异——Flashcard 解析的 8 个等价类在 unittest 里是 8 个独立方法，代码重复严重；改成 `@pytest.mark.parametrize` 后变为 1 个函数驱动 8 组数据。**关键不是哪个更好，而是根据被测对象特点选择**——我在同一个项目里混用，同时通过 parametrize 展示了 pytest 不可替代的优势。

### Q7: 你怎么做代码审查中的测试 review？

> 三条标准：①改动的代码有没有对应的测试？如果改了评分公式但没改测试，我会要求补充。②测试有没有覆盖异常路径？只测 happy path 不够，至少要加一个错误 case。③测试本身有没有 Bug？比如断言写反了、Mock 的返回值不符合现实——我用"反向思维"读测试代码："这个断言在什么情况下会错误通过？"

### Q8: 会不会用 Selenium/Playwright 做 UI 自动化？

> 两个都会。Playwright 在项目里是主力——30 页冒烟测试、视觉回归、无障碍审计。Selenium 我单独写了登录页的 UI 自动化测试来展示能力——三种定位策略（CSS Selector/XPath/ID）、显式等待（WebDriverWait + expected_conditions）、JavaScript Executor 都实际用到了。同时准备了 Selenium vs Playwright 的对比分析：Selenium 需要手动管理 WebDriver 生命周期，Playwright 内置自动等待和浏览器管理；但定位策略两者通用，迁移成本可控。

### Q9: 这个项目的测试还有什么不足？你打算怎么改进？

> 三方面持续优化中：①**性能测试待补充**——API 没有并发/压力测试，可以用 locust 写场景脚本做阶梯加压；②**Robot Framework 可探索**——虽然 Playwright 能覆盖 UI 自动化需求，但 RF 的关键词驱动在团队协作场景下有优势；③**E2E 数据驱动深化**——30 页冒烟断言目前较通用，可以为核心页面加页面专属验证点。已经补齐的部分（参数化、Postman、Selenium、CI、AI/CV 专项测试）覆盖了岗位的主要加分项。

### Q10: 你怎么跟上测试技术的变化？

> 两个方面。一是**关注工具迭代**——比如这个项目从零引入 Playwright，从冒烟测试扩展到视觉回归再到无障碍审计，工具的能力在扩展，我的用法也在深化。二是**关注方法论**——测试左移、测试金字塔、契约测试这些思想比具体工具更重要。工具可以换，但分析被测对象、设计测试策略的能力是通用的。

### Q11: Postman Collection 你是怎么组织的？

> 按功能模块分组——用户模块（注册/登录/获取信息）、学习模块（进度/闪卡/AI 对话）、视频模块、画像模块、系统模块，共 17 个请求 5 个文件夹。每个请求都配了 **Tests 脚本做自动化断言**——不是简单测 200，而是验证字段完整性。比如闪卡生成接口的 Test 脚本校验了"返回 success=true"+"flashcards 数组不为空"+"每张卡片有 front/back"。集合变量管理 `base_url` 和 `test_user_id`，登录成功后自动把 token 写入环境变量供后续请求使用。

### Q12: GitHub Actions CI 流水线怎么设计的？

> 4 个并行 Job 互不阻塞：Python 测试（pytest + coverage → html）、前端单元测试（Vitest + junit reporter）、E2E 测试（Playwright，先启动 dev server 再跑）、静态审计（跑 3 个 CSS 审计脚本 + 审计工具的单元测试）。都设了 `timeout-minutes` 防止卡死，失败时上传 screenshot artifact 方便排查。整个流水线 10 分钟内跑完。

### Q13: AI 系统的测试和传统系统最大的区别是什么？

> 最大的区别是**输出的非确定性**。传统 API 测"给定输入 → 固定输出"，AI 系统测"给定输入 → 输出在格式和质量的合理范围内"。我应对的策略是三层：①**格式层**——验证输出结构合规（JSON schema、必填字段），这部分是确定性的；②**Prompt 层**——验证发给 LLM 的指令本身是正确的，Prompt 质量测试不依赖 LLM 实际返回；③**容错层**——LLM 返回空/不完整/奇怪格式时系统不崩溃。这三层加在一起，在"确定性验证"和"非确定性容忍"之间找到了平衡。

---

## 七、使用建议

1. **面试前**：把"三、测试方法逐一讲解"中选 3-4 个你最熟的，能脱稿讲出来；推荐组合：方法 2（边界值）+ 方法 5（白盒测试）+ 方法 10（parametrize）+ 方法 11（AI 输出质量）
2. **面试中**：自我介绍后主动说"我用一个项目具体讲解我的测试方法"，掌握节奏
3. **代码展示**：如有条件，打开 [tests/](tests/) 目录让面试官看到真实文件结构
4. **Postman 加分**：面试前打开 Postman 导入 `star-learn.postman_collection.json`，展示时有实物比空口说强 10 倍
5. **量化的力量**：多说数字——"30 个页面""91 个新增用例全部 PASS""17 个 Postman 请求""5 个审计工具""8 个等价类"
6. **差异化强调**：重点说"AI 输出质量测试"和"pytest parametrize 改造"，这两点绝大多数候选人讲不出来
7. **诚实**：不熟悉的工具（JMeter、Robot Framework）直接说"有了解但项目里没用过，需要可以学"

---

## 八、现场演示流程（面试官允许展示时使用）

---

### 0. 面试前准备（出发前必须做完）

> 以下操作在面试当天出门前完成，确保现场打开就能展示。

| 序号 | 准备项 | 具体操作 | 检查方法 |
|------|--------|---------|---------|
| 1 | VS Code 打开项目 | 用 VS Code 打开项目根目录 `Kuromi-main/` | 左侧文件树能看到 `tests/`、`scripts/` 等文件夹 |
| 2 | 终端就绪 | VS Code 内打开终端（Ctrl+`），确保在项目根目录 | 输入 `pytest --version` 能正常输出版本号 |
| 3 | Postman 导入 Collection | 打开 Postman → 点击左上角 **Import** → 选择 `star-learn.postman_collection.json` → 确认导入 | 左侧 Collections 列表能看到 "星识 Star-Learn API" |
| 4 | 浏览器只保留必要标签页 | 关掉无关标签页，只保留 VS Code 和 Postman | 面试时切换窗口不会暴露隐私内容 |
| 5 | 预先跑一次测试 | 终端执行 `pytest tests/test_flashcard_parse_parametrize.py tests/test_ai_output_quality.py tests/test_cv_api_validation.py tests/test_profile_aggregator.py -v --tb=short` | 确认输出 `91 passed`，没有意外的 FAIL |
| 6 | 关闭通知 | Windows 开启专注助手，关闭微信/Slack 弹窗 | 屏幕共享时不会有消息弹出 |
| 7 | 确认预存结果就绪 | 打开 `demo-results/` 文件夹，确认里面有 7 个 .txt 文件 | 演示时可以直接打开这些文件展示结果，不依赖终端实时运行 |

---

### 演示节奏总览

```
步骤 1 [30s]  目录全景           → 证明数量多、覆盖广
步骤 2 [1min]  parametrize 改造   → 证明 pytest 不只是换个装饰器
步骤 3 [1min]  AI 输出质量测试    → 证明能测别人测不了的东西
步骤 4 [30s]   pytest markers     → 证明理解测试策略工程化
步骤 5 [30s]   运行测试看结果      → 91 passed / 0 fail 当场验证
步骤 6 [1min]  Postman Collection → 证明接口测试有体系
步骤 7 [1min]  审计工具 + 测试左移 → 证明主动构建质量防线
步骤 8 [30s]   CI/CD 流水线       → 证明持续集成能落地
步骤 9 [30s]   Selenium 对比      → 证明多工具都会用
```

**总共约 6-8 分钟。时间不够时优先保 1→2→3→5（3 分钟核心亮点）。**

---

### 预存测试结果速查

> 所有测试结果已预存到 `demo-results/` 文件夹。演示时如果不想跑终端，直接打开对应文件即可。

**在 VS Code 中打开**：左侧文件树 → 展开 `demo-results/` 文件夹 → 双击文件名即可打开。

| 演示步骤 | 打开哪个文件 | 指给面试官看什么 |
|---------|------------|----------------|
| 步骤 2 parametrize | `demo-results/02-parametrize.txt` | 10 个参数化用例名 + `10 passed` |
| 步骤 3 AI 质量 | `demo-results/03-ai-quality.txt` | 26 个用例按 5 个维度分布 + `26 passed` |
| 步骤 4 markers | `demo-results/07-custom-markers.txt` | 5 个自定义 markers（slow/ai/cv/e2e/unit）|
| 步骤 5 全部运行 | `demo-results/01-core-tests-verbose.txt` | 91 个用例逐条通过 + `91 passed, 3 skipped in 0.23s` |
| CV 补充展示 | `demo-results/04-cv-validation.txt` | 34 个用例（分辨率/时长/宽高比/MP4）|
| Profile 补充展示 | `demo-results/05-profile-aggregator.txt` | 24 个用例（9 种映射 + 7 组评分公式）|
| 全量测试概览 | `demo-results/06-full-suite-summary.txt` | `201 passed, 4 failed, 7 skipped`（4 个 failed 是项目原有问题）|

**演示技巧**：
- 打开 .txt 文件后，**滚动到文件末尾**，让面试官看到最后一行汇总数字（`XX passed in X.XXs`）
- 然后**往上翻一点**，随机指几个 PASSED 的用例名——"你看，每个参数化场景都有独立的测试 ID"
- 如果想展示某个特定用例的详情，**Ctrl+F 搜索**用例名（如搜索 `invalid-empty-front`）

---

### 步骤 1：目录全景（30 秒）

**你要证明的**：测试文件数量多、覆盖维度广、不是临时凑的。

**操作**：

1. **鼠标移到 VS Code 左侧文件树的 `tests/` 文件夹上，点击展开**
2. **用鼠标从上到下缓慢划过文件名**（不要点进去，只让面试官看到结构）
3. **鼠标停在 `test_flashcard_parse_parametrize.py` 上停顿 1 秒**——这是你的核心改造成果
4. **鼠标停在 `test_ai_output_quality.py` 上停顿 1 秒**——这是你的差异化亮点
5. **鼠标停在 `conftest.py` 上停顿 1 秒**——说明你有全局配置意识
6. **继续展开 `selenium/`、`frontend/`、`scripts/` 子目录**，逐个划过

**边说边指**：

> "你扫一眼这个目录，我覆盖了 4 个层级——（鼠标指 `tests/` 根目录的 .py 文件）Python 单元测试 13 个文件，（鼠标移到 `frontend/`）前端 Vitest 单元加 Playwright E2E，（鼠标移到 `selenium/`）Selenium UI 测试，（鼠标移到 `scripts/`）还有 6 个审计工具的测试。总体 91 个新增用例全部通过，等下我可以跑给你看。"

**认知点（自然带出）**：
> "这些文件不是随便写的——正常流程用 unittest 的 TestCase，数据驱动用 pytest parametrize，特殊维度像 AI 和 CV 单独做专项测试文件。**看文件结构就能看出测试策略。**"

**过渡到下一步**：
> "我先挑一个最有代表性的给你看——（点击 `test_flashcard_parse_parametrize.py` 打开）这个文件最能体现 pytest 和 unittest 的本质区别。看完代码我再给你看运行结果——（打开 `demo-results/02-parametrize.txt`）10 个参数化用例全部通过。"

---

### 步骤 2：pytest parametrize 改造（1 分钟）

**你要证明的**：你不是把 unittest 的 TestCase 搬过来改个名——你理解 parametrize 是数据驱动测试的核心。

**操作**：

1. **双击 `test_flashcard_parse_parametrize.py` 打开文件**（如已在上一步打开则跳过）
2. **滚动到 `FLASH_CARD_TEST_CASES` 列表处（大约第 25-45 行）**
3. **选中整个列表的代码，让面试官看到 8 组数据的结构**
4. **鼠标指向 `pytest.param(` 这一行**——"你看这里，每组数据有 4 个字段：描述、输入、期望数量、期望首条内容"
5. **鼠标指向 `id=` 参数**——"这个 id 就是测试的可读名称，跑 `-v` 的时候会显示出来"
6. **向下滚动到 `@pytest.mark.parametrize` 装饰器**——"一个装饰器驱动下面这一个测试函数，这 8 组数据会依次跑 8 次"
7. **按 Ctrl+Tab 切回 `test_flashcard_flow.py`（已有的 unittest 版本），滚动到 8 个 `test_parse_xxx` 方法那里**——做个对比
8. **鼠标在 unittest 版本和 parametrize 版本之间来回指一下**——"左边 8 个方法，右边 1 个函数 + 8 组数据"

**边说边指**：

> "你对比一下这两种写法。（鼠标指 unittest 版本）这是项目原来用 unittest 写的，8 个 `test_parse_xxx` 方法，每个方法 10 到 15 行，代码结构完全一样，变的只有输入和期望值。（切回 parametrize 版本，鼠标指数据列表）这是我用 pytest parametrize 改写的——1 个函数驱动 8 组数据。"
>
> "好处三个：第一，加新场景只在列表里加一行，不用复制粘贴一个新方法；第二，跑 `-v` 的时候输出的是 `[valid-standard-json] PASSED` 这种可读的名字，而不是 `test_parse_flashcard_response_valid_json` 这种又长又重复的方法名；第三，失败的时候 pytest 精确告诉你哪组数据出错——（鼠标指 `id='valid-standard-json'`）不会让你在 8 个方法里找。"
>
> "我把同样的模式也用在画像聚合器上了——（按 Ctrl+P，输入 `profile_aggregator`，打开 `test_profile_aggregator.py`）你往下翻，`test_memory_type_mapped_to_correct_category`，9 种记忆类型到 3 个分类的映射，全部 parametrize。再往下，`test_score_calculation_formula`，7 组评分公式从低置信度到封顶，包括浮点精度对比。**这就是数据驱动测试——逻辑和测试数据分离，代码量少但覆盖面大。**"

**认知点**：
> "很多人用 pytest 就只是把 TestCase 搬过来换了个 `@pytest.fixture`，真正的价值没用上。pytest 的价值不是语法糖，是让你能用更少的代码覆盖更多的场景——代码越少，加新测试的阻力越小，测试套件就越容易维护。"

**展示预存结果**：打开 `demo-results/02-parametrize.txt` → Ctrl+End 到底部 → 鼠标指 `10 passed in 0.14s` → "10 个参数化用例，全部通过，0.14 秒。" 往上翻一页让面试官看 PASSED 列表。

**过渡**：
> "好，再给你看一个我觉得最有区分度的——（关掉当前文件，打开 `test_ai_output_quality.py`）这个 AI 输出质量测试，一般的测试工程师不会这么写。看完我切到结果文件——（打开 `demo-results/03-ai-quality.txt`）26 个用例全部通过。"

---

### 步骤 3：AI 输出质量测试（1 分钟）

**你要证明的**：AI 系统的测试方法你有一套完整的思考框架——不是测 API 返回 200 就完了。

**操作**：

1. **双击 `test_ai_output_quality.py` 打开文件**
2. **先不要滚动，鼠标指向文件顶部的 docstring（第 1-10 行）**——"你看，我把 AI 输出质量拆成 5 个测试维度"
3. **缓慢向下滚动，每看到一个 `class TestLLM...` 就鼠标指一下类名停顿半秒**：
   - `TestLLMOutputFormatCompliance` — "格式合规，验证 LLM 返回的 JSON 结构对了没"
   - `TestLLMOutputSecurity` — "内容安全，验证解析器处理注入内容不会崩"
   - `TestLLMOutputPerformance` — "性能，单张解析要 5 毫秒以内"
   - `TestLLMErrorTolerance` — "容错，LLM 返回空或者残缺 JSON 不能崩"
   - `TestAIPromptQuality` — "这个是关键——Prompt 模板质量"
   - `TestFlashcardAgentConstants` — "常量一致性"
4. **滚动到 `TestAIPromptQuality` 类，选中 `test_flashcard_prompt_contains_quality_keywords` 方法**——"你看这个测试，它不测 API，它测的是 SYSTEM_PROMPT 里有没有'压缩规则''输出格式''质量控制'这些关键词"
5. **鼠标指向参数化列表中的关键词**——"Prompt 如果没写清楚输出格式要求，LLM 就不会按 JSON 输出；如果没写字符限制，闪卡就会超长。**Prompt 是 AI 系统的第一道防线。**"
6. **滚回顶部，鼠标在几个类名之间划一圈**——"总共 26 个用例，全部不消耗 API 配额"

**边说边指**：

> "这个项目特殊在哪——它的核心依赖是 LLM，而 LLM 的输出天生不稳定。传统 API 测 200 就完了，但 LLM 不行——返回格式可能有 8 种变体、可能半截 JSON 断了、可能 Prompt Injection 了。"
>
> "所以我专门拆了 5 个维度来测——（鼠标缓慢划过 5 个 TestClass）格式合规、安全性、性能、容错、Prompt 质量。注意一个关键设计——**所有测试不消耗真实 API 配额**。我 Mock 了 LLM 调用层，只测解析逻辑和 Prompt 模板。这样快、免费、不会 flaky。"
>
> "尤其这个 Prompt 质量测试（鼠标指 `TestAIPromptQuality`）——很少有测试工程师会去测 Prompt 模板。但你想，如果发给 LLM 的指令本身没写清楚字符限制，LLM 就不会遵守。**Prompt 是源头，源头错了后面全错。**"

**认知点**：
> "测 AI 系统和测传统系统的核心区别——输出不确定怎么办？我的策略是三层分层：**格式层做确定性校验**（JSON schema、字段完整性），**Prompt 层做源头校验**（指令是否正确完整），**容错层做兜底保护**（空返回、残缺 JSON 不崩溃）。这套方法论对任何 AI 产品都适用。"

**展示预存结果**：打开 `demo-results/03-ai-quality.txt` → Ctrl+End 到底部 → 鼠标指 `26 passed in 0.15s` → "26 个 AI 质量用例，全部通过，0.15 秒跑完。"

**过渡**：
> "再给你看一个小的但体现工程思维的——（打开 `conftest.py`）这个文件只有 35 行，但体现了测试策略怎么落地。"

---

### 步骤 4：pytest markers — 测试策略的工程化管理（30 秒）

**你要证明的**：你理解测试策略不是一个一个用例，而是一套执行规则。

**操作**：

1. **双击 `tests/conftest.py` 打开文件**
2. **鼠标从上到下缓慢划过 5 个 `config.addinivalue_line("markers", ...)` 调用**
3. **逐个指**——"slow 标记真正的 LLM API 调用，ai 标记消耗配额的，cv 标记音视频处理，e2e 标记需要启动服务的，unit 标记纯逻辑无依赖的"
4. **收起文件，面向面试官**

**边说边指**：

> "5 个 markers，不是用来分类好看的，（鼠标逐个指）是拿来控制执行策略的。实际用法是 `pytest -m 'not slow and not ai'`——一键跳过所有要调 LLM 的测试。这在 CI 里特别有用：PR 提交跑完整测试，commit push 只跑 unit，发版前跑全量。**测试不是越多越好，是在正确的时间跑正确的测试。**"

**认知点**：
> "markers 定义不是写个 `pytest.ini` 就完了——你要考虑团队的测试执行节奏：哪些每次 commit 跑、哪些 nightly build 跑、哪些发版前跑。**测试策略不只是在用例里，也在执行规则里。**"

**过渡**：
> "好，光看代码不够——（切换到终端，Ctrl+`）我跑一下给你看实际结果。不想跑的话我已经提前跑好了，直接看结果文件也行。"

**备选（预存结果）**：打开 `demo-results/07-custom-markers.txt`，展示 5 个自定义 markers 的注册信息。

---

### 步骤 5：运行测试看结果（30 秒）

**你要证明的**：91 个用例 0 失败不是吹的，当场跑给你看。

**方案 A（推荐）：展示预存结果文件**

> 不依赖终端环境，最稳定，不会翻车。

1. **在 VS Code 左侧文件树中，展开 `demo-results/` 文件夹**
2. **双击打开 `01-core-tests-verbose.txt`**
3. **直接滚动到文件末尾**（Ctrl+End），鼠标指向最后一行：
   ```
   ======================== 91 passed, 3 skipped in 0.23s ========================
   ```
4. **鼠标指向 `91 passed`**——"91 个全过"
5. **鼠标指向 `3 skipped`**——"3 个跳过是因为没有 .mp4 测试文件，自动 skip"
6. **鼠标指向 `0.23s`**——"0.23 秒，不到四分之一秒跑完 91 个用例"
7. **往上翻几页**，让面试官看到密密麻麻的 PASSED 列表——"你看，每个参数化用例都有独立的测试 ID"

**方案 B：终端实时运行**

> 如果面试环境允许、想展示实时执行效果。

1. **切换到 VS Code 终端**（Ctrl+`），确保在项目根目录
2. **输入以下命令并回车**：
   ```bash
   pytest tests/test_flashcard_parse_parametrize.py tests/test_ai_output_quality.py tests/test_cv_api_validation.py tests/test_profile_aggregator.py -v --tb=short
   ```
3. **等待输出滚动完毕（大约 1-2 秒）**
4. **鼠标指向最后两行**——`91 passed, 3 skipped in 0.23s`

**边说边指**（两种方案共用）：

> "刚刚看的那些文件，当场跑——（终端滚动完毕，鼠标指结果行）91 passed，0 failed，3 skipped。skipped 的 3 个是 CV 测试，需要 .mp4 文件，没有文件自动跳过不会报错。"
>
> "注意时间——**0.24 秒跑完 91 个用例**。因为 LLM 调用全部 Mock 隔离了，不需要等网络。Mock 的价值不只是省 API 钱——**速度快了开发者才愿意跑。** 一个 10 分钟的测试没人每次 commit 都跑；0.24 秒，保存一次跑一次都没压力。"

**认知点**：
> "**测试的执行速度决定了它会不会被真正使用。** 再完善的测试如果太慢，最终都会沦为 CI 上的装饰品。所以我设计测试的时候，Mock 隔离是默认策略——快才能被高频执行。"

**过渡**：
> "代码层面的测试看完了，我再给你看一下接口测试和工程化这块——（切换到 Postman 窗口）"

---

### 步骤 6：Postman Collection（1 分钟）

**你要证明的**：你的 Postman 不是存几个 curl，是有组织、有脚本、可执行的 API 文档。

**操作**：

1. **Alt+Tab 切换到 Postman 窗口**
2. **鼠标指向左侧 Collections 列表中的 "星识 Star-Learn API"**——确保面试官看到集合名称
3. **点击集合名左边的 ▶ 展开，露出 5 个文件夹**——：
   ```
   📁 用户模块 (4)
   📁 学习模块 (5)
   📁 视频模块 (2)
   📁 画像模块 (2)
   📁 系统模块 (3)
   ```
4. **鼠标从上到下缓慢划过 5 个文件夹**——"按功能模块分的，不是堆在一起"
5. **点击 "学习模块" 文件夹展开，点击 "生成闪卡" 请求**
6. **在右侧面板中，点击 **Pre-request Script** 标签页**——展示数据准备的代码
7. **点击 **Tests** 标签页**——展示自动化断言的代码
8. **鼠标选中 Tests 里的断言代码，逐条指**：
   - `pm.test("状态码 200", ...)` — "第一条断言：状态码"
   - `pm.test("返回 success 为 true", ...)` — "第二条：业务状态"
   - `pm.test("每张卡片字段完整", ...)` — "第三条：字段完整性"
9. **鼠标移到右上角的集合变量区域**——"`base_url` 和 `test_user_id` 在这里统一管理，所有请求共享"

**边说边指**：

> "17 个接口，5 个功能模块——（鼠标逐个指文件夹）用户模块 4 个、学习模块 5 个、视频 2 个、画像 2 个、系统 3 个。不是随便存的 curl 命令。"
>
> "每个请求都配了两段脚本。（点击 Pre-request Script）Pre-request Script 做数据准备——比如生成测试用的 userId、设置请求头。（点击 Tests）Tests 做自动化断言——不是只测 200，你看这里（鼠标指代码），我验证了三层：状态码、业务返回的 success 字段、还有 flashcards 数组里每张卡片 front 和 back 不能为空。"
>
> "集合变量 `base_url` 在这统一管理（鼠标指变量区域），登录成功后 token 自动写入环境变量，后面所有请求自动带上 Authorization——不用手动复制粘贴。"

**认知点**：
> "**好的 Postman Collection 本身就是一个可执行的 API 文档。** 新人来了导入就能跑，就能看懂所有接口的契约。Tests 脚本是活的——接口改了、断言马上就挂，比静态文档可靠得多。"

**过渡**：
> "代码测试和接口测试讲完了，但测试开发不只是写测试——（Alt+Tab 切回 VS Code，打开 `scripts/` 目录）我给你看一下我主动做的质量防线。"

---

### 步骤 7：审计工具 + 测试左移（1 分钟）

**你要证明的**：你主动往前一步建了质量防线——不是等 Bug 报修，而是在 Bug 产生之前拦截。

**操作**：

1. **回到 VS Code，在左侧文件树中展开 `scripts/` 目录**
2. **鼠标从上到下逐个指 5 个审计脚本**，每指一个停顿一下：
   - `audit_compound_selectors.py` — "这个扫 CSS 文件里的 `*` 选择器和 body 选择器，会污染全局样式"
   - `audit_frontend_smells.py` — "这个扫内联样式、魔法数字、console.log、未完成的 TODO"
   - `audit_undefined_css_vars.py` — "这个扫引用了但没有定义的 CSS 变量，样式会静默失效，肉眼根本看不出来"
   - `build_tech_debt_inventory.py` — "汇总上面所有结果，按 P0 到 P3 排优先级，生成 Markdown 报告"
   - `fix_p0_tech_debt.py` — "P0 级别的自动修——注入缺失的 CSS 变量、删除溢出规则"
3. **鼠标移到 `tests/scripts/` 目录**——"审计工具本身也有测试，用临时文件隔离，不碰真实代码"
4. **收起文件树，面对面试官**

**边说边指**：

> "这 5 个审计脚本不是需求要求的，是我主动写的。思考很简单——（鼠标指 `audit_undefined_css_vars.py`）一个引用了但没有定义的 CSS 变量，页面渲染出来是白的，用户看不到内容但是不会报错。手工检查 30 个页面的 CSS 变量？查一天都查不完。"
>
> "所以我写了这些审计工具——开发提交代码的时候自动扫，扫出问题按 P0 到 P3 排优先级（鼠标指 `build_tech_debt_inventory.py`），P0 的自动修（鼠标指 `fix_p0_tech_debt.py`）。**这就是测试左移——把质量检查从'测出来再修'变成'提交前就拦住'。**"
>
> "而且（鼠标移到 `tests/scripts/`）这些审计工具自己也写了单元测试。用临时文件隔离，不碰真实 CSS 文件。**质量工具也是软件，也要被测试。**"

**认知点**：
> "**自动化测试和测试自动化是两回事**。自动化测试是用工具执行测试用例；测试自动化是把一切可以自动化的质量活动都自动化——代码扫描、CSS 变量检查、Prompt 模板校验。这几个审计脚本属于后者。工具替你查、替你修、生成报告——人只做决策。"

**过渡**：
> "这些都跑在哪呢——（打开 `.github/workflows/test.yml`）我搭了一个 CI 流水线把上面这些串起来。"

---

### 步骤 8：CI/CD 流水线（30 秒）

**你要证明的**：你知道怎么把测试集成到开发流程里，不是"写了测试但没人跑"。

**操作**：

1. **在 VS Code 中打开 `.github/workflows/test.yml`**
2. **鼠标指向 `jobs:` 下面的 4 个 Job 名称**：
   - `python-tests` — "Python 测试：pytest + coverage → html 报告"
   - `frontend-unit` — "前端单元测试：Vitest + junit reporter"
   - `e2e-tests` — "Playwright E2E：先启动 dev server 再跑，截图作为 artifact"
   - `static-audit` — "静态审计：跑那 5 个审计脚本 + 它们自己的单元测试"
3. **鼠标指向每个 Job 的 `timeout-minutes`**——"每个 Job 都设了超时，防止卡死"
4. **鼠标指向 artifact 上传部分**——"E2E 失败了会自动上传截图，排查的时候一眼看到问题"

**边说边指**：

> "4 个并行 Job，（鼠标逐个指）Python 测试、前端单元、Playwright E2E、静态审计，各跑各的互不阻塞。都设了超时，失败了自动上传截图。（收起文件）整个过程 10 分钟内全跑完。"

**认知点**：
> "CI 配置的关键不是'能跑'——是**跑得快、失败了好找原因**。并行化保证速度，artifact 上传保证可追溯。流水线是测试策略的最后一道自动化防线。"

**过渡**：
> "最后给你看一下 Selenium——（打开 `tests/selenium/test_login_selenium.py`）虽然我主力用 Playwright，但 Selenium 我实际也用过。"

---

### 步骤 9：Selenium — 证明"我也会"（30 秒）

**你要证明的**：你不是只会 Playwright，Selenium 也实际写过——而且理解两者的差异。

**操作**：

1. **双击 `tests/selenium/test_login_selenium.py` 打开文件**
2. **滚动到文件开头（import guard 部分，大约前 15 行）**——鼠标指 `SELENIUM_AVAILABLE` 变量和 `try/except ImportError` 块
3. **滚动到元素定位部分**——鼠标逐行指三种定位策略：
   - `By.CSS_SELECTOR, "input[name='username']"` — "CSS 选择器定位用户名输入框"
   - `By.XPATH, "//button[contains(text(),'登录')]"` — "XPath 定位登录按钮"
   - `By.ID, "submit-btn"` — "ID 定位"
4. **滚动到显式等待部分**——鼠标指 `WebDriverWait` 和 `EC.visibility_of_element_located`
5. **鼠标指 `import guard` 下面 `@unittest.skipUnless(SELENIUM_AVAILABLE, ...)` 装饰器**——"没装 selenium 的时候自动 skip，不阻塞"

**边说边指**：

> "Selenium 我单独写了一整套登录页的测试——三个关键点：（鼠标逐个指）三种定位策略，CSS Selector、XPath、ID 全用了；（鼠标指显式等待）显式等待 `WebDriverWait` + `expected_conditions`，等表单卡片出现再操作；（鼠标指 import guard）import guard ——没装 selenium 的时候测试自动 skip，不会报错。"
>
> "实际上 Selenium 和 Playwright 我对比过——定位策略两边完全通用，差异在 Selenium 需要手动管理 WebDriver 和显式等待，Playwright 内置了自动等待和浏览器管理。理解原理之后，切工具只是换 API 名字。"

**认知点**：
> "工具会变，但**定位策略、等待策略、页面对象模式这些方法是通用的**。面试官不用纠结我用 Playwright 还是 Selenium——两个我都实际写过，换哪个都能上手。"

---

### 演示收尾

**全部展示完后说**：

> "以上就是在项目里实际做的测试工作。总结一下：用例设计上黑盒白盒都用、而且会配合使用；自动化上 Python 测试、前端测试、Postman、CI 流水线都搭了；我还额外做了 AI 输出质量专项测试和自动化审计工具——算是超出了常规测试工程师的工作范围。这就是我对测试开发的理解：**不只是写用例，是给整个团队搭质量基础设施。**"

---

### 演示时间控制

| 面试节奏 | 演示步骤 | 累计时间 |
|---------|---------|---------|
| 时间充裕 | 全部 9 步 | 6-8 分钟 |
| 时间偏紧 | 跳过 7+8+9，只做 1→2→3→4→5→6 | 4-5 分钟 |
| 时间很紧 | 只做 1→2→3→5 | 3 分钟 |
| 面试官指定看 | 直接跳到对应步骤 | 1-2 分钟 |

### 演示禁忌

- ❌ **不要逐行读代码**——指结构、讲设计思路。面试官自己会看代码内容
- ❌ **不要纠结细节**——比如"这个变量名取得不好"，只展示亮点
- ❌ **终端执行失败不要慌**——说"环境依赖问题，不影响代码逻辑，我给你看之前跑的结果截图"
- ❌ **不要抢话**——每展示完一个步骤停一下，问"要不要展开看这块的细节？"
- ❌ **不要频繁切换窗口**——尽量在 VS Code 内完成所有代码展示，只在 Postman 和 CI 时切换
- ❌ **不要用鼠标乱晃**——指哪里就停在哪里，让面试官看懂你在指什么

---

> 📅 最后更新：2026-06-08
> 📂 基于项目：[星识 (Star-Learn) 伴学系统](.)
