---
 部署步骤

 步骤 1：环境准备

 # 创建虚拟环境
 python -m venv cogvideo_env
 # Windows: cogvideo_env\Scripts\activate

 # 安装 PyTorch (CUDA 11.8)
 pip install torch torchvision torchaudio -i https://pypi.tuna.tsinghua.edu.cn/simple

 # 安装 ModelScope 和依赖
  国内加速安装方法：

  方法 1：使用清华镜像（推荐）

  pip install torch torchvision torchaudio -i https://pypi.tuna.tsinghua.edu.cn/simple

  方法 2：使用阿里镜像

  pip install torch torchvision torchaudio -i https://mirrors.aliyun.com/pypi/simple

  方法 3：先改镜像源再安装

  # 设置全局镜像源
  pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple

  # 然后正常安装
  pip install torch torchvision torchaudio
  
 步骤 2：下载模型

 模型来源：https://huggingface.co/THUDM/CogVideoX-2B

 会自动下载，首次使用时会提示登录 HuggingFace token（用于接受协议）。

3. 测试文生视频
  python cogvideo_text_to_video.py --prompt "一个男人在公园里跑步"

  4. 测试图生视频
  python cogvideo_image_to_video.py --image input.png --prompt "这个人开始走路"

  5. 启动 API 服务
  pip install fastapi uvicorn python-multipart
  uvicorn cogvideo_api:app --reload --port 8000