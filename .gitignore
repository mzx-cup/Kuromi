# ==========================================
# 1. 环境变量配置 (Environment Variables)
# ==========================================
.env
.env.local
.env.*.local

# ==========================================
# 2. Python 虚拟环境与缓存 (Python Environment)
# ==========================================
.venv/
venv/
env/
__pycache__/
*.pyc
.pytest_cache/

# ==========================================
# 3. 前端依赖与构建产物 (Frontend & Build)
# ==========================================
node_modules/
dist/

# ==========================================
# 4. 数据库文件 (Databases)
# ==========================================
# 使用通配符，自动拦截包含 xingshi.db、starlearn_v2.db 在内的所有本地数据库及缓存
*.db
*.sqlite3
*.db-shm
*.db-wal

# ==========================================
# 5. 业务本地存储与临时文件 (Project Storage)
# ==========================================
storage/state_storage/
storage/task_storage/
.worktrees/
# 已迁移到 xingshi_v2.db 的遗留 JSON 存储 (SLICE_STATUS 阶段 2.3)
# 保留在磁盘作为只读归档, 不再纳入版本控制
local_storage.json
local_storage.json.pre-migration.bak

# 忽略临时 JSON，但强制保留项目核心的包管理配置！
*.json
!package.json
!package-lock.json
!tsconfig.json


# ==========================================
.superpowers/

# 6. 编辑器与系统生成文件 (IDE & System)
# ==========================================
.vscode/
.idea/
.DS_Store

# ==========================================
# 7. 自动化测试与浏览器缓存 (Browser Profiles)
# ==========================================
.edge-*/
chrome-profile/
user-data-dir/=0.*
*.backup

# 测试报告（自动生成的 HTML/JSON）
tests/frontend/reports/

# 前端单元测试覆盖率
coverage/

# Playwright 测试结果
test-results/

# 开发产物 (截图/测试输出/验证报告/审计) 统一在 artifacts/
artifacts/

# E2E screenshots directory (keep folder, ignore PNGs)
tests/frontend/e2e/__screenshots__/*.png

# ==========================================
# 8. 比赛/演示运行产物 (Competition Artifacts) — P0 Task 11
# ==========================================
# 媒体生成 (TTS/ASR/Bilibili) 临时音频
audio/
# 演示打包产物
packaging/
# superpowers 运行时数据
.superpowers/data/
# agent 沙箱 worktree (用户已有未提交改动)
.claude/worktrees/
# codex 缓存
.codex/

