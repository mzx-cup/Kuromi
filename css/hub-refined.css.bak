/* ============================================
   hub-refined.css — 仿造个人网页配色
   仅覆盖颜色与装饰属性，不改动布局/功能
   ============================================ */

/* --------------------------------------------
   1. 品牌色变量（仅自定义变量，不覆盖主题变量）
   -------------------------------------------- */
.hub-body {
    --accent-warm: #f59e0b;
    --accent-purple: #a78bfa;
    --accent-cyan: #67e8f9;
    --accent-green: #34d399;
    --accent-orange: #fbbf24;
    --accent-warm-soft: rgba(245, 158, 11, 0.12);
    --accent-warm-glow: rgba(245, 158, 11, 0.25);
    --accent-purple-soft: rgba(167, 139, 250, 0.12);
    --glass-warm: rgba(245, 158, 11, 0.08);
}

/* --------------------------------------------
   2. 背景层 — 支持壁纸显示
   -------------------------------------------- */
.hub-body .hub-bg {
    background: transparent;
}

/* 壁纸层兜底：确保 index.css 未加载时壁纸仍能显示 */
.hub-body .global-bg-layer {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -2;
    pointer-events: none;
    background-image: var(--leleo-bg-image);
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    filter: brightness(var(--leleo-brightness, 85%)) blur(var(--leleo-blur, 5px));
    transition: background-image 0.6s ease;
}

.hub-body .global-bg-video {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -2;
    pointer-events: none;
    filter: brightness(var(--leleo-brightness, 85%)) blur(var(--leleo-blur, 5px));
}

/* --------------------------------------------
   3. 导航栏
   -------------------------------------------- */
.hub-body .hub-navbar {
    background: linear-gradient(180deg, rgba(13, 13, 20, 0.95) 0%, rgba(13, 13, 20, 0.85) 100%);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
}

.hub-body .logo-svg {
    filter: drop-shadow(0 0 8px rgba(245, 158, 11, 0.3));
}

.hub-body .nav-item {
    color: var(--text-tertiary);
    border: 1px solid transparent;
    transition: all 0.25s ease;
}

.hub-body .nav-item:hover {
    color: #f59e0b;
    background: rgba(245, 158, 11, 0.08);
    border-color: rgba(245, 158, 11, 0.15);
}

.hub-body .nav-item.active {
    color: #f59e0b;
    background: rgba(245, 158, 11, 0.12);
    border-color: rgba(245, 158, 11, 0.25);
    font-weight: 600;
}

.hub-body .nav-item svg {
    color: inherit;
}

.hub-body .nav-flow-line {
    stroke: rgba(245, 158, 11, 0.3);
}

.hub-body .nav-flow-dot {
    fill: #f59e0b;
    filter: drop-shadow(0 0 4px rgba(245, 158, 11, 0.5));
}

/* 主题设置 & 通知按钮 */
.hub-body .theme-settings-btn,
.hub-body .notification-btn {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.06);
    color: var(--text-tertiary);
    transition: all 0.25s ease;
}

.hub-body .theme-settings-btn:hover,
.hub-body .notification-btn:hover {
    background: rgba(245, 158, 11, 0.12);
    border-color: rgba(245, 158, 11, 0.2);
    color: #f59e0b;
}

.hub-body .notification-dot {
    background: #f59e0b;
    box-shadow: 0 0 0 2px rgba(245, 158, 11, 0.3);
}

.hub-body .user-avatar {
    border: 2px solid rgba(245, 158, 11, 0.3);
    transition: all 0.3s ease;
}

.hub-body .user-avatar:hover {
    border-color: #f59e0b;
    box-shadow: 0 0 12px rgba(245, 158, 11, 0.3);
}

.hub-body .online-indicator {
    background: #34d399;
    border: 2px solid var(--bg-primary);
}

/* --------------------------------------------
   4. 侧边栏
   -------------------------------------------- */
.hub-body .hub-sidebar {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.04);
    border-radius: 12px;
}

.hub-body .sidebar-section-title {
    color: var(--text-heading);
    font-weight: 700;
}

.hub-body .sidebar-section-title::before {
    background: linear-gradient(180deg, #f59e0b 0%, #a78bfa 100%);
}

/* --------------------------------------------
   5. 主卡片
   -------------------------------------------- */
.hub-body .hub-card {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.04);
    border-radius: 12px;
    transition: all 0.3s ease;
}

.hub-body .hub-card:hover {
    border-color: rgba(245, 158, 11, 0.15);
    box-shadow: 0 8px 32px rgba(245, 158, 11, 0.06);
}

.hub-body .hub-card-header {
    border-bottom: 1px solid rgba(255, 255, 255, 0.04);
}

.hub-body .hub-card-title {
    color: var(--text-heading);
    font-weight: 700;
}

.hub-body .hub-card-title::before {
    background: linear-gradient(180deg, #f59e0b 0%, #a78bfa 100%);
}

.hub-body .hub-card-icon {
    color: #f59e0b;
}

/* --------------------------------------------
   6. 进度条
   -------------------------------------------- */
.hub-body .progress-bar {
    background: rgba(255, 255, 255, 0.06);
    border-radius: 999px;
}

.hub-body .progress-fill {
    background: linear-gradient(90deg, #f59e0b 0%, #a78bfa 100%);
    border-radius: 999px;
}

.hub-body .progress-text {
    color: #f59e0b;
    font-weight: 700;
}

/* --------------------------------------------
   7. 统计数据
   -------------------------------------------- */
.hub-body .stat-number {
    color: var(--text-heading);
    font-weight: 800;
}

.hub-body .stat-label {
    color: var(--text-muted);
}

.hub-body .stat-trend-up {
    color: #34d399;
}

.hub-body .stat-trend-down {
    color: #f87171;
}

.hub-body .stat-badge {
    background: rgba(245, 158, 11, 0.12);
    color: #f59e0b;
    border-radius: 6px;
    font-weight: 600;
}

/* --------------------------------------------
   8. 聚焦数据 / ECG
   -------------------------------------------- */
.hub-body .focus-data {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.04);
}

.hub-body .ecg-line {
    stroke: url(#ecgGradient);
    filter: drop-shadow(0 0 6px rgba(245, 158, 11, 0.3));
}

.hub-body .focus-value {
    color: #f59e0b;
    font-weight: 800;
}

.hub-body .focus-label {
    color: var(--text-muted);
}

/* --------------------------------------------
   9. 时间轴
   -------------------------------------------- */
.hub-body .timeline-item::before {
    background: linear-gradient(180deg, #f59e0b 0%, #a78bfa 100%);
}

.hub-body .timeline-dot {
    background: #f59e0b;
    border: 2px solid var(--bg-primary);
    box-shadow: 0 0 8px rgba(245, 158, 11, 0.4);
}

.hub-body .timeline-time {
    color: var(--text-muted);
}

.hub-body .timeline-title {
    color: var(--text-heading);
    font-weight: 600;
}

.hub-body .timeline-desc {
    color: var(--text-tertiary);
}

/* --------------------------------------------
   10. 聊天消息
   -------------------------------------------- */
.hub-body .chat-message {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.04);
}

.hub-body .chat-message.own {
    background: rgba(245, 158, 11, 0.08);
    border-color: rgba(245, 158, 11, 0.15);
}

.hub-body .chat-avatar {
    border: 2px solid rgba(245, 158, 11, 0.2);
}

.hub-body .chat-name {
    color: var(--text-heading);
    font-weight: 600;
}

.hub-body .chat-time {
    color: var(--text-muted);
}

.hub-body .chat-text {
    color: var(--text-secondary);
}

/* --------------------------------------------
   11. 任务列表
   -------------------------------------------- */
.hub-body .task-item {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.04);
    transition: all 0.25s ease;
}

.hub-body .task-item:hover {
    border-color: rgba(245, 158, 11, 0.15);
    background: rgba(245, 158, 11, 0.04);
}

.hub-body .task-checkbox {
    accent-color: #f59e0b;
}

.hub-body .task-title {
    color: var(--text-secondary);
}

.hub-body .task-title.completed {
    color: var(--text-muted);
    text-decoration: line-through;
}

.hub-body .task-priority-high {
    color: #f87171;
}

.hub-body .task-priority-medium {
    color: #fbbf24;
}

.hub-body .task-priority-low {
    color: #34d399;
}

/* --------------------------------------------
   12. 热力图
   -------------------------------------------- */
.hub-body .heatmap-cell {
    border-radius: 3px;
}

.hub-body .heatmap-cell.level-1 { background: rgba(245, 158, 11, 0.15); }
.hub-body .heatmap-cell.level-2 { background: rgba(245, 158, 11, 0.3); }
.hub-body .heatmap-cell.level-3 { background: rgba(245, 158, 11, 0.5); }
.hub-body .heatmap-cell.level-4 { background: rgba(245, 158, 11, 0.75); }
.hub-body .heatmap-cell.level-5 { background: #f59e0b; }

/* --------------------------------------------
   13. 宠物
   -------------------------------------------- */
.hub-body .pet-avatar {
    border: 2px solid rgba(245, 158, 11, 0.3);
    transition: all 0.3s ease;
}

.hub-body .pet-avatar:hover {
    border-color: #f59e0b;
    box-shadow: 0 0 16px rgba(245, 158, 11, 0.3);
}

.hub-body .pet-name {
    color: var(--text-heading);
    font-weight: 700;
}

.hub-body .pet-mood {
    color: #f59e0b;
}

/* --------------------------------------------
   14. 加载动画
   -------------------------------------------- */
.hub-body .skeleton {
    background: linear-gradient(90deg, rgba(255,255,255,0.03) 25%, rgba(255,255,255,0.06) 50%, rgba(255,255,255,0.03) 75%);
    background-size: 200% 100%;
}

/* --------------------------------------------
   15. 通知面板
   -------------------------------------------- */
.hub-body .notification-panel {
    background: rgba(26, 26, 46, 0.95);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 12px;
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    box-shadow: 0 16px 48px rgba(0, 0, 0, 0.3);
}

.hub-body .notification-header {
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
}

.hub-body .notification-header h3 {
    color: var(--text-heading);
    font-weight: 700;
}

.hub-body .notification-item {
    border-bottom: 1px solid rgba(255, 255, 255, 0.04);
    transition: background 0.2s ease;
}

.hub-body .notification-item:hover {
    background: rgba(245, 158, 11, 0.05);
}

.hub-body .notification-item.unread {
    background: rgba(245, 158, 11, 0.04);
}

.hub-body .notification-icon.system {
    background: rgba(103, 232, 249, 0.12);
    color: #67e8f9;
}

.hub-body .notification-icon.success {
    background: rgba(52, 211, 153, 0.12);
    color: #34d399;
}

.hub-body .notification-icon.warning {
    background: rgba(251, 191, 36, 0.12);
    color: #fbbf24;
}

.hub-body .notification-icon.error {
    background: rgba(248, 113, 113, 0.12);
    color: #f87171;
}

.hub-body .notification-content h4 {
    color: var(--text-heading);
    font-weight: 600;
}

.hub-body .notification-content p {
    color: var(--text-tertiary);
}

.hub-body .notification-time {
    color: var(--text-muted);
}

/* --------------------------------------------
   16. 新闻弹窗
   -------------------------------------------- */
.hub-body .news-modal-overlay {
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(4px);
}

.hub-body .news-modal-card {
    background: rgba(26, 26, 46, 0.98);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 16px;
    box-shadow: 0 24px 64px rgba(0, 0, 0, 0.4);
}

.hub-body .news-modal-header {
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
}

.hub-body .news-modal-title {
    color: var(--text-heading);
    font-weight: 800;
}

.hub-body .news-tab {
    color: var(--text-tertiary);
    background: transparent;
    border: 1px solid transparent;
    transition: all 0.2s ease;
}

.hub-body .news-tab:hover {
    color: #f59e0b;
    background: rgba(245, 158, 11, 0.08);
}

.hub-body .news-tab.active {
    color: #f59e0b;
    background: rgba(245, 158, 11, 0.12);
    border-color: rgba(245, 158, 11, 0.25);
    font-weight: 600;
}

.hub-body .news-modal-close {
    color: var(--text-muted);
    transition: color 0.2s;
}

.hub-body .news-modal-close:hover {
    color: #f59e0b;
}

/* --------------------------------------------
   17. 按钮
   -------------------------------------------- */
.hub-body .btn-primary {
    background: linear-gradient(135deg, #f59e0b 0%, #a78bfa 100%);
    color: var(--text-heading);
    border: none;
    border-radius: 8px;
    font-weight: 600;
    transition: all 0.25s ease;
}

.hub-body .btn-primary:hover {
    box-shadow: 0 4px 16px rgba(245, 158, 11, 0.35);
    filter: brightness(1.1);
}

.hub-body .btn-secondary {
    background: rgba(255, 255, 255, 0.06);
    color: var(--text-secondary);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 8px;
    transition: all 0.25s ease;
}

.hub-body .btn-secondary:hover {
    background: rgba(245, 158, 11, 0.1);
    border-color: rgba(245, 158, 11, 0.2);
    color: #f59e0b;
}

/* --------------------------------------------
   18. 标签/徽章
   -------------------------------------------- */
.hub-body .tag-warm {
    background: rgba(245, 158, 11, 0.12);
    color: #f59e0b;
    border-radius: 6px;
    font-weight: 600;
}

.hub-body .tag-purple {
    background: rgba(167, 139, 250, 0.12);
    color: #a78bfa;
    border-radius: 6px;
    font-weight: 600;
}

.hub-body .tag-cyan {
    background: rgba(103, 232, 249, 0.12);
    color: #67e8f9;
    border-radius: 6px;
    font-weight: 600;
}

.hub-body .tag-green {
    background: rgba(52, 211, 153, 0.12);
    color: #34d399;
    border-radius: 6px;
    font-weight: 600;
}

.hub-body .tag-orange {
    background: rgba(251, 191, 36, 0.12);
    color: #fbbf24;
    border-radius: 6px;
    font-weight: 600;
}

/* --------------------------------------------
   19. 输入框
   -------------------------------------------- */
.hub-body input[type="text"],
.hub-body input[type="search"],
.hub-body textarea {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.06);
    color: var(--text-secondary);
    border-radius: 8px;
    transition: all 0.2s ease;
}

.hub-body input[type="text"]:focus,
.hub-body input[type="search"]:focus,
.hub-body textarea:focus {
    border-color: rgba(245, 158, 11, 0.3);
    box-shadow: 0 0 0 3px rgba(245, 158, 11, 0.1);
    outline: none;
}

.hub-body input::placeholder {
    color: var(--text-muted);
}

/* --------------------------------------------
   20. 滚动条
   -------------------------------------------- */
.hub-body ::-webkit-scrollbar {
    width: 6px;
    height: 6px;
}

.hub-body ::-webkit-scrollbar-track {
    background: transparent;
}

.hub-body ::-webkit-scrollbar-thumb {
    background: rgba(255, 255, 255, 0.08);
    border-radius: 3px;
}

.hub-body ::-webkit-scrollbar-thumb:hover {
    background: rgba(245, 158, 11, 0.3);
}

/* --------------------------------------------
   21. 响应式
   -------------------------------------------- */
@media (max-width: 768px) {
    .hub-body .hub-sidebar {
        background: rgba(26, 26, 46, 0.98);
    }
}
