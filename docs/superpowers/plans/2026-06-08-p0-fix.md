# P0 前端技术债修复 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不引入视觉 regression 的前提下，单 commit 修复 217 个 P0 前端技术债（212 个未定义 CSS 变量 + 5 个复合选择器外溢）。

**Architecture:** 1 个 Python 数据驱动修复脚本读 audit JSON → 语义推断 + 静态规则生成 `tokens.css` 追加 + 5 个外溢文件删除 → 三重验收 (L1 静态 + L2 HTTP + L3 Playwright) 验证。

**Tech Stack:**
- Python 3.10+ (标准库)
- Playwright 1.45.0 (npm，已通过 `npm config set cache` 修复 ACL 问题)
- 复用 3 个已有 audit 脚本的 JSON 输出

---

## 文件结构

### 新建（生产代码）
| 文件 | 职责 |
|------|------|
| `scripts/inference_rules.py` | 静态映射表 + `infer_value(var_name) -> (value, source_tag)` 纯函数 |
| `scripts/fix_p0_tech_debt.py` | 读 audit JSON，应用 212 vars + 5 文件删除 |

### 新建（测试代码）
| 文件 | 职责 |
|------|------|
| `tests/scripts/test_inference_rules.py` | unittest，纯函数 |
| `tests/scripts/test_fix_p0_tech_debt.py` | unittest，tempfile fixture |
| `tests/fixtures/fix/sample-tokens.css` | mock tokens.css |
| `tests/fixtures/fix/sample-overflow.css` | mock 外溢文件（含 *、html、body 规则） |

### 修改（修复目标）
| 文件 | 变更 |
|------|------|
| `css/tokens.css` | 末尾追加新 `:root` 块（含 212 vars，按字母排序） |
| `css/classroom-premium-dna.css` | 删除 `*`/`html`/`body` 顶层规则 |
| `css/hub.css` | 同上 |
| `css/index.css` | 同上 |
| `css/personal.css` | 同上 |
| `css/plant.css` | 同上 |

### 修改（基础设施）
| 文件 | 变更 |
|------|------|
| `package.json` / `package-lock.json` | `npm install` 添加 `@playwright/test` (已在 devDeps) |
| `tests/frontend/screenshots/` | 新增 visual.spec.js 基准截图 (Playwright `--update-snapshots` 产物) |

### 不入库（备份）
| 文件 | 用途 |
|------|------|
| `css/tokens.css.bak-pre-p0-fix` | 紧急回退，commit 前删除 |

---

## 通用约定

- **Windows 兼容**：Python 脚本入口加 `sys.stdout.reconfigure(encoding="utf-8")`
- **退出码**：`0` = OK，`1` = 验收失败或缺输入，`2` = 工具自身故障
- **commit message 中文**：与项目历史一致
- **Playwright 环境**：所有 Playwright 命令前置 `PLAYWRIGHT_BROWSERS_PATH=$HOME/playwright-browsers` 避免污染默认路径

---

## Task 1: 安装 Playwright + 生成 visual baseline

**Files:**
- Modify: `package.json` / `package-lock.json` (via npm install)
- Create: `tests/frontend/screenshots/*.png` (baseline screenshots)

- [ ] **Step 1: 确认 npm cache 修复仍生效**

```bash
npm config get cache
```

预期：输出 `C:\Users\zwc\npm-cache`（如不是，重跑 `npm config set cache "C:\Users\zwc\npm-cache"`）。

- [ ] **Step 2: 安装 @playwright/test**

```bash
cd "c:/Users/zwc/Downloads/Kuromi-main/Kuromi-main"
npm install
```

预期：`@playwright/test` 已安装 (在 devDeps 声明)。退出码 0。

- [ ] **Step 3: 验证 Playwright 可执行**

```bash
npx playwright --version
```

预期：输出 `Version 1.45.0` (与 package.json 锁定版本一致)。

- [ ] **Step 4: 下载 Chromium 浏览器**

```bash
npx playwright install chromium
```

预期：下载 300MB Chromium 到 `~/.cache/ms-playwright/`。退出码 0。

**若失败**（按用户指示：不降级）:
- 重试 3 次（指数退避）：`for i in 1 2 3; do sleep $((i*i)); npx playwright install chromium && break; done`
- 仍失败 → 切镜像源：
  ```bash
  export PLAYWRIGHT_DOWNLOAD_HOST=https://npmmirror.com/mirrors/playwright
  npx playwright install chromium
  ```
- 仍失败 → 输出错误信息（具体 URL、磁盘空间、代理设置），**不降级，要求人工干预**

- [ ] **Step 5: 验证 Chromium 可启动（dry-run）**

```bash
npx playwright test --list 2>&1 | head -10
```

预期：列出 `tests/frontend/e2e/` 下的 spec 文件。

- [ ] **Step 6: 生成 visual baseline (当前 buggy 状态)**

```bash
mkdir -p tests/frontend/screenshots
npx playwright test tests/frontend/e2e/visual.spec.js --update-snapshots
```

预期：生成 5 个页面的基准截图到 `tests/frontend/screenshots/`。退出码 0。

- [ ] **Step 7: 提交 baseline + 基础设施**

```bash
git add package.json package-lock.json tests/frontend/screenshots/
git commit -m "chore(playwright): 安装 Playwright 并生成 visual baseline"
```

---

## Task 2: inference_rules.py (TDD)

**Files:**
- Create: `tests/scripts/test_inference_rules.py`
- Create: `scripts/inference_rules.py`

### Step 1: 写失败的测试

- [ ] **Step 1.1: 创建 `tests/scripts/test_inference_rules.py`**

```python
"""Unit tests for inference_rules.py"""
import unittest

from scripts.inference_rules import (
    COLOR_NAMES,
    ROLE_COLORS,
    FALLBACK_COLOR,
    infer_value,
)


class ColorNamesTest(unittest.TestCase):
    def test_contains_common_colors(self):
        for name in ("amber", "rose", "violet", "teal", "blue"):
            self.assertIn(name, COLOR_NAMES)
            self.assertTrue(COLOR_NAMES[name].startswith("#"))

    def test_all_values_are_3_or_6_digit_hex(self):
        for k, v in COLOR_NAMES.items():
            self.assertRegex(v, r"^#[0-9a-fA-F]{6}$", f"{k}={v} not 6-digit hex")


class RoleColorsTest(unittest.TestCase):
    def test_contains_expected_roles(self):
        for role in ("agent", "moderator", "student", "teacher"):
            self.assertIn(role, ROLE_COLORS)


class FallbackColorTest(unittest.TestCase):
    def test_is_valid_hex(self):
        self.assertRegex(FALLBACK_COLOR, r"^#[0-9a-fA-F]{6}$")


class InferValueTest(unittest.TestCase):
    def test_color_token_in_name(self):
        # "accent-amber" 中含 "amber"
        value, source = infer_value("accent-amber")
        self.assertEqual(value, "#f59e0b")
        self.assertEqual(source, "mapped")

    def test_role_in_avatar_gradient(self):
        value, source = infer_value("avatar-gradient-student")
        self.assertEqual(value, "#3b82f6")
        self.assertEqual(source, "role")

    def test_unknown_returns_fallback(self):
        value, source = infer_value("totally-unknown-thing-xyz")
        self.assertEqual(value, FALLBACK_COLOR)
        self.assertEqual(source, "fallback")

    def test_multiple_tokens_first_match_wins(self):
        # "agent-color" — 没有直接 color token，但 "agent" 也不是颜色
        # 期望: 走 fallback (因为不是 avatar-gradient-)
        value, source = infer_value("agent-color")
        # 取决于优先级: 先 COLOR_NAMES 还是先 ROLE 路径
        # 实际: 拆 token 后没有匹配 color，role 路径不匹配前缀
        # 所以 fallback
        self.assertEqual(source, "fallback")

    def test_case_sensitive_tokens(self):
        # 推断按小写 token 匹配
        value, source = infer_value("accent-AMBER")
        # "AMBER" 不会匹配 (COLOR_NAMES["amber"] 小写)
        self.assertEqual(source, "fallback")


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 1.2: 运行测试，验证失败**

```bash
python -m unittest tests.scripts.test_inference_rules -v
```

预期：FAIL，`ModuleNotFoundError: No module named 'scripts.inference_rules'`

### Step 2: 实现 inference_rules.py

- [ ] **Step 2.1: 创建 `scripts/inference_rules.py`**

```python
"""
inference_rules.py — 语义推断规则 (用于 P0 修复)

将 CSS 变量名映射到合理的色值，供 fix_p0_tech_debt.py 使用。

推断策略 (优先级从高到低):
  1. 拆 var 名为 token，匹配 COLOR_NAMES
  2. avatar-gradient-{role} 模式匹配 ROLE_COLORS
  3. 都不匹配 → FALLBACK_COLOR + 注释 "TODO: refine color"
"""

# 颜色名 → 十六进制
COLOR_NAMES = {
    "amber": "#f59e0b", "rose": "#f43f5e", "violet": "#8b5cf6",
    "purple": "#8b5cf6", "teal": "#14b8a6", "blue": "#3b82f6",
    "green": "#10b981", "orange": "#f97316", "red": "#ef4444",
    "yellow": "#eab308", "pink": "#ec4899", "cyan": "#06b6d4",
    "indigo": "#6366f1", "lime": "#84cc16", "fuchsia": "#d946ef",
    "sky": "#0ea5e9", "glow": "#fef3c7", "hover": "#fde68a",
    "strong": "#1e40af", "index": "#3b82f6",
}

# 角色 → 颜色 (用于 avatar-gradient-* 模式)
ROLE_COLORS = {
    "agent": "#8b5cf6", "moderator": "#f59e0b",
    "student": "#3b82f6", "teacher": "#10b981",
    "user": "#3b82f6", "system": "#6b7280",
}

# fallback
FALLBACK_COLOR = "#9ca3af"


def infer_value(var_name: str) -> tuple[str, str]:
    """根据变量名推断色值

    Returns:
        (value, source_tag), source_tag ∈ {'mapped', 'role', 'fallback'}
    """
    # 策略 1: 拆 token 匹配 COLOR_NAMES
    for token in var_name.split("-"):
        if token in COLOR_NAMES:
            return COLOR_NAMES[token], "mapped"

    # 策略 2: avatar-gradient-{role} 模式
    if var_name.startswith("avatar-gradient-"):
        role = var_name.replace("avatar-gradient-", "", 1)
        if role in ROLE_COLORS:
            return ROLE_COLORS[role], "role"

    # 策略 3: fallback
    return FALLBACK_COLOR, "fallback"
```

- [ ] **Step 2.2: 运行测试，验证通过**

```bash
python -m unittest tests.scripts.test_inference_rules -v
```

预期：4 个测试类全部 PASS (10 tests)。

- [ ] **Step 2.3: 提交**

```bash
git add tests/scripts/test_inference_rules.py scripts/inference_rules.py
git commit -m "feat(fix): 新增 inference_rules.py 语义推断静态规则"
```

---

## Task 3: fix_p0_tech_debt.py (TDD)

**Files:**
- Create: `tests/fixtures/fix/sample-tokens.css`
- Create: `tests/fixtures/fix/sample-overflow.css`
- Create: `tests/scripts/test_fix_p0_tech_debt.py`
- Create: `scripts/fix_p0_tech_debt.py`

### Step 1: 创建 fixtures

- [ ] **Step 1.1: 创建 `tests/fixtures/fix/sample-tokens.css`**

```bash
mkdir -p tests/fixtures/fix
```

写入 `tests/fixtures/fix/sample-tokens.css`：
```css
:root {
    --existing-color: #abc;
}
```

- [ ] **Step 1.2: 创建 `tests/fixtures/fix/sample-overflow.css`**

写入 `tests/fixtures/fix/sample-overflow.css`：
```css
* { box-sizing: border-box; }
.card { padding: 16px; }
*, *::before, *::after { box-sizing: inherit; }
html { font-size: 16px; }
body { margin: 0; }
.safe { color: red; }
```

### Step 2: 写失败的测试

- [ ] **Step 2.1: 创建 `tests/scripts/test_fix_p0_tech_debt.py`**

```python
"""Unit tests for fix_p0_tech_debt.py"""
import json
import unittest
import tempfile
from pathlib import Path

from scripts.fix_p0_tech_debt import (
    extract_p0_vars,
    extract_overflow_files,
    build_tokens_appendix,
    delete_overflow_rules,
)


class ExtractP0VarsTest(unittest.TestCase):
    def test_filters_truly_undefined(self):
        data = {
            "items": [
                {"var": "a", "category": "truly_undefined"},
                {"var": "b", "category": "tailwind_product"},
                {"var": "c", "category": "truly_undefined"},
            ]
        }
        result = extract_p0_vars(data)
        self.assertEqual(result, ["a", "c"])

    def test_empty(self):
        self.assertEqual(extract_p0_vars({}), [])
        self.assertEqual(extract_p0_vars({"items": []}), [])


class ExtractOverflowFilesTest(unittest.TestCase):
    def test_filters_non_allowed(self):
        data = {
            "offending_files": [
                {"file": "app-base.css", "allowed": True, "hits": []},
                {"file": "hub.css", "allowed": False, "hits": [{"line": 1}]},
            ]
        }
        result = extract_overflow_files(data)
        self.assertEqual(result, {"hub.css": [{"line": 1}]})


class BuildTokensAppendixTest(unittest.TestCase):
    def test_generates_sorted_var_definitions(self):
        vars_list = ["zeta-color", "alpha-color"]
        appendix = build_tokens_appendix(vars_list)
        # alpha 应在 zeta 之前 (按字母)
        self.assertLess(
            appendix.index("--alpha-color"),
            appendix.index("--zeta-color"),
        )

    def test_includes_p0_marker_comment(self):
        appendix = build_tokens_appendix(["test-var"])
        self.assertIn("P0 fix", appendix)
        self.assertIn(":root", appendix)
        self.assertIn("--test-var:", appendix)

    def test_fallback_var_gets_todo_comment(self):
        # "unknown-xyz-thing" 是 fallback
        appendix = build_tokens_appendix(["unknown-xyz-thing"])
        self.assertIn("TODO: refine color", appendix)


class DeleteOverflowRulesTest(unittest.TestCase):
    def test_removes_named_lines(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            css_file = Path(tmpdir) / "x.css"
            css_file.write_text(
                "* { box-sizing: border-box; }\n"
                ".card { padding: 16px; }\n"
                "html { font-size: 16px; }\n",
                encoding="utf-8",
            )
            # 删除第 1 行 (* rule) 和第 3 行 (html rule)
            delete_overflow_rules(css_file, {1, 3})
            content = css_file.read_text(encoding="utf-8")
            self.assertNotIn("box-sizing", content)
            self.assertIn(".card", content)
            self.assertNotIn("font-size", content)

    def test_empty_hits_no_change(self):
        with tempfile.TemporaryDirectory() as tmpdir:
            css_file = Path(tmpdir) / "x.css"
            original = ".card { padding: 16px; }\n"
            css_file.write_text(original, encoding="utf-8")
            delete_overflow_rules(css_file, set())
            self.assertEqual(css_file.read_text(encoding="utf-8"), original)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2.2: 运行测试，验证失败**

```bash
python -m unittest tests.scripts.test_fix_p0_tech_debt -v
```

预期：FAIL，`ModuleNotFoundError: No module named 'scripts.fix_p0_tech_debt'`

### Step 3: 实现 fix_p0_tech_debt.py

- [ ] **Step 3.1: 创建 `scripts/fix_p0_tech_debt.py`**

```python
#!/usr/bin/env python3
"""
fix_p0_tech_debt.py — 数据驱动的 P0 修复脚本

读 audit JSON → 应用 212 vars 补全 + 5 文件外溢规则删除

退出码: 0 = 成功; 1 = audit JSON 缺失; 2 = 工具故障
"""
import json
import shutil
import sys
from datetime import date
from pathlib import Path

from scripts.inference_rules import infer_value

ROOT = Path(__file__).parent.parent
OUTPUT_DIR = ROOT / "output"
CSS_DIR = ROOT / "css"
TOKENS_FILE = CSS_DIR / "tokens.css"
TOKENS_BAK = CSS_DIR / "tokens.css.bak-pre-p0-fix"

AUDIT_UNDEFINED = OUTPUT_DIR / "audit_undefined_css_vars.json"
AUDIT_COMPOUND = OUTPUT_DIR / "audit_compound_selectors.json"


def extract_p0_vars(audit_undefined: dict) -> list[str]:
    """从 audit_undefined_css_vars.json 提取 truly_undefined 类别的 var 名"""
    items = audit_undefined.get("items", [])
    return sorted(
        item["var"] for item in items
        if item.get("category") == "truly_undefined"
    )


def extract_overflow_files(audit_compound: dict) -> dict[str, list[dict]]:
    """从 audit_compound_selectors.json 提取 allowed==false 的文件及其 hits"""
    offenders = audit_compound.get("offending_files", [])
    return {
        f["file"]: f["hits"]
        for f in offenders
        if not f.get("allowed", False)
    }


def build_tokens_appendix(vars_list: list[str]) -> str:
    """生成要追加到 tokens.css 的 :root 块"""
    lines = [
        "",
        f"/* P0 fix — auto-generated by fix_p0_tech_debt.py on {date.today().isoformat()} */",
        ":root {",
    ]
    for var in sorted(vars_list):
        value, source = infer_value(var)
        comment = "  /* TODO: refine color */" if source == "fallback" else ""
        lines.append(f"    --{var}: {value};{comment}")
    lines.append("}")
    lines.append("")
    return "\n".join(lines)


def delete_overflow_rules(css_file: Path, line_numbers: set[int]) -> None:
    """从 css_file 中删除指定行号集合的内容"""
    if not line_numbers:
        return
    text = css_file.read_text(encoding="utf-8")
    lines = text.splitlines(keepends=True)
    # 保留不在 line_numbers 中的行
    kept = [
        line for idx, line in enumerate(lines, start=1)
        if idx not in line_numbers
    ]
    css_file.write_text("".join(kept), encoding="utf-8")


def _load_or_exit(path: Path) -> dict | None:
    if not path.exists():
        print(f"❌ 缺少输入: {path}", file=sys.stderr)
        print(f"   请先运行: python scripts/audit_{path.stem.replace('audit_', '')}.py", file=sys.stderr)
        return None
    try:
        return json.loads(path.read_text(encoding="utf-8"))
    except json.JSONDecodeError as e:
        print(f"❌ JSON 解析失败: {path}: {e}", file=sys.stderr)
        return None


def main() -> int:
    try:
        sys.stdout.reconfigure(encoding="utf-8")
    except Exception:
        pass

    # 1) 读 audit JSON
    audit_undef = _load_or_exit(AUDIT_UNDEFINED)
    audit_comp = _load_or_exit(AUDIT_COMPOUND)
    if audit_undef is None or audit_comp is None:
        return 1

    # 2) 提取 P0 列表
    p0_vars = extract_p0_vars(audit_undef)
    overflow = extract_overflow_files(audit_comp)
    print(f"📋 P0 vars to add: {len(p0_vars)}")
    print(f"📋 Overflow files to clean: {len(overflow)}")

    # 3) 备份 tokens.css
    if not TOKENS_BAK.exists():
        shutil.copy2(TOKENS_FILE, TOKENS_BAK)
        print(f"💾 Backup created: {TOKENS_BAK.name}")

    # 4) 追加 212 vars 到 tokens.css
    appendix = build_tokens_appendix(p0_vars)
    with open(TOKENS_FILE, "a", encoding="utf-8") as f:
        f.write(appendix)
    print(f"✏️  Appended {len(p0_vars)} vars to {TOKENS_FILE.name}")

    # 5) 删除 5 个外溢文件的规则
    for fname, hits in overflow.items():
        css_file = CSS_DIR / fname
        if not css_file.exists():
            print(f"⚠️  {fname} not found, skip")
            continue
        line_nums = {h["line"] for h in hits if "line" in h}
        delete_overflow_rules(css_file, line_nums)
        print(f"✏️  Cleaned {len(line_nums)} rules from {fname}")

    # 6) 摘要
    print()
    print("✅ P0 修复应用完成")
    print(f"   vars: +{len(p0_vars)} in tokens.css")
    print(f"   files: {len(overflow)} overflow files cleaned")
    print(f"   backup: {TOKENS_BAK.name} (gitignored)")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 3.2: 运行测试，验证通过**

```bash
python -m unittest tests.scripts.test_fix_p0_tech_debt -v
```

预期：4 个测试类全部 PASS (7 tests)。

- [ ] **Step 3.3: 提交**

```bash
git add tests/fixtures/fix/ tests/scripts/test_fix_p0_tech_debt.py scripts/fix_p0_tech_debt.py
git commit -m "feat(fix): 新增 fix_p0_tech_debt.py 数据驱动 P0 修复"
```

---

## Task 4: 应用 P0 修复 (真实运行)

**Files:**
- Modify: `css/tokens.css` (+~212 lines)
- Modify: 5 CSS files (delete compound rules)
- Create: `css/tokens.css.bak-pre-p0-fix` (local backup)

- [ ] **Step 1: 确认 audit JSON 是最新的**

```bash
ls -la output/audit_undefined_css_vars.json output/audit_compound_selectors.json
```

预期：两个文件都存在。如缺失，跑：
```bash
python scripts/audit_undefined_css_vars.py
python scripts/audit_compound_selectors.py
```

- [ ] **Step 2: 跑修复脚本**

```bash
python scripts/fix_p0_tech_debt.py
```

预期输出：
```
📋 P0 vars to add: 212
📋 Overflow files to clean: 5
💾 Backup created: tokens.css.bak-pre-p0-fix
✏️  Appended 212 vars to tokens.css
✏️  Cleaned N rules from classroom-premium-dna.css
✏️  Cleaned N rules from hub.css
...
✅ P0 修复应用完成
```

退出码 0。

- [ ] **Step 3: 验证 tokens.css 增量为 ~212**

```bash
grep -c "^\s*--" css/tokens.css
```

预期：行数比修复前多 212 左右（具体看 fallback 注释行）。

- [ ] **Step 4: 验证备份文件存在**

```bash
ls css/tokens.css.bak-pre-p0-fix
```

预期：文件存在。

- [ ] **Step 5: 不提交 (留给 Task 8 单 commit)**

**重要**: 此时**不**做 git commit。变更暂留在工作区。

- [ ] **Step 6: 跑 L1 验收（audit 验证 P0=0）**

```bash
python scripts/audit_undefined_css_vars.py
python scripts/audit_compound_selectors.py
```

预期：
- `audit_undefined_css_vars.py` 退出码 0，输出 `truly_undefined: 0` (P0 应为 0)
- `audit_compound_selectors.py` 退出码 0 (无外溢文件)

**若 L1 失败**: 阻断，输出剩余 P0 列表，检查 fix 脚本 bug。

- [ ] **Step 7: 跑 L2 验收 (HTTP 200)**

```bash
python scripts/visual_verify_static.py
```

预期：5 高风险页面 (login / register / hub / personal / teacher-dashboard) HTTP 200，CSS 加载顺序正确。

**若 L2 失败**: 阻断，输出失败 URL。可能 fix 破坏了某 CSS 引用。

- [ ] **Step 8: 报告 L1 + L2 结果**

不 commit，等 L3 (Task 7)。

---

## Task 5: 准备 L3 视觉验收

**Files:** 无修改

- [ ] **Step 1: 确认 Chromium 浏览器仍可用**

```bash
ls "$USERPROFILE/.cache/ms-playwright/" 2>&1 | head -5
```

预期：包含 `chromium-xxxx` 目录。若不存在，重跑 `npx playwright install chromium`。

- [ ] **Step 2: 跑 visual regression**

```bash
npx playwright test tests/frontend/e2e/visual.spec.js
```

预期：测试输出 5 个 PASS 或 5 个 FAIL（与 baseline 对比）。

- [ ] **Step 3: 分析结果**

**若 PASS**: L3 视觉未变，跳到 Task 8 commit。
**若 FAIL**: 预期会 FAIL，因为 P0 修复改变了颜色。查看 `test-results/` 下的 diff 截图。

- [ ] **Step 4: 视觉 diff 审查**

对每个 diff 截图，目视判断：
- **预期改进** (如：原本灰色 placeholder 现在显示语义颜色) → 接受更新 baseline
- **意外损坏** (如：原本正常颜色现在变成奇怪颜色) → 阻断，修复 fix 脚本的推断

- [ ] **Step 5: 报告 L3 结论**

- L3 PASS: 跳到 Task 8
- L3 FAIL 但 diff 都是预期改进: 跳到 Task 6 (更新 baseline) 然后 Task 8
- L3 FAIL 且有意外损坏: 阻断，回到 Task 4 调整 fix 脚本

---

## Task 6: 更新 visual baseline (L3 改进后)

**Files:**
- Modify: `tests/frontend/screenshots/*.png` (update snapshots)

- [ ] **Step 1: 更新 baseline 截图**

```bash
npx playwright test tests/frontend/e2e/visual.spec.js --update-snapshots
```

预期：5 个页面新截图替换旧 baseline。退出码 0。

- [ ] **Step 2: 验证 diff 已消除**

```bash
npx playwright test tests/frontend/e2e/visual.spec.js
```

预期：5 个 PASS（与新 baseline 对比无差异）。

- [ ] **Step 3: 不单独 commit (留给 Task 8)**

---

## Task 7: 最终验收 + 准备单 commit

**Files:** 无修改

- [ ] **Step 1: 跑所有单元测试**

```bash
python -m unittest discover tests/scripts -v 2>&1 | tail -15
```

预期：所有测试 PASS。

- [ ] **Step 2: 跑所有 audit 脚本**

```bash
python scripts/audit_undefined_css_vars.py
python scripts/audit_compound_selectors.py
python scripts/audit_frontend_smells.py
```

预期：
- `audit_undefined_css_vars.py` 退出 0，truly_undefined=0
- `audit_compound_selectors.py` 退出 0，无外溢
- `audit_frontend_smells.py` 退出 0 或 1（smells 是 P2 范畴，不要求=0）

- [ ] **Step 3: 重跑 build_tech_debt_inventory 确认 P0=0**

```bash
python scripts/build_tech_debt_inventory.py
```

预期：报告生成，P0 计数 = 0。

- [ ] **Step 4: 检查 git 状态**

```bash
git status
```

预期看到以下 staged 之外的修改:
- `css/tokens.css` (modified)
- 5 个 CSS 文件 (modified)
- `tests/frontend/screenshots/*.png` (modified — baseline 更新)
- 可能的 `package.json` / `package-lock.json` (如 Task 1 安装了 deps)

**不应看到**:
- `css/tokens.css.bak-pre-p0-fix` (应已 gitignore 或不被 add)

- [ ] **Step 5: 检查 backup 不在 staging**

```bash
git status --ignored | grep tokens.css.bak
```

预期：backup 文件在 ignored 列表中。如未忽略，加入 `.gitignore`：
```bash
echo "css/tokens.css.bak-pre-p0-fix" >> .gitignore
git add .gitignore
```

---

## Task 8: 单 commit + 清理

**Files:**
- Modify: 1 commit includes tokens.css + 5 CSS + screenshots + .gitignore (如需)

- [ ] **Step 1: Stage 所有相关变更**

```bash
git add css/tokens.css
git add css/classroom-premium-dna.css css/hub.css css/index.css css/personal.css css/plant.css
git add tests/frontend/screenshots/
git add .gitignore
```

- [ ] **Step 2: 验证 staging 内容**

```bash
git diff --cached --stat
```

预期：~7 个文件，~220 行变更。

- [ ] **Step 3: 单 commit (中文 message)**

```bash
git commit -m "fix(css): 修复 217 个 P0 前端技术债 (212 vars + 5 selectors)"
```

- [ ] **Step 4: 删除本地 backup**

```bash
rm css/tokens.css.bak-pre-p0-fix
```

- [ ] **Step 5: 最终 git 状态检查**

```bash
git status
```

预期：除 `local_storage.json` 和 `requirements.txt` (已知 untracked) 外无其他变更。

- [ ] **Step 6: 验证 commit 内容**

```bash
git log -1 --stat
```

预期：单 commit 含所有修复。

- [ ] **Step 7: 更新原 inventory 报告的 P0 状态 (可选)**

在 `docs/superpowers/notes/frontend-tech-debt-2026-06-08.md` 顶部添加一行：
```markdown
> **P0 状态 (2026-06-08 修复)**: 全部 217 项已修复 — 见 commit xxx
```

**可选**: 不强制，可由后续 plan 处理。

---

## 验收标准 (Acceptance Criteria)

- [ ] AC1: 3 个 audit 重跑 → P0 总数 = 0
- [ ] AC2: `css/tokens.css` 含 212 个新 var 定义
- [ ] AC3: 5 个外溢文件不再含 `*`/`html`/`body` 顶层规则
- [ ] AC4: `visual_verify_static.py` 通过
- [ ] AC5: `visual.spec.js` 通过 (baseline 已更新)
- [ ] AC6: 单 commit, 中文 message
- [ ] AC7: 备份文件已删除 (commit 后无残留)

---

## 后续 (Out of This Plan)

- 调优 fallback 颜色 (212 vars 中可能 ~50 个是 fallback)
- 主题切换适配 (深色/浅色主题的 212 vars 自动重定义)
- P1 修复 (12 个超大 JS 文件)
- P2 修复 (18,386 个魔法数字)

---

## Self-Review

执行人: writing-plans skill
日期: 2026-06-08

| 检查 | 结论 |
|------|------|
| Spec coverage | 6 阶段 spec 全部覆盖 (Task 1-8) |
| Placeholder scan | 无 TBD/TODO/「类似 Task N」/「添加适当」类占位 (代码块全展开) |
| Type consistency | `extract_p0_vars` / `extract_overflow_files` / `build_tokens_appendix` / `delete_overflow_rules` / `infer_value` 跨任务签名一致 |
| 命令路径 | `python -m unittest tests.scripts.xxx` 路径与实际文件路径一致 |
| Windows 兼容 | 每个 Python 脚本入口加 `sys.stdout.reconfigure` |
| Playwright fallback | 用户指定无降级，已纳入 3 重试 + 镜像源切换 |
| 退出码 | 0/1/2 约定贯穿所有脚本 |
