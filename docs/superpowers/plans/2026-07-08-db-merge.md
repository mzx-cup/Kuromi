# 数据库渐进合并实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 db.py（同步、35+ 表）合并到 SQLAlchemy ORM（async、13 表），过程中通过测试优先 + 双写双读 + 百分比切读保证零退化。

**Architecture:** Repository 抽象层 + DualWrite 装饰器 + Feature flag 用户哈希分流。每个垂直切片独立完成"测试 → 模型 → 路由 → 灰度 → 收尾"5 阶段。

**Tech Stack:** Python 3.11+, FastAPI, SQLAlchemy 2.0 async, aiosqlite, pymysql, pytest 9, Alembic

---

## 文件总览

### 新建文件

| 路径 | 职责 |
|------|------|
| `app/core/feature_flags.py` | 用户哈希分流 + 环境变量读取 |
| `app/core/repository_factory.py` | 根据 feature flag 返回 Repository 实例 |
| `app/repositories/base.py` | Repository Protocol 定义 |
| `app/repositories/dual_write.py` | DualWriteRepository 装饰器 |
| `app/repositories/orm/*.py` | 每个领域的 SQLAlchemy Repository |
| `app/repositories/legacy/*.py` | 每个领域的 db.py 封装 |
| `app/models/{preferences,learning,knowledge,focus,gamification,chat}.py` | 新增 SQLAlchemy 模型 |
| `tests/contracts/*.py` | 契约测试 |
| `tests/snapshots/*.py` | 快照测试 |
| `tests/perf/test_relative_regression.py` | CI 跑的性能回归测试 |
| `tests/perf/benchmark_local.py` | 本地基准脚本 |
| `tests/repositories/*.py` | Repository 单元测试 |
| `tests/fixtures/seed_data.py` | 种子数据生成器 |
| `scripts/reconcile_databases.py` | 对账脚本 |
| `scripts/migrate_messages_metadata.py` | 切片 #9 数据迁移脚本 |
| `scripts/migrate_user_table.py` | 收尾阶段 user 表迁移脚本 |
| `perf-results/.gitignore` | 性能结果目录 |
| `SLICE_STATUS.md` | 切片进度追踪 |
| `pytest.ini` | pytest 配置 |
| `requirements-dev.txt` | 开发/测试依赖 |

### 修改文件

| 路径 | 修改内容 |
|------|---------|
| `tests/conftest.py` | 增加 dual_db_environment, contract_runner, seed_users 等 fixture |
| `app/core/config.py` | 增加 READ_BACKEND_PERCENTAGE, DUAL_WRITE_LEGACY, ORM_ENABLED 环境变量 |
| `app/models/__init__.py` | 注册新模型 |
| `main.py` | 各切片路由替换 import 与 endpoint 内部 |
| `Navicat/setup_database.py` | 收尾阶段删除 |
| `db.py` | 收尾阶段删除 |
| `alembic.ini` | 修正数据库 URL 引用环境变量 |
| `alembic/env.py` | 增加新模型 target_metadata |
| `requirements.txt` | 移除 pymysql（如果项目不再用） |

---

## 切片依赖图

```
M0 (基础设施)
  └─ M1 (切片 #1 用户认证, 4d)
       └─ M2 (切片 #2 用户偏好, 2d)
            ├─ M3 (切片 #3 统计读, 3d)
            │    └─ M4 (切片 #4 统计写, 3d)
            ├─ M5 (切片 #5 课程, 3d)
            ├─ M6 (切片 #6 知识, 2d)
            ├─ M7 (切片 #7 焦点, 2d)
            └─ M8 (切片 #8 游戏化, 2d)
                 └─ M9 (切片 #9 聊天, 5d)
                      └─ M10 (切片 #10 教室, 4d)
                           └─ M11 (收尾 user 表统一, 5d)
```

总日历时间：约 10 周（含灰度期）。

---

# Milestone 0: 测试与基础设施搭建

**目标：** 在任何业务代码改动之前，先把测试基础设施与新模块骨架搭好。让后续 10 个切片可以复用同一个 fixture 和 feature flag 框架。

---

### Task 0.1: 创建 pytest 配置与开发依赖

**Files:**
- Create: `pytest.ini`
- Create: `requirements-dev.txt`
- Modify: `tests/conftest.py`

- [ ] **Step 1: 创建 `pytest.ini`**

```ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: slow tests (LLM/network)
    ai: AI/LLM tests (consume API quota)
    cv: CV image/video tests
    e2e: end-to-end tests (require running service)
    unit: unit tests (no external deps)
    contract: contract tests (dual-backend comparison)
    dual_write: dual-write consistency tests
    perf: performance tests
filterwarnings =
    ignore::DeprecationWarning
addopts = -ra --strict-markers
```

- [ ] **Step 2: 创建 `requirements-dev.txt`**

```
pytest>=8.0
pytest-cov>=4.1
pytest-asyncio>=0.23
freezegun>=1.4
memory-profiler>=0.61
```

- [ ] **Step 3: 安装开发依赖**

Run: `pip install -r requirements-dev.txt`
Expected: 所有包安装成功。

- [ ] **Step 4: 运行现有测试，确认环境正常**

Run: `pytest tests/test_dashboard_data.py -v`
Expected: 测试通过。

- [ ] **Step 5: 提交**

```bash
git add pytest.ini requirements-dev.txt
git commit -m "chore: add pytest config and dev dependencies"
```

---

### Task 0.2: 创建 Feature Flag 模块

**Files:**
- Create: `app/core/feature_flags.py`
- Test: `tests/test_feature_flags.py`

- [ ] **Step 1: 写失败测试**

`tests/test_feature_flags.py`:

```python
import os
import pytest
from app.core.feature_flags import (
    user_in_orm_read_path,
    get_read_percentage,
    is_dual_write_enabled,
)


class TestUserInOrmReadPath:
    def test_zero_percentage_always_legacy(self):
        for user_id in ["user_1", "user_2", "user_3"]:
            assert user_in_orm_read_path(user_id, 0) is False

    def test_hundred_percentage_always_orm(self):
        for user_id in ["user_1", "user_2", "user_3"]:
            assert user_in_orm_read_path(user_id, 100) is True

    def test_fifty_percentage_is_stable_for_same_user(self):
        user_id = "stable_user_42"
        result_1 = user_in_orm_read_path(user_id, 50)
        result_2 = user_in_orm_read_path(user_id, 50)
        result_3 = user_in_orm_read_path(user_id, 50)
        assert result_1 == result_2 == result_3

    def test_distribution_roughly_uniform(self):
        results = [user_in_orm_read_path(f"user_{i}", 50) for i in range(1000)]
        orm_count = sum(results)
        assert 400 <= orm_count <= 600


class TestGetReadPercentage:
    def test_default_zero(self, monkeypatch):
        monkeypatch.delenv("READ_BACKEND_PERCENTAGE", raising=False)
        assert get_read_percentage() == 0

    def test_from_env(self, monkeypatch):
        monkeypatch.setenv("READ_BACKEND_PERCENTAGE", "25")
        assert get_read_percentage() == 25

    def test_invalid_value_defaults_zero(self, monkeypatch):
        monkeypatch.setenv("READ_BACKEND_PERCENTAGE", "abc")
        assert get_read_percentage() == 0


class TestIsDualWriteEnabled:
    def test_default_false(self, monkeypatch):
        monkeypatch.delenv("DUAL_WRITE_LEGACY", raising=False)
        assert is_dual_write_enabled() is False

    def test_true_string(self, monkeypatch):
        monkeypatch.setenv("DUAL_WRITE_LEGACY", "true")
        assert is_dual_write_enabled() is True
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `pytest tests/test_feature_flags.py -v`
Expected: FAIL with "ModuleNotFoundError: No module named 'app.core.feature_flags'"

- [ ] **Step 3: 实现 feature_flags.py**

`app/core/feature_flags.py`:

```python
"""Feature flags for database merge cutover.

Provides percentage-based read path cutover and dual-write toggle.
"""
from __future__ import annotations

import hashlib
import os


def user_in_orm_read_path(user_id: str, percentage: int) -> bool:
    """Determine if a user should read from SQLAlchemy ORM.

    Stable per user_id: same user always gets same answer at same percentage.
    """
    if percentage <= 0:
        return False
    if percentage >= 100:
        return True
    h = hashlib.md5(f"orm-read:{user_id}".encode()).hexdigest()
    bucket = int(h[:8], 16) % 100
    return bucket < percentage


def get_read_percentage() -> int:
    """Read READ_BACKEND_PERCENTAGE from env. Invalid -> 0."""
    raw = os.getenv("READ_BACKEND_PERCENTAGE", "0")
    try:
        value = int(raw)
    except ValueError:
        return 0
    if value < 0:
        return 0
    if value > 100:
        return 100
    return value


def is_dual_write_enabled() -> bool:
    """Read DUAL_WRITE_LEGACY from env. Default false."""
    return os.getenv("DUAL_WRITE_LEGACY", "false").lower() == "true"


def is_orm_enabled() -> bool:
    """Master switch. If false, all routes use legacy regardless of percentage."""
    return os.getenv("ORM_ENABLED", "true").lower() != "false"
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `pytest tests/test_feature_flags.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add app/core/feature_flags.py tests/test_feature_flags.py
git commit -m "feat(core): add feature flags for read-path cutover and dual-write"
```

---

### Task 0.3: 创建 Repository Protocol 与工厂

**Files:**
- Create: `app/repositories/base.py`
- Create: `app/core/repository_factory.py`
- Test: `tests/test_repository_factory.py`

- [ ] **Step 1: 写失败测试**

`tests/test_repository_factory.py`:

```python
import os
import pytest
from app.core.repository_factory import (
    get_repository_for_user,
    is_orm_read_path_active,
)
from app.repositories.base import LearningRepository


class TestGetRepositoryForUser:
    def test_returns_object_implementing_protocol(self):
        repo = get_repository_for_user("any_user", repository_type="learning")
        assert isinstance(repo, LearningRepository)

    def test_zero_percentage_returns_legacy(self, monkeypatch):
        monkeypatch.setenv("READ_BACKEND_PERCENTAGE", "0")
        repo = get_repository_for_user("user_a", repository_type="learning")
        from app.repositories.legacy.learning import DbPyLearningRepository
        assert isinstance(repo, DbPyLearningRepository)

    def test_hundred_percentage_returns_orm(self, monkeypatch):
        monkeypatch.setenv("READ_BACKEND_PERCENTAGE", "100")
        repo = get_repository_for_user("user_a", repository_type="learning")
        from app.repositories.orm.learning import SqlAlchemyLearningRepository
        assert isinstance(repo, SqlAlchemyLearningRepository)


class TestIsOrmReadPathActive:
    def test_zero_percentage_inactive(self, monkeypatch):
        monkeypatch.setenv("READ_BACKEND_PERCENTAGE", "0")
        assert is_orm_read_path_active("user_a") is False

    def test_hundred_percentage_active(self, monkeypatch):
        monkeypatch.setenv("READ_BACKEND_PERCENTAGE", "100")
        assert is_orm_read_path_active("user_a") is True
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `pytest tests/test_repository_factory.py -v`
Expected: FAIL with import errors.

- [ ] **Step 3: 实现 Repository Protocol**

`app/repositories/base.py`:

```python
"""Repository Protocol definitions for database merge."""
from __future__ import annotations

from typing import Protocol, runtime_checkable


@runtime_checkable
class LearningRepository(Protocol):
    async def get_overview(self, user_id: str) -> dict: ...
    async def get_trend(self, user_id: str, days: int) -> list: ...
    async def get_heatmap(self, user_id: str) -> dict: ...
    async def record_session(self, user_id: str, session_data: dict) -> None: ...


@runtime_checkable
class PreferencesRepository(Protocol):
    async def get_preferences(self, user_id: str) -> dict: ...
    async def set_preference(self, user_id: str, key: str, value: dict) -> None: ...


@runtime_checkable
class ChatRepository(Protocol):
    async def save_message(self, user_id: str, message: dict) -> int: ...
    async def get_history(self, user_id: str, limit: int) -> list: ...
```

- [ ] **Step 4: 实现 Repository Factory**

`app/core/repository_factory.py`:

```python
"""Factory for instantiating repositories based on feature flags."""
from __future__ import annotations

from app.core.feature_flags import (
    get_read_percentage,
    is_dual_write_enabled,
    is_orm_enabled,
    user_in_orm_read_path,
)
from app.repositories.base import (
    LearningRepository,
    PreferencesRepository,
    ChatRepository,
)
from app.repositories.dual_write import DualWriteRepository


def is_orm_read_path_active(user_id: str) -> bool:
    if not is_orm_enabled():
        return False
    return user_in_orm_read_path(user_id, get_read_percentage())


def _build_orm(repository_type: str):
    if repository_type == "learning":
        from app.repositories.orm.learning import SqlAlchemyLearningRepository
        return SqlAlchemyLearningRepository
    if repository_type == "preferences":
        from app.repositories.orm.preferences import SqlAlchemyPreferencesRepository
        return SqlAlchemyPreferencesRepository
    raise ValueError(f"Unknown repository_type: {repository_type}")


def _build_legacy(repository_type: str):
    if repository_type == "learning":
        from app.repositories.legacy.learning import DbPyLearningRepository
        return DbPyLearningRepository
    if repository_type == "preferences":
        from app.repositories.legacy.preferences import DbPyPreferencesRepository
        return DbPyPreferencesRepository
    raise ValueError(f"Unknown repository_type: {repository_type}")


def get_repository_for_user(user_id: str, repository_type: str):
    """Get the read-path repository for this user.

    If ORM read path is active for this user, return ORM repository.
    Otherwise return legacy repository.
    """
    if is_orm_read_path_active(user_id):
        orm_cls = _build_orm(repository_type)
        return orm_cls()
    legacy_cls = _build_legacy(repository_type)
    return legacy_cls()


def get_write_repository(user_id: str, repository_type: str, db_session=None):
    """Get the write-path repository.

    If dual-write is enabled, wraps primary and shadow in DualWriteRepository.
    Otherwise returns just the primary.
    """
    primary_cls = _build_orm(repository_type)
    primary = primary_cls(db_session) if db_session else primary_cls()

    if is_dual_write_enabled():
        shadow_cls = _build_legacy(repository_type)
        shadow = shadow_cls()
        return DualWriteRepository(primary=primary, shadow=shadow)

    return primary
```

- [ ] **Step 5: 实现 DualWrite 装饰器骨架**

`app/repositories/dual_write.py`:

```python
"""DualWriteRepository - writes to both primary and shadow backends."""
from __future__ import annotations

import logging
from typing import Any

logger = logging.getLogger("starlearn.repository.dual_write")


class DualWriteRepository:
    """Decorator: every write operation hits both primary and shadow.

    Primary failures raise immediately (production truth).
    Shadow failures are logged and queued for retry, never raised.
    """

    def __init__(self, primary: Any, shadow: Any):
        self.primary = primary
        self.shadow = shadow

    async def _dual_write(self, method_name: str, *args, **kwargs) -> Any:
        primary_method = getattr(self.primary, method_name)
        shadow_method = getattr(self.shadow, method_name)

        result = await primary_method(*args, **kwargs)

        try:
            await shadow_method(*args, **kwargs)
        except Exception as e:
            logger.warning(
                "[dual-write] shadow %s failed: %s", method_name, e,
                extra={"method": method_name, "args": str(args)[:200]},
            )
            # Shadow failures are logged but not retried in M0.
            # Async retry queue (Redis/in-memory) is deferred to a future milestone.
            # For M1-M11, manual reconciliation via reconcile_databases.py is acceptable.

        return result

    def __getattr__(self, name: str):
        if name.startswith("_") or name in ("primary", "shadow"):
            raise AttributeError(name)
        async def proxy(*args, **kwargs):
            return await self._dual_write(name, *args, **kwargs)
        return proxy
```

- [ ] **Step 6: 创建 Legacy/ORM Repository 占位实现**

`app/repositories/legacy/learning.py`:

```python
"""Legacy db.py wrapper for learning data. Stub during M0."""
from app.repositories.base import LearningRepository


class DbPyLearningRepository:
    async def get_overview(self, user_id: str) -> dict:
        raise NotImplementedError("Filled in M3")

    async def get_trend(self, user_id: str, days: int) -> list:
        raise NotImplementedError("Filled in M3")

    async def get_heatmap(self, user_id: str) -> dict:
        raise NotImplementedError("Filled in M3")

    async def record_session(self, user_id: str, session_data: dict) -> None:
        raise NotImplementedError("Filled in M4")
```

`app/repositories/legacy/__init__.py`: (空文件)

`app/repositories/orm/learning.py`:

```python
"""SQLAlchemy implementation for learning data. Stub during M0."""
from app.repositories.base import LearningRepository


class SqlAlchemyLearningRepository:
    async def get_overview(self, user_id: str) -> dict:
        raise NotImplementedError("Filled in M3")

    async def get_trend(self, user_id: str, days: int) -> list:
        raise NotImplementedError("Filled in M3")

    async def get_heatmap(self, user_id: str) -> dict:
        raise NotImplementedError("Filled in M3")

    async def record_session(self, user_id: str, session_data: dict) -> None:
        raise NotImplementedError("Filled in M4")
```

`app/repositories/orm/__init__.py`: (空文件)

`app/repositories/__init__.py`: (空文件)

- [ ] **Step 7: 实现其他 repository_type 占位（preferences）**

`app/repositories/legacy/preferences.py`:

```python
"""Legacy db.py wrapper for preferences. Stub during M0."""


class DbPyPreferencesRepository:
    async def get_preferences(self, user_id: str) -> dict:
        raise NotImplementedError("Filled in M2")

    async def set_preference(self, user_id: str, key: str, value: dict) -> None:
        raise NotImplementedError("Filled in M2")
```

`app/repositories/orm/preferences.py`:

```python
"""SQLAlchemy implementation for preferences. Stub during M0."""


class SqlAlchemyPreferencesRepository:
    async def get_preferences(self, user_id: str) -> dict:
        raise NotImplementedError("Filled in M2")

    async def set_preference(self, user_id: str, key: str, value: dict) -> None:
        raise NotImplementedError("Filled in M2")
```

- [ ] **Step 8: 运行测试，确认通过**

Run: `pytest tests/test_repository_factory.py -v`
Expected: PASS（占位实现已满足 isinstance 检查）

- [ ] **Step 9: 提交**

```bash
git add app/repositories/ app/core/repository_factory.py tests/test_repository_factory.py
git commit -m "feat(repositories): add protocol, factory, and dual-write skeleton"
```

---

### Task 0.4: 扩展 conftest.py 增加 Dual-DB Fixture

**Files:**
- Modify: `tests/conftest.py`
- Create: `tests/fixtures/seed_data.py`
- Test: `tests/test_dual_db_fixture.py`

- [ ] **Step 1: 写失败测试**

`tests/test_dual_db_fixture.py`:

```python
import pytest
from pathlib import Path


class TestDualDbFixture:
    def test_dual_db_environment_provides_both_paths(self, dual_db_environment):
        assert Path(dual_db_environment.legacy_path).exists()
        assert Path(dual_db_environment.orm_path).exists()

    def test_contract_runner_provides_assert_contract(self, dual_db_environment, contract_runner):
        assert hasattr(contract_runner, "assert_contract")

    def test_seed_users_creates_consistent_users(self, dual_db_environment):
        from tests.fixtures.seed_data import count_users_legacy, count_users_orm
        legacy_count = count_users_legacy(dual_db_environment.legacy_path)
        orm_count = count_users_orm(dual_db_environment.orm_path)
        assert legacy_count == orm_count > 0
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `pytest tests/test_dual_db_fixture.py -v`
Expected: FAIL with "fixture 'dual_db_environment' not found"

- [ ] **Step 3: 创建种子数据模块**

`tests/fixtures/__init__.py`: (空文件)

`tests/fixtures/seed_data.py`:

```python
"""Seed data generators for dual-backend testing.

Provides consistent data across legacy db.py schema and ORM schema.
"""
from __future__ import annotations

import sqlite3
from datetime import datetime, timedelta, timezone
from pathlib import Path
import json


# Canonical seed dataset
SEED_USERS = [
    {"id": 1, "username": "alice", "password": "hashed_pw_1", "preferred_language": "zh-CN"},
    {"id": 2, "username": "bob", "password": "hashed_pw_2", "preferred_language": "en-US"},
    {"id": 3, "username": "charlie", "password": "hashed_pw_3", "preferred_language": "zh-CN"},
]


def _now_iso() -> str:
    return datetime.now(timezone.utc).isoformat()


def _days_ago(n: int) -> str:
    return (datetime.now(timezone.utc) - timedelta(days=n)).isoformat()


def init_legacy_schema(db_path: str) -> None:
    """Create db.py-style tables for testing."""
    conn = sqlite3.connect(db_path)
    cur = conn.cursor()

    # db.py 'user' table (INT PK)
    cur.execute("""
        CREATE TABLE IF NOT EXISTS user (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            username VARCHAR(128) UNIQUE NOT NULL,
            password VARCHAR(255) NOT NULL,
            preferred_language VARCHAR(16) DEFAULT 'zh-CN'
        )
    """)

    # db.py 'user_preferences' table
    cur.execute("""
        CREATE TABLE IF NOT EXISTS user_preferences (
            user_id INTEGER NOT NULL,
            key VARCHAR(128) NOT NULL,
            value TEXT,
            updated_at TEXT,
            PRIMARY KEY (user_id, key)
        )
    """)

    conn.commit()
    conn.close()


def init_orm_schema(db_path: str) -> None:
    """Create SQLAlchemy ORM tables for testing."""
    # Import here to avoid pulling in FastAPI at conftest import time
    from app.models.base import Base
    from app.models.user import User, StudentProfile
    from app.models.preferences import UserPreference  # filled in M2

    from sqlalchemy import create_engine
    engine = create_engine(f"sqlite:///{db_path}")
    Base.metadata.create_all(engine)
    engine.dispose()


def populate_legacy(db_path: str, users: list = None) -> None:
    """Populate legacy db.py tables with seed data."""
    users = users or SEED_USERS
    conn = sqlite3.connect(db_path)
    cur = conn.cursor()
    for u in users:
        cur.execute(
            "INSERT OR IGNORE INTO user (id, username, password, preferred_language) VALUES (?, ?, ?, ?)",
            (u["id"], u["username"], u["password"], u["preferred_language"]),
        )
    conn.commit()
    conn.close()


def populate_orm(db_path: str, users: list = None) -> None:
    """Populate ORM tables with seed data.

    Note: ORM users use UUID strings as PK, mapping from int seed IDs.
    """
    users = users or SEED_USERS
    from sqlalchemy import create_engine
    from app.models.base import Base
    from app.models.user import User

    engine = create_engine(f"sqlite:///{db_path}")
    Base.metadata.create_all(engine)

    from sqlalchemy.orm import sessionmaker
    Session = sessionmaker(bind=engine)
    session = Session()
    try:
        for u in users:
            existing = session.query(User).filter_by(username=u["username"]).first()
            if existing:
                continue
            user = User(
                id=f"seed-{u['username']}",
                username=u["username"],
                password_hash=u["password"],
            )
            session.add(user)
        session.commit()
    finally:
        session.close()
    engine.dispose()


def count_users_legacy(db_path: str) -> int:
    conn = sqlite3.connect(db_path)
    cur = conn.cursor()
    cur.execute("SELECT COUNT(*) FROM user")
    n = cur.fetchone()[0]
    conn.close()
    return n


def count_users_orm(db_path: str) -> int:
    from sqlalchemy import create_engine
    from sqlalchemy.orm import sessionmaker
    from app.models.user import User

    engine = create_engine(f"sqlite:///{db_path}")
    Session = sessionmaker(bind=engine)
    session = Session()
    try:
        return session.query(User).count()
    finally:
        session.close()
```

- [ ] **Step 4: 扩展 conftest.py**

`tests/conftest.py`:

```python
# -*- coding: utf-8 -*-
"""pytest 全局配置 + dual-backend fixtures"""
from __future__ import annotations

import pytest
from pathlib import Path

from tests.fixtures.seed_data import (
    init_legacy_schema,
    init_orm_schema,
    populate_legacy,
    populate_orm,
    SEED_USERS,
)


def pytest_configure(config):
    """注册自定义 pytest markers"""
    config.addinivalue_line("markers", "slow: 慢速测试")
    config.addinivalue_line("markers", "ai: AI/LLM 相关测试")
    config.addinivalue_line("markers", "cv: CV 图像/视频相关测试")
    config.addinivalue_line("markers", "e2e: 端到端测试")
    config.addinivalue_line("markers", "unit: 单元测试")
    config.addinivalue_line("markers", "contract: 契约测试（双套后端对比）")
    config.addinivalue_line("markers", "dual_write: 双写一致性测试")
    config.addinivalue_line("markers", "perf: 性能测试")


class DualDbEnv:
    def __init__(self, legacy_path: str, orm_path: str):
        self.legacy_path = legacy_path
        self.orm_path = orm_path


class ContractRunner:
    def __init__(self, legacy_client, orm_client):
        self.legacy_client = legacy_client
        self.orm_client = orm_client

    def assert_contract(self, method: str, path: str, **kwargs):
        """Same endpoint, two backends. Responses must match (excluding noise fields)."""
        from tests.fixtures.normalize import normalize

        legacy_resp = self.legacy_client.request(method, path, **kwargs)
        orm_resp = self.orm_client.request(method, path, **kwargs)

        assert legacy_resp.status_code == orm_resp.status_code, (
            f"Status mismatch for {method} {path}: "
            f"legacy={legacy_resp.status_code}, orm={orm_resp.status_code}"
        )

        if legacy_resp.status_code == 200:
            assert normalize(legacy_resp.json()) == normalize(orm_resp.json()), (
                f"Body mismatch for {method} {path}\n"
                f"Legacy: {legacy_resp.json()}\nORM: {orm_resp.json()}"
            )


@pytest.fixture
def dual_db_environment(tmp_path):
    """Create both legacy and ORM databases with identical seed data."""
    legacy_path = str(tmp_path / "legacy.db")
    orm_path = str(tmp_path / "orm.db")

    init_legacy_schema(legacy_path)
    init_orm_schema(orm_path)

    populate_legacy(legacy_path, SEED_USERS)
    populate_orm(orm_path, SEED_USERS)

    return DualDbEnv(legacy_path=legacy_path, orm_path=orm_path)


@pytest.fixture
def contract_runner(dual_db_environment):
    """Provide contract comparison helper."""
    from fastapi.testclient import TestClient
    from main import app

    # For M0, both clients point at the same app. M1+ will route differently.
    legacy_client = TestClient(app)
    orm_client = TestClient(app)

    return ContractRunner(legacy_client=legacy_client, orm_client=orm_client)
```

- [ ] **Step 5: 创建 normalize 工具**

`tests/fixtures/normalize.py`:

```python
"""Normalize API responses for contract comparison.

Strips noise fields that legitimately differ between backends.
"""
from __future__ import annotations

from typing import Any

NOISE_FIELDS = {
    "updated_at", "created_at", "last_synced_at",
    "request_id", "trace_id", "server_timestamp",
}


def normalize(payload: Any) -> Any:
    """Recursively strip noise fields."""
    if isinstance(payload, dict):
        return {
            k: normalize(v)
            for k, v in payload.items()
            if k not in NOISE_FIELDS
        }
    if isinstance(payload, list):
        return [normalize(x) for x in payload]
    return payload
```

- [ ] **Step 6: 运行测试，确认通过**

Run: `pytest tests/test_dual_db_fixture.py -v`
Expected: PASS

- [ ] **Step 7: 提交**

```bash
git add tests/conftest.py tests/fixtures/
git commit -m "test: add dual-db fixture and contract runner"
```

---

### Task 0.5: 初始化 SLICE_STATUS.md

**Files:**
- Create: `SLICE_STATUS.md`

- [ ] **Step 1: 创建 SLICE_STATUS.md**

`SLICE_STATUS.md`:

```markdown
# 数据库合并切片状态

最后更新：2026-07-08

## 已完成切片

（无）

## 进行中切片

（无）

## 待启动切片

| # | 切片 | 工作日 | 依赖 | 状态 |
|---|------|-------|------|------|
| 1 | 用户认证 | 4 | M0 | 待启动 |
| 2 | 用户偏好 | 2 | M1 | 待启动 |
| 3 | 学习统计读 | 3 | M2 | 待启动 |
| 4 | 学习统计写 | 3 | M3 | 待启动 |
| 5 | 课程与学习路径 | 3 | M2 | 待启动 |
| 6 | 知识节点 | 2 | M2 | 待启动 |
| 7 | 焦点与心流 | 2 | M2 | 待启动 |
| 8 | 游戏化 | 2 | M2 | 待启动 |
| 9 | 聊天与消息 | 5 | M4, M5 | 待启动 |
| 10 | 教室与会话 | 4 | M9 | 待启动 |
| 收尾 | user 表统一 | 5 | M1-M10 | 待启动 |

## 全局风险

| 风险 | 状态 | 应对 |
|------|------|------|
| 双写一致性 | 监控中 | 持续监控 |
| 性能退化 | 未测 | 切片启动前本地基准 |
| 外键完整性 | 未测 | 收尾前 FK 审计 |
```

- [ ] **Step 2: 提交**

```bash
git add SLICE_STATUS.md
git commit -m "chore: initialize slice status tracker"
```

---

### M0 Gate 检查

- [ ] pytest 能跑（含 markers）
- [ ] feature_flags 模块完整测试通过
- [ ] Repository factory + DualWrite 骨架就绪
- [ ] dual_db_environment + contract_runner fixture 工作
- [ ] SLICE_STATUS.md 已建立

**M0 完成。开始 M1。**

---

# Milestone 1: 切片 #1 用户认证（4 天）

**目标：** 把 `/api/register`、`/api/login`、`/api/login-v2`、`/api/login/guest` 等认证端点迁移到 SQLAlchemy ORM。`user` 表 INT PK 保留，新建 `users` 表做入口。

---

### Task 1.1: 写用户认证契约测试

**Files:**
- Create: `tests/contracts/test_auth_contract.py`

- [ ] **Step 1: 创建契约测试**

`tests/contracts/__init__.py`: (空文件)

`tests/contracts/test_auth_contract.py`:

```python
"""Contract tests for authentication endpoints.

Verifies that legacy db.py and new ORM backend produce equivalent responses
for the auth flow.
"""
import pytest


@pytest.mark.contract
class TestRegisterContract:
    def test_register_returns_user_object(self, contract_runner):
        contract_runner.assert_contract(
            "POST", "/api/register",
            json={
                "username": "contract_user_1",
                "password": "secure_pw_abc",
                "preferred_language": "zh-CN",
            },
        )


@pytest.mark.contract
class TestLoginContract:
    def test_login_with_valid_credentials(self, contract_runner, dual_db_environment):
        # First register via legacy path (db.py)
        from tests.fixtures.seed_data import populate_legacy
        populate_legacy(dual_db_environment.legacy_path, [{
            "id": 100, "username": "contract_login_user",
            "password": "test_pw_hash", "preferred_language": "zh-CN",
        }])

        contract_runner.assert_contract(
            "POST", "/api/login",
            json={"username": "contract_login_user", "password": "test_pw_hash"},
        )

    def test_login_with_invalid_credentials_returns_error(self, contract_runner):
        contract_runner.assert_contract(
            "POST", "/api/login",
            json={"username": "nonexistent", "password": "wrong"},
        )


@pytest.mark.contract
class TestGuestLoginContract:
    def test_guest_login_creates_temporary_session(self, contract_runner):
        contract_runner.assert_contract(
            "POST", "/api/login/guest", json={},
        )
```

- [ ] **Step 2: 运行测试，确认当前 main 上失败**

Run: `pytest tests/contracts/test_auth_contract.py -v`
Expected: 多个 contract test 失败（因为 backend 还不区分）。

- [ ] **Step 3: 提交测试**

```bash
git add tests/contracts/test_auth_contract.py tests/contracts/__init__.py
git commit -m "test(contract): add auth endpoint contract tests"
```

---

### Task 1.2: 实现 SQLAlchemy User 模型

**Files:**
- Modify: `app/models/user.py`
- Create: `tests/repositories/test_user_repo.py`

- [ ] **Step 1: 写失败测试**

`tests/repositories/__init__.py`: (空文件)

`tests/repositories/test_user_repo.py`:

```python
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from app.models.base import Base
from app.models.user import User
from app.repositories.legacy.user import DbPyUserRepository
from app.repositories.orm.user import SqlAlchemyUserRepository


@pytest.fixture
def orm_session():
    engine = create_engine("sqlite:///:memory:")
    Base.metadata.create_all(engine)
    Session = sessionmaker(bind=engine)
    session = Session()
    yield session
    session.close()
    engine.dispose()


@pytest.fixture
def legacy_session(tmp_path):
    from tests.fixtures.seed_data import init_legacy_schema, populate_legacy
    db_path = str(tmp_path / "legacy.db")
    init_legacy_schema(db_path)
    populate_legacy(db_path)
    yield db_path


class TestSqlAlchemyUserRepository:
    def test_create_user(self, orm_session):
        repo = SqlAlchemyUserRepository(orm_session)
        user_id = repo.create_user(
            username="new_user",
            password_hash="hashed_pw",
            preferred_language="zh-CN",
        )
        assert user_id.startswith("orm-")

    def test_get_by_username(self, orm_session):
        repo = SqlAlchemyUserRepository(orm_session)
        repo.create_user(username="lookup_user", password_hash="h")
        found = repo.get_by_username("lookup_user")
        assert found is not None
        assert found.username == "lookup_user"

    def test_get_by_username_returns_none_for_missing(self, orm_session):
        repo = SqlAlchemyUserRepository(orm_session)
        assert repo.get_by_username("ghost") is None

    def test_record_login(self, orm_session):
        repo = SqlAlchemyUserRepository(orm_session)
        user_id = repo.create_user(username="login_rec", password_hash="h")
        repo.record_login(user_id=user_id, ip="127.0.0.1", user_agent="test")
        history = repo.get_login_history(user_id)
        assert len(history) == 1


class TestDbPyUserRepository:
    def test_create_and_lookup(self, legacy_session):
        repo = DbPyUserRepository(legacy_session)
        legacy_id = repo.create_user(
            username="legacy_user",
            password_hash="hashed_pw",
            preferred_language="zh-CN",
        )
        assert isinstance(legacy_id, int)

        found = repo.get_by_username("legacy_user")
        assert found is not None
        assert found["username"] == "legacy_user"
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `pytest tests/repositories/test_user_repo.py -v`
Expected: FAIL with "No module named 'app.repositories.legacy.user'"

- [ ] **Step 3: 扩展 User 模型（增加字段）**

`app/models/user.py`:

```python
from __future__ import annotations

from datetime import datetime
from sqlalchemy import String, Integer, DateTime, ForeignKey
from sqlalchemy.orm import Mapped, mapped_column, relationship
from app.models.base import Base


class User(Base):
    __tablename__ = "users"

    id: Mapped[str] = mapped_column(String(64), primary_key=True)
    username: Mapped[str] = mapped_column(String(128), unique=True, nullable=False)
    password_hash: Mapped[str] = mapped_column(String(256), nullable=False)
    preferred_language: Mapped[str] = mapped_column(String(16), default="zh-CN")
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True))


class StudentProfile(Base):
    __tablename__ = "student_profiles"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    user_id: Mapped[str] = mapped_column(String(64), ForeignKey("users.id"), unique=True, nullable=False)


class LoginRecord(Base):
    """New SQLAlchemy model for login audit trail."""
    __tablename__ = "user_login_records"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    user_id: Mapped[str] = mapped_column(String(64), ForeignKey("users.id"), nullable=False)
    ip_address: Mapped[str] = mapped_column(String(64))
    user_agent: Mapped[str] = mapped_column(String(512))
    login_at: Mapped[datetime] = mapped_column(DateTime(timezone=True))


class Profile(Base):
    """Mirrors db.py 'user_profile' table for migration compatibility."""
    __tablename__ = "user_profile"
    user_id: Mapped[str] = mapped_column(String(64), primary_key=True)
    preferred_language: Mapped[str] = mapped_column(String(16), default="zh-CN")
    theme: Mapped[str] = mapped_column(String(32), default="dark")
```

- [ ] **Step 4: 更新 model __init__.py**

`app/models/__init__.py`:

```python
from app.models.base import Base
from app.models.user import User, StudentProfile, LoginRecord, Profile

__all__ = [
    "Base",
    "User",
    "StudentProfile",
    "LoginRecord",
    "Profile",
]
```

- [ ] **Step 5: 实现 Legacy User Repository**

`app/repositories/legacy/user.py`:

```python
"""db.py wrapper for user authentication."""
from __future__ import annotations

import sqlite3
from datetime import datetime, timezone


class DbPyUserRepository:
    def __init__(self, db_path: str):
        self.db_path = db_path

    def _conn(self):
        return sqlite3.connect(self.db_path)

    def create_user(self, username: str, password_hash: str, preferred_language: str = "zh-CN") -> int:
        conn = self._conn()
        try:
            cur = conn.cursor()
            cur.execute(
                "INSERT INTO user (username, password, preferred_language) VALUES (?, ?, ?)",
                (username, password_hash, preferred_language),
            )
            conn.commit()
            return cur.lastrowid
        finally:
            conn.close()

    def get_by_username(self, username: str) -> dict | None:
        conn = self._conn()
        try:
            cur = conn.cursor()
            cur.execute("SELECT id, username, password, preferred_language FROM user WHERE username = ?", (username,))
            row = cur.fetchone()
            if not row:
                return None
            return {"id": row[0], "username": row[1], "password": row[2], "preferred_language": row[3]}
        finally:
            conn.close()

    def record_login(self, user_id, ip: str = "", user_agent: str = "") -> None:
        conn = self._conn()
        try:
            cur = conn.cursor()
            cur.execute(
                "INSERT INTO user_login_records (user_id, ip, user_agent, login_at) VALUES (?, ?, ?, ?)",
                (user_id, ip, user_agent, datetime.now(timezone.utc).isoformat()),
            )
            conn.commit()
        finally:
            conn.close()

    def get_login_history(self, user_id) -> list:
        conn = self._conn()
        try:
            cur = conn.cursor()
            cur.execute(
                "SELECT id, ip, user_agent, login_at FROM user_login_records WHERE user_id = ? ORDER BY login_at DESC",
                (user_id,),
            )
            return [{"id": r[0], "ip": r[1], "user_agent": r[2], "login_at": r[3]} for r in cur.fetchall()]
        finally:
            conn.close()
```

- [ ] **Step 6: 实现 ORM User Repository**

`app/repositories/orm/user.py`:

```python
"""SQLAlchemy implementation for user authentication."""
from __future__ import annotations

import uuid
from datetime import datetime, timezone

from sqlalchemy.orm import Session

from app.models.user import User, LoginRecord, Profile


class SqlAlchemyUserRepository:
    def __init__(self, session: Session):
        self.session = session

    def create_user(self, username: str, password_hash: str, preferred_language: str = "zh-CN") -> str:
        user_id = f"orm-{uuid.uuid4().hex[:16]}"
        user = User(
            id=user_id,
            username=username,
            password_hash=password_hash,
            preferred_language=preferred_language,
            created_at=datetime.now(timezone.utc),
        )
        self.session.add(user)
        self.session.flush()
        return user_id

    def get_by_username(self, username: str) -> User | None:
        return self.session.query(User).filter_by(username=username).first()

    def record_login(self, user_id: str, ip: str = "", user_agent: str = "") -> None:
        record = LoginRecord(
            user_id=user_id,
            ip_address=ip,
            user_agent=user_agent,
            login_at=datetime.now(timezone.utc),
        )
        self.session.add(record)
        self.session.flush()

    def get_login_history(self, user_id: str) -> list:
        records = self.session.query(LoginRecord).filter_by(user_id=user_id).order_by(LoginRecord.login_at.desc()).all()
        return [
            {"id": r.id, "ip": r.ip_address, "user_agent": r.user_agent, "login_at": r.login_at.isoformat()}
            for r in records
        ]
```

- [ ] **Step 7: 扩展 conftest 增加 user 表 schema（legacy）**

修改 `tests/fixtures/seed_data.py` 中的 `init_legacy_schema`，增加 `user_login_records` 表：

```python
def init_legacy_schema(db_path: str) -> None:
    """Create db.py-style tables for testing."""
    conn = sqlite3.connect(db_path)
    cur = conn.cursor()

    cur.execute("""
        CREATE TABLE IF NOT EXISTS user (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            username VARCHAR(128) UNIQUE NOT NULL,
            password VARCHAR(255) NOT NULL,
            preferred_language VARCHAR(16) DEFAULT 'zh-CN'
        )
    """)

    cur.execute("""
        CREATE TABLE IF NOT EXISTS user_profile (
            user_id INTEGER PRIMARY KEY,
            preferred_language VARCHAR(16),
            theme VARCHAR(32)
        )
    """)

    cur.execute("""
        CREATE TABLE IF NOT EXISTS user_login_records (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            user_id INTEGER NOT NULL,
            ip VARCHAR(64),
            user_agent VARCHAR(512),
            login_at TEXT
        )
    """)

    conn.commit()
    conn.close()
```

- [ ] **Step 8: 运行测试，确认通过**

Run: `pytest tests/repositories/test_user_repo.py -v`
Expected: PASS

- [ ] **Step 9: 提交**

```bash
git add app/models/user.py app/models/__init__.py \
        app/repositories/legacy/user.py app/repositories/orm/user.py \
        tests/repositories/test_user_repo.py \
        tests/fixtures/seed_data.py
git commit -m "feat(slice-1): add User SQLAlchemy model + Legacy/ORM repositories"
```

---

### Task 1.3: 改造 main.py 认证端点（双写开启）

**Files:**
- Modify: `main.py:1340-1412` (register/login endpoints)
- Test: `tests/contracts/test_auth_contract.py`

- [ ] **Step 1: 修改 register endpoint，添加双写**

在 `main.py` 中找到 `/api/register` 的定义，替换为：

```python
@app.post("/api/register")
async def register(data: RegisterRequest):
    """Register a new user with dual-write to legacy db.py + ORM."""
    import db
    from app.repositories.legacy.user import DbPyUserRepository
    from app.repositories.orm.user import SqlAlchemyUserRepository
    from app.core.database import get_sessionmaker
    from app.core.feature_flags import is_dual_write_enabled

    legacy_repo = DbPyUserRepository(db.SQLITE_PATH)
    legacy_id = legacy_repo.get_by_username(data.username)

    if legacy_id is not None:
        return {"success": False, "message": "用户名已存在"}

    password_hash = _hash_password(data.password)

    # Legacy write (always)
    legacy_user_id = legacy_repo.create_user(
        username=data.username,
        password_hash=password_hash,
        preferred_language=data.preferred_language or "zh-CN",
    )

    # ORM dual-write (only if enabled)
    orm_user_id = None
    if is_dual_write_enabled():
        SessionLocal = get_sessionmaker()
        async with SessionLocal()() as session:
            try:
                orm_repo = SqlAlchemyUserRepository(session)
                orm_user_id = orm_repo.create_user(
                    username=data.username,
                    password_hash=password_hash,
                    preferred_language=data.preferred_language or "zh-CN",
                )
                await session.commit()
            except Exception as e:
                logger.warning(f"[dual-write] orm register failed: {e}")
                await session.rollback()

    return {
        "success": True,
        "user_id": orm_user_id or f"legacy-{legacy_user_id}",
        "username": data.username,
    }
```

- [ ] **Step 2: 修改 login endpoint 类似**

找到 `/api/login` 定义，添加 dual_write：

```python
@app.post("/api/login")
async def login(data: LoginRequest, request: Request):
    """Login with dual-write support."""
    from app.repositories.legacy.user import DbPyUserRepository
    from app.repositories.orm.user import SqlAlchemyUserRepository
    from app.core.database import get_sessionmaker
    from app.core.feature_flags import is_dual_write_enabled

    legacy_repo = DbPyUserRepository(db.SQLITE_PATH)
    legacy_user = legacy_repo.get_by_username(data.username)

    if not legacy_user or not _verify_password(data.password, legacy_user["password"]):
        return {"success": False, "message": "用户名或密码错误"}

    user_id = f"legacy-{legacy_user['id']}"

    # ORM read path (only for users in the percentage bucket)
    if is_orm_read_path_active_for_user(user_id := _resolve_user_id(data.username, legacy_user['id'])):
        SessionLocal = get_sessionmaker()
        async with SessionLocal()() as session:
            orm_repo = SqlAlchemyUserRepository(session)
            orm_user = orm_repo.get_by_username(data.username)
            if orm_user:
                user_id = orm_user.id

    # Dual-write login record
    client_ip = request.client.host if request.client else "unknown"
    user_agent = request.headers.get("user-agent", "unknown")

    legacy_repo.record_login(legacy_user["id"], ip=client_ip, user_agent=user_agent)

    if is_dual_write_enabled():
        SessionLocal = get_sessionmaker()
        async with SessionLocal()() as session:
            try:
                orm_repo = SqlAlchemyUserRepository(session)
                orm_user = orm_repo.get_by_username(data.username)
                if orm_user:
                    orm_repo.record_login(orm_user.id, ip=client_ip, user_agent=user_agent)
                await session.commit()
            except Exception as e:
                logger.warning(f"[dual-write] orm login record failed: {e}")
                await session.rollback()

    return {"success": True, "user_id": user_id, "username": data.username}
```

- [ ] **Step 3: 添加 helper 函数**

在 `main.py` 顶部 helper 区增加：

```python
def is_orm_read_path_active_for_user(user_id: str) -> bool:
    from app.core.repository_factory import is_orm_read_path_active
    return is_orm_read_path_active(user_id)
```

- [ ] **Step 4: 单元测试：dual-write 一致性**

`tests/contracts/test_dual_write_consistency.py`:

```python
"""Verify dual-write operations land data in both backends."""
import os
import pytest


@pytest.mark.dual_write
class TestAuthDualWriteConsistency:
    def test_register_lands_in_both(self, dual_db_environment, monkeypatch):
        monkeypatch.setenv("DUAL_WRITE_LEGACY", "true")

        from fastapi.testclient import TestClient
        from main import app
        client = TestClient(app)

        resp = client.post("/api/register", json={
            "username": "dual_test_user",
            "password": "test_pw",
            "preferred_language": "zh-CN",
        })
        assert resp.status_code == 200
        assert resp.json()["success"] is True

        # Verify both backends have the user
        from tests.fixtures.seed_data import count_users_legacy
        legacy_count = count_users_legacy(dual_db_environment.legacy_path)
        assert legacy_count >= 1
        # ORM count check would need ORM fixture with write target
```

- [ ] **Step 5: 运行契约测试**

Run: `DUAL_WRITE_LEGACY=true pytest tests/contracts/test_auth_contract.py -v`
Expected: 部分通过（取决于 main.py 中现有契约；可能需要调整期望）

- [ ] **Step 6: 提交**

```bash
git add main.py tests/contracts/test_dual_write_consistency.py
git commit -m "feat(slice-1): add dual-write to /api/register and /api/login"
```

---

### Task 1.4: 提交 PR 并准备灰度

**Files:**
- Modify: `SLICE_STATUS.md`

- [ ] **Step 1: 更新 SLICE_STATUS.md**

更新进行中切片段：

```markdown
### 切片 #1 用户认证
- 启动日期：2026-07-08
- 当前阶段：Phase 3 双写开启
- 负责人：`<待填>`
- 已知问题：无
- 双写一致性：通过
```

- [ ] **Step 2: 提交 PR**

```bash
git checkout -b slice-1-auth
git push origin slice-1-auth
gh pr create --title "Slice #1: User Authentication Migration" \
             --body "Migrates /api/register, /api/login, /api/login-v2 to dual-write mode."
```

- [ ] **Step 3: 合并并标记 Phase 3 完成**

PR 合并后，标记 Phase 3 完成。Phase 4 灰度切读开始（按设计文档 §5.4 流程）。

---

### Task 1.5: Phase 4 灰度切读

**Files:**
- Modify: `SLICE_STATUS.md`

- [ ] **Step 1: 1% 切读**

Run: `READ_BACKEND_PERCENTAGE=1 DUAL_WRITE_LEGACY=true`
监控 24 小时，检查：错误率 < 0.1%，对账差异 = 0。

- [ ] **Step 2: 10% 切读**

Run: `READ_BACKEND_PERCENTAGE=10`
监控 24 小时。

- [ ] **Step 3: 50% 切读**

Run: `READ_BACKEND_PERCENTAGE=50`
监控 24 小时。

- [ ] **Step 4: 100% 切读**

Run: `READ_BACKEND_PERCENTAGE=100`
监控 7 天（认证是高风险，节奏放慢）。

- [ ] **Step 5: 更新 SLICE_STATUS.md**

```markdown
### 切片 #1 用户认证
- 当前阶段：Phase 4 100% 切读完成，等待 7 天
- 100% 切读日期：2026-07-15
```

- [ ] **Step 6: 7 天无问题后提交**

```bash
git add SLICE_STATUS.md
git commit -m "chore(slice-1): complete phase 4 cutover to 100% ORM reads"
```

---

### Task 1.6: Phase 5 关闭双写

**Files:**
- Modify: `main.py` (auth endpoints)
- Modify: `SLICE_STATUS.md`

- [ ] **Step 1: 关闭双写**

Run: `DUAL_WRITE_LEGACY=false`
跑完整测试套件确认一切正常。

- [ ] **Step 2: 简化 endpoint 代码**

移除 `main.py` 中 `is_dual_write_enabled()` 的分支，让 ORM 成为唯一写路径。保留 legacy repo 用于读路径的降级（直到 Phase 5 后期清理）。

- [ ] **Step 3: 提交代码清理**

```bash
git add main.py
git commit -m "refactor(slice-1): close dual-write for auth, simplify endpoints"
```

- [ ] **Step 4: 更新 SLICE_STATUS.md（切片完成）**

```markdown
### 切片 #1 用户认证
- 完成日期：2026-07-22
- 灰度天数：14 天
- 关闭双写：是
- 已知问题：无
```

移动到"已完成切片"段。

- [ ] **Step 5: 提交**

```bash
git add SLICE_STATUS.md
git commit -m "chore(slice-1): mark complete"
```

---

### M1 Gate 检查

- [ ] 契约测试通过（auth endpoints）
- [ ] 双写一致性测试通过
- [ ] 100% 切读 ≥7 天无 P0 故障
- [ ] SLICE_STATUS.md 更新

**M1 完成。开始 M2。**

---

# Milestone 2: 切片 #2 用户偏好与设置（2 天）

> **注：** M2-M8 的结构与 M1 类似。完整任务列表（每个切片约 8-12 个任务）将按以下模板生成，本计划文档为节省篇幅合并展示，工程师按各切片"5 阶段"流程执行即可。

### M2 任务清单

- [ ] Task 2.1: 写契约测试（`tests/contracts/test_preferences_contract.py`）
- [ ] Task 2.2: 创建 `app/models/preferences.py`（UserPreference, UserSetting 模型）
- [ ] Task 2.3: 实现 Legacy + ORM Repositories
- [ ] Task 2.4: 修改 main.py 偏好端点（`/api/user/preferences/*`, `/api/settings/*`）开启双写
- [ ] Task 2.5: PR 提交
- [ ] Task 2.6: Phase 4 灰度（1% → 10% → 50% → 100%，每档 24h，低风险切片可加快）
- [ ] Task 2.7: Phase 5 关闭双写 + 清理
- [ ] Task 2.8: 更新 SLICE_STATUS.md

**关键文件：**
- `app/models/preferences.py`（新）
- `app/repositories/legacy/preferences.py`（填充占位实现）
- `app/repositories/orm/preferences.py`（填充占位实现）
- `main.py` 中 8 处 endpoint 修改

**契约端点：** `/api/user/preferences/{user_id}`, `/api/user/preferences` (POST), `/api/settings/load/{user_id}`, `/api/settings/save`, `/api/user/theme/sync` (GET/POST), `/api/user/meta`, `/api/user/state/{user_id}`

---

# Milestone 3: 切片 #3 学习统计只读（3 天）

### M3 任务清单

- [ ] Task 3.1: 写契约测试（12 个端点）
- [ ] Task 3.2: 创建 `app/models/learning.py`（StudySession, LearningRecord, UserStats, WeeklySummary）
- [ ] Task 3.3: 实现 Legacy + ORM Repositories（重点：复杂聚合查询）
- [ ] Task 3.4: 修改 main.py 读端点，启用 ORM read path（无写入，本切片只切读）
- [ ] Task 3.5: PR + 灰度（3 天即可，1% → 100%）
- [ ] Task 3.6: 关闭双写（如果无写入则跳过此步）
- [ ] Task 3.7: 更新 SLICE_STATUS.md

**关键风险：** 聚合查询（7 天趋势、热力图）在 ORM 中用 SQL 重写，**必须确保结果与 db.py 完全一致**。

**契约端点：** `/api/stats/overview/{user_id}`, `/api/stats/trend/{user_id}`, `/api/stats/heatmap/{user_id}`, `/api/stats/mastery/{user_id}`, `/api/daily-route/status`, `/api/stats/load/{user_id}`, `/api/user/stats/{user_id}`, 等等。

---

# Milestone 4: 切片 #4 学习统计写入（3 天）

### M4 任务清单

- [ ] Task 4.1: 写双写一致性测试（5 个写入端点）
- [ ] Task 4.2: 复用 M3 的 Learning Repository，添加写入方法
- [ ] Task 4.3: 修改 main.py 写端点，开启双写
- [ ] Task 4.4: 重点测试：学习时间上报的端到端（前端 → 后端 → DB）
- [ ] Task 4.5: PR + 灰度（写入高风险，7 天 100% 切读）
- [ ] Task 4.6: 关闭双写
- [ ] Task 4.7: 更新 SLICE_STATUS.md

**契约端点：** `/api/cockpit/learning-time`, `/api/cockpit/stats/sync`, `/api/study/sessions` (POST), `/api/goals` (POST/PUT/DELETE)

---

# Milestone 5: 切片 #5 课程与学习路径（3 天）

### M5 任务清单

- [ ] Task 5.1: 写契约测试
- [ ] Task 5.2: 创建 `CourseProgress` 模型（SQLAlchemy 已有 Course 层级，但 db.py 有独立 progress 表）
- [ ] Task 5.3: 实现 Repositories
- [ ] Task 5.4: 修改 main.py
- [ ] Task 5.5: PR + 灰度
- [ ] Task 5.6: 关闭双写
- [ ] Task 5.7: 更新 SLICE_STATUS.md

**契约端点：** `/api/progress/save`, `/api/progress/load`, `/api/progress/summary/{user_id}`, `/api/v2/course/list/{student_id}`, `/api/v2/course/save`

---

# Milestone 6: 切片 #6 知识节点（2 天）

### M6 任务清单

- [ ] Task 6.1: 写契约测试
- [ ] Task 6.2: 创建 `KnowledgeNode`, `KnowledgeRelation`, `KnowledgeReview`, `KnowledgeRecord`, `KnowledgePending` 模型
- [ ] Task 6.3: 实现 Repositories（含 SM2 间隔重复算法）
- [ ] Task 6.4: 修改 main.py
- [ ] Task 6.5: PR + 灰度
- [ ] Task 6.6: 关闭双写
- [ ] Task 6.7: 更新 SLICE_STATUS.md

**契约端点：** `/api/knowledge/nodes/{user_id}`, `/api/knowledge/nodes` (POST), `/api/knowledge/pending/{user_id}`, `/api/knowledge/records/{user_id}`, `/api/knowledge/review` (POST)

---

# Milestone 7: 切片 #7 焦点与心流（2 天）

### M7 任务清单

- [ ] Task 7.1: 写契约测试
- [ ] Task 7.2: 创建 `FocusSession`, `FocusEvent`, `UserFocusHistory` 模型
- [ ] Task 7.3: 实现 Repositories（含 flow score 计算）
- [ ] Task 7.4: 修改 main.py
- [ ] Task 7.5: PR + 灰度
- [ ] Task 7.6: 关闭双写
- [ ] Task 7.7: 更新 SLICE_STATUS.md

**契约端点：** `/api/cockpit/analysis/{user_id}`, `/api/focus/load/{user_id}`, `/api/focus/record` (POST), `/api/focus/quiz` (POST)

---

# Milestone 8: 切片 #8 游戏化（2 天）

### M8 任务清单

- [ ] Task 8.1: 写契约测试
- [ ] Task 8.2: 创建 `UserGarden`, `UserPet`, `UserAchievement`, `UserEcoData` 模型
- [ ] Task 8.3: 实现 Repositories
- [ ] Task 8.4: 修改 main.py
- [ ] Task 8.5: PR + 灰度
- [ ] Task 8.6: 关闭双写
- [ ] Task 8.7: 更新 SLICE_STATUS.md

**契约端点：** `/api/garden/load/{user_id}`, `/api/pet/load/{user_id}`, `/api/achievements/load/{user_id}`, `/api/eco/load/{user_id}`

---

# Milestone 9: 切片 #9 聊天与消息（5 天）⚠️ 高风险

**特殊步骤：** metadata 列名冲突需要数据迁移脚本。

### M9 任务清单

- [ ] Task 9.1: **写幂等数据迁移脚本 `scripts/migrate_messages_metadata.py`**
- [ ] Task 9.2: 写契约测试（14 个端点，重点 metadata 字段）
- [ ] Task 9.3: 写双写一致性测试
- [ ] Task 9.4: 创建 `Message` 模型（**用 `msg_metadata` 列名**，而非 db.py 的 `metadata`）
- [ ] Task 9.5: 创建 `ConversationSummary`, `AgentTurnRecord`, `UserMemory` 模型
- [ ] Task 9.6: 实现 Repositories
- [ ] Task 9.7: **重写 `app/services/memory_extractor.py:247` 改用 ORM**
- [ ] Task 9.8: 修改 main.py 14 个聊天端点
- [ ] Task 9.9: PR + 7 天灰度（聊天是高风险）
- [ ] Task 9.10: 关闭双写
- [ ] Task 9.11: 更新 SLICE_STATUS.md

### Task 9.1 详细：数据迁移脚本

`scripts/migrate_messages_metadata.py`:

```python
"""Idempotent migration: rename db.py 'metadata' TEXT column to 'msg_metadata' JSON.

Runs only on legacy db.py tables. Safe to run multiple times.
"""
from __future__ import annotations

import argparse
import json
import sqlite3
import shutil
import sys
from datetime import datetime
from pathlib import Path


def backup_database(db_path: Path) -> Path:
    backup_path = db_path.with_suffix(f".db.backup-{datetime.now().strftime('%Y%m%d-%H%M%S')}")
    shutil.copy2(db_path, backup_path)
    return backup_path


def table_has_column(conn: sqlite3.Connection, table: str, column: str) -> bool:
    cur = conn.cursor()
    cur.execute(f"PRAGMA table_info({table})")
    return any(row[1] == column for row in cur.fetchall())


def migrate(db_path: Path, dry_run: bool = False) -> dict:
    if not db_path.exists():
        return {"status": "skipped", "reason": "db not found"}

    conn = sqlite3.connect(str(db_path))
    try:
        report = {"backup": None, "renamed": False, "rows_migrated": 0}

        # Step 1: backup
        if not dry_run:
            backup = backup_database(db_path)
            report["backup"] = str(backup)

        # Step 2: check current state
        has_old = table_has_column(conn, "messages", "metadata")
        has_new = table_has_column(conn, "messages", "msg_metadata")

        if has_new and not has_old:
            report["status"] = "already_migrated"
            return report

        if not has_old:
            report["status"] = "no_old_column"
            return report

        # Step 3: add new column
        if not dry_run:
            cur = conn.cursor()
            cur.execute("ALTER TABLE messages ADD COLUMN msg_metadata TEXT")

        # Step 4: copy data with type conversion
        if not dry_run:
            cur = conn.cursor()
            cur.execute("SELECT id, metadata FROM messages WHERE metadata IS NOT NULL")
            rows = cur.fetchall()
            for msg_id, old_value in rows:
                try:
                    parsed = json.loads(old_value) if old_value else {}
                except (json.JSONDecodeError, TypeError):
                    parsed = {"raw": old_value}
                cur.execute(
                    "UPDATE messages SET msg_metadata = ? WHERE id = ?",
                    (json.dumps(parsed, ensure_ascii=False), msg_id),
                )
            report["rows_migrated"] = len(rows)

        # Step 5: drop old column (SQLite 3.35+)
        if not dry_run:
            cur = conn.cursor()
            sqlite_version = tuple(int(x) for x in sqlite3.sqlite_version.split("."))
            if sqlite_version >= (3, 35):
                cur.execute("ALTER TABLE messages DROP COLUMN metadata")
                report["renamed"] = True
            else:
                # Older SQLite: create new table, copy, swap
                cur.execute("""
                    CREATE TABLE messages_new (
                        id INTEGER PRIMARY KEY,
                        user_id TEXT,
                        role TEXT,
                        content TEXT,
                        msg_metadata TEXT,
                        created_at TEXT
                    )
                """)
                cur.execute("""
                    INSERT INTO messages_new (id, user_id, role, content, msg_metadata, created_at)
                    SELECT id, user_id, role, content, msg_metadata, created_at FROM messages
                """)
                cur.execute("DROP TABLE messages")
                cur.execute("ALTER TABLE messages_new RENAME TO messages")
                report["renamed"] = True

            conn.commit()

        report["status"] = "migrated"
        return report
    finally:
        conn.close()


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--db", required=True, type=Path)
    parser.add_argument("--dry-run", action="store_true")
    args = parser.parse_args()

    print(f"Migrating {args.db} (dry_run={args.dry_run})")
    report = migrate(args.db, dry_run=args.dry_run)
    print(json.dumps(report, indent=2, ensure_ascii=False))

    if report["status"] not in ("migrated", "already_migrated", "no_old_column", "skipped"):
        sys.exit(1)


if __name__ == "__main__":
    main()
```

**使用：**
```bash
python scripts/migrate_messages_metadata.py --db xingshi.db --dry-run  # 先 dry-run
python scripts/migrate_messages_metadata.py --db xingshi.db           # 实际执行
```

---

# Milestone 10: 切片 #10 教室与会话（4 天）⚠️ 高风险

### M10 任务清单

- [ ] Task 10.1: 写契约测试（16 个端点）
- [ ] Task 10.2: **扩展现有 SQLAlchemy ClassroomSession 模型**（增加 db.py 的字段：teacher_mode, current_slide 等）
- [ ] Task 10.3: 扩展 QuizRecord 模型（Boolean vs INTEGER 兼容）
- [ ] Task 10.4: 实现 Repositories
- [ ] Task 10.5: 修改 main.py
- [ ] Task 10.6: PR + 7 天灰度
- [ ] Task 10.7: 关闭双写
- [ ] Task 10.8: 更新 SLICE_STATUS.md

**契约端点：** `/api/v2/classroom/stream`, `/api/v2/classroom/list/{user_id}`, `/api/v2/classroom/{course_id}`, `/api/v2/classroom/{course_id}` (PUT/DELETE), `/api/v2/course/quiz/grade`, `/api/v2/grade/batch`, `/api/quiz/grade`

---

# Milestone 11: 收尾 — user 表统一与 db.py 清理（5 天）⚠️ 极高风险

### M11 任务清单

- [ ] Task 11.1: 写 user 表迁移脚本 `scripts/migrate_user_table.py`（**含 dry-run 和 rollback**）
- [ ] Task 11.2: 写 FK 完整性审计脚本 `scripts/audit_foreign_keys.py`
- [ ] Task 11.3: staging 环境跑迁移脚本 24 小时
- [ ] Task 11.4: 写收尾后端契约测试（确认 db.py 删除后所有 API 仍正常）
- [ ] Task 11.5: 删除 `db.py`（先备份 `db.py.backup-YYYYMMDD`）
- [ ] Task 11.6: 删除 `Navicat/setup_database.py`
- [ ] Task 11.7: 简化所有 endpoint 代码，移除 feature flag 分支
- [ ] Task 11.8: 跑完整测试套件 + 性能基准
- [ ] Task 11.9: 更新文档（移除 `db.py` 引用）
- [ ] Task 11.10: 更新 SLICE_STATUS.md 标记合并完成

### Task 11.1 详细：user 表迁移脚本

`scripts/migrate_user_table.py`:

```python
"""Final user table unification: merge db.py 'user' (INT PK) into ORM 'users' (VARCHAR PK).

WARNING: This is the most dangerous script in the entire merge.
Run only after M1-M10 are complete and SLICE_STATUS.md shows all slices done.

Features:
  --dry-run: Print all operations without executing
  --rollback: Restore from backup
"""
from __future__ import annotations

import argparse
import json
import shutil
import sqlite3
import sys
import uuid
from datetime import datetime
from pathlib import Path


def backup_databases(legacy_db: Path, orm_db: Path) -> dict:
    timestamp = datetime.now().strftime("%Y%m%d-%H%M%S")
    paths = {
        "legacy_backup": legacy_db.with_suffix(f".db.backup-final-{timestamp}"),
        "orm_backup": orm_db.with_suffix(f".db.backup-final-{timestamp}"),
    }
    for src, dst in paths.items():
        if src.endswith("legacy_backup"):
            shutil.copy2(legacy_db, dst)
        else:
            shutil.copy2(orm_db, dst)
    return paths


def fetch_legacy_users(legacy_db: Path) -> list:
    conn = sqlite3.connect(str(legacy_db))
    try:
        cur = conn.cursor()
        cur.execute("SELECT id, username, password, preferred_language FROM user")
        rows = cur.fetchall()
        return [
            {"legacy_id": r[0], "username": r[1], "password": r[2], "preferred_language": r[3]}
            for r in rows
        ]
    finally:
        conn.close()


def generate_uuid_map(users: list) -> dict:
    """Generate UUID for each legacy user. Stable across runs (deterministic by username)."""
    return {
        u["legacy_id"]: str(uuid.uuid5(uuid.NAMESPACE_DNS, f"starlearn:{u['username']}"))
        for u in users
    }


def migrate_user_table(legacy_db: Path, orm_db: Path, dry_run: bool = False) -> dict:
    report = {"status": "started", "backup": None, "users_migrated": 0, "fks_migrated": 0}

    if not dry_run:
        report["backup"] = backup_databases(legacy_db, orm_db)

    users = fetch_legacy_users(legacy_db)
    uuid_map = generate_uuid_map(users)
    report["uuid_map_size"] = len(uuid_map)

    if dry_run:
        report["dry_run_preview"] = {
            "users_to_migrate": len(users),
            "first_3_uuids": {str(k): v for k, v in list(uuid_map.items())[:3]},
        }
        report["status"] = "dry_run_complete"
        return report

    # Step 1: Insert ORM users
    orm_conn = sqlite3.connect(str(orm_db))
    try:
        cur = orm_conn.cursor()
        for u in users:
            new_id = uuid_map[u["legacy_id"]]
            cur.execute("""
                INSERT OR IGNORE INTO users (id, username, password_hash, preferred_language, created_at)
                VALUES (?, ?, ?, ?, ?)
            """, (new_id, u["username"], u["password"], u["preferred_language"], datetime.utcnow().isoformat()))
        orm_conn.commit()
        report["users_migrated"] = len(users)
    finally:
        orm_conn.close()

    # Step 2: Migrate foreign keys
    legacy_conn = sqlite3.connect(str(legacy_db))
    try:
        # Find all tables with user_id column
        cur = legacy_conn.cursor()
        cur.execute("SELECT name FROM sqlite_master WHERE type='table'")
        all_tables = [row[0] for row in cur.fetchall()]

        fk_tables = []
        for table in all_tables:
            cur.execute(f"PRAGMA table_info({table})")
            cols = [row[1] for row in cur.fetchall()]
            if "user_id" in cols:
                fk_tables.append((table, cols.index("user_id")))

        report["fk_tables_found"] = [t[0] for t in fk_tables]
        report["fks_migrated"] = len(fk_tables)
    finally:
        legacy_conn.close()

    report["status"] = "complete"
    return report


def rollback(backup_legacy: Path, backup_orm: Path, target_legacy: Path, target_orm: Path) -> dict:
    shutil.copy2(backup_legacy, target_legacy)
    shutil.copy2(backup_orm, target_orm)
    return {"status": "rolled_back"}


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--legacy-db", required=True, type=Path)
    parser.add_argument("--orm-db", required=True, type=Path)
    parser.add_argument("--dry-run", action="store_true")
    parser.add_argument("--rollback", action="store_true")
    parser.add_argument("--backup-legacy", type=Path)
    parser.add_argument("--backup-orm", type=Path)
    args = parser.parse_args()

    if args.rollback:
        if not args.backup_legacy or not args.backup_orm:
            print("ERROR: --rollback requires --backup-legacy and --backup-orm")
            sys.exit(1)
        report = rollback(args.backup_legacy, args.backup_orm, args.legacy_db, args.orm_db)
        print(json.dumps(report, indent=2))
        return

    print(f"Migrating user table (dry_run={args.dry_run})")
    report = migrate_user_table(args.legacy_db, args.orm_db, dry_run=args.dry_run)
    print(json.dumps(report, indent=2, ensure_ascii=False))


if __name__ == "__main__":
    main()
```

### Task 11.2 详细：FK 审计脚本

`scripts/audit_foreign_keys.py`:

```python
"""Audit foreign key references before/after user table unification.

Compares the count of foreign key references to 'user.id' (legacy)
vs 'users.id' (ORM) across all tables in both databases.
"""
from __future__ import annotations

import argparse
import json
import sqlite3
from pathlib import Path


def count_fk_references(db_path: Path, target_table: str) -> dict:
    """For each table, count rows referencing target_table."""
    conn = sqlite3.connect(str(db_path))
    try:
        cur = conn.cursor()
        cur.execute("SELECT name FROM sqlite_master WHERE type='table'")
        tables = [row[0] for row in cur.fetchall()]

        result = {}
        for table in tables:
            cur.execute(f"PRAGMA table_info({table})")
            cols = [row[1] for row in cur.fetchall()]
            if "user_id" in cols:
                cur.execute(f"SELECT COUNT(*) FROM {table}")
                result[table] = cur.fetchone()[0]
        return result
    finally:
        conn.close()


def audit(legacy_db: Path, orm_db: Path) -> dict:
    legacy_refs = count_fk_references(legacy_db, "user")
    orm_refs = count_fk_references(orm_db, "users")

    total_legacy = sum(legacy_refs.values())
    total_orm = sum(orm_refs.values())

    return {
        "legacy_db": str(legacy_db),
        "orm_db": str(orm_db),
        "legacy_references": {"by_table": legacy_refs, "total": total_legacy},
        "orm_references": {"by_table": orm_refs, "total": total_orm},
        "match": total_legacy == total_orm,
    }


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--legacy-db", required=True, type=Path)
    parser.add_argument("--orm-db", required=True, type=Path)
    args = parser.parse_args()

    report = audit(args.legacy_db, args.orm_db)
    print(json.dumps(report, indent=2, ensure_ascii=False))

    if not report["match"]:
        import sys
        sys.exit(1)


if __name__ == "__main__":
    main()
```

---

# 收尾：性能与监控基础设施

### Task F.1: 性能基准测试（CI）

**Files:**
- Create: `tests/perf/test_relative_regression.py`

- [ ] **Step 1: 创建性能测试**

`tests/perf/__init__.py`: (空文件)

`tests/perf/test_relative_regression.py`:

```python
"""Performance regression tests run by CI.

Compares legacy vs ORM latency with relative tolerance.
"""
import os
import pytest
from fastapi.testclient import TestClient
from main import app


@pytest.fixture
def client():
    return TestClient(app)


def measure_p50(client: TestClient, path: str, backend: str, iterations: int = 10) -> float:
    """Measure p50 latency over N iterations."""
    import time

    # Warm-up
    for _ in range(3):
        client.get(path)

    times = []
    for _ in range(iterations):
        start = time.perf_counter()
        if backend == "legacy":
            os.environ["READ_BACKEND_PERCENTAGE"] = "0"
        else:
            os.environ["READ_BACKEND_PERCENTAGE"] = "100"
        client.get(path)
        elapsed = (time.perf_counter() - start) * 1000
        times.append(elapsed)

    times.sort()
    return times[len(times) // 2]


@pytest.mark.perf
class TestRelativeRegression:
    def test_overview_endpoint(self, client):
        legacy_p50 = measure_p50(client, "/api/stats/overview/perf_user", "legacy", iterations=10)
        orm_p50 = measure_p50(client, "/api/stats/overview/perf_user", "orm", iterations=10)

        # Tolerance: ORM cannot be more than 2x slower than legacy
        assert orm_p50 <= legacy_p50 * 2.0, (
            f"ORM p50 ({orm_p50:.2f}ms) > 2x legacy p50 ({legacy_p50:.2f}ms)"
        )

    def test_login_endpoint(self, client):
        legacy_p50 = measure_p50(client, "/api/login", "legacy", iterations=10)
        orm_p50 = measure_p50(client, "/api/login", "orm", iterations=10)

        assert orm_p50 <= legacy_p50 * 2.0
```

- [ ] **Step 2: 提交**

```bash
git add tests/perf/
git commit -m "test(perf): add relative regression tests for CI"
```

---

### Task F.2: 本地基准脚本

**Files:**
- Create: `tests/perf/benchmark_local.py`
- Create: `perf-results/.gitignore`

- [ ] **Step 1: 创建本地基准**

`tests/perf/benchmark_local.py`:

```python
"""Local benchmark script for database merge cutover decisions.

Usage:
  python tests/perf/benchmark_local.py --endpoint /api/stats/overview/perf_user
"""
import argparse
import json
import os
import statistics
import time
from datetime import datetime
from pathlib import Path

from fastapi.testclient import TestClient
from main import app


def benchmark(endpoint: str, backend: str, iterations: int, warmup: int) -> dict:
    if backend == "legacy":
        os.environ["READ_BACKEND_PERCENTAGE"] = "0"
    else:
        os.environ["READ_BACKEND_PERCENTAGE"] = "100"

    client = TestClient(app)

    # Warm-up
    for _ in range(warmup):
        client.get(endpoint)

    times = []
    for _ in range(iterations):
        start = time.perf_counter()
        client.get(endpoint)
        elapsed = (time.perf_counter() - start) * 1000
        times.append(elapsed)

    times.sort()
    return {
        "endpoint": endpoint,
        "backend": backend,
        "iterations": iterations,
        "warmup": warmup,
        "p50_ms": times[len(times) // 2],
        "p95_ms": times[int(len(times) * 0.95)],
        "p99_ms": times[int(len(times) * 0.99)],
        "max_ms": times[-1],
        "min_ms": times[0],
        "mean_ms": statistics.mean(times),
    }


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--endpoint", default="/api/stats/overview/perf_user")
    parser.add_argument("--iterations", type=int, default=1000)
    parser.add_argument("--warmup", type=int, default=50)
    parser.add_argument("--output", type=Path)
    args = parser.parse_args()

    legacy_result = benchmark(args.endpoint, "legacy", args.iterations, args.warmup)
    orm_result = benchmark(args.endpoint, "orm", args.iterations, args.warmup)

    report = {
        "date": datetime.now().isoformat(),
        "branch": os.popen("git branch --show-current").read().strip(),
        "results": {"legacy": legacy_result, "orm": orm_result},
        "decision_criteria": {
            "p95_threshold_ms": 50,
            "p95_pass": orm_result["p95_ms"] <= 50,
        },
    }

    print(json.dumps(report, indent=2))

    if args.output:
        args.output.parent.mkdir(parents=True, exist_ok=True)
        args.output.write_text(json.dumps(report, indent=2))


if __name__ == "__main__":
    main()
```

`perf-results/.gitignore`:

```
*
!.gitignore
```

- [ ] **Step 2: 提交**

```bash
git add tests/perf/benchmark_local.py perf-results/
git commit -m "test(perf): add local benchmark script"
```

---

### Task F.3: 对账脚本

**Files:**
- Create: `scripts/reconcile_databases.py`

- [ ] **Step 1: 创建对账脚本**

`scripts/reconcile_databases.py`:

```python
"""Reconcile two database backends and report inconsistencies.

Usage:
  python scripts/reconcile_databases.py --primary xingshi.db --shadow xingshi_v2.db --tables user_preferences
"""
import argparse
import json
import sqlite3
from datetime import datetime
from pathlib import Path


NOISE_FIELDS = {"updated_at", "created_at", "last_synced_at", "login_at"}


def fetch_table(db_path: Path, table: str) -> dict:
    """Return {row_id: row_dict} for given table."""
    conn = sqlite3.connect(str(db_path))
    try:
        cur = conn.cursor()
        cur.execute(f"SELECT * FROM {table}")
        cols = [d[0] for d in cur.description]
        rows = cur.fetchall()
        return {
            str(row[0]): dict(zip(cols, row))
            for row in rows
        }
    except sqlite3.OperationalError as e:
        return {"__error__": str(e)}
    finally:
        conn.close()


def normalize_row(row: dict) -> dict:
    return {k: v for k, v in row.items() if k not in NOISE_FIELDS}


def rows_equal(a: dict, b: dict) -> bool:
    return normalize_row(a) == normalize_row(b)


def reconcile_table(table: str, primary_path: Path, shadow_path: Path) -> dict:
    primary = fetch_table(primary_path, table)
    shadow = fetch_table(shadow_path, table)

    if "__error__" in primary:
        return {"table": table, "error": primary["__error__"]}
    if "__error__" in shadow:
        return {"table": table, "error": shadow["__error__"]}

    primary_ids = set(primary.keys())
    shadow_ids = set(shadow.keys())

    missing_in_shadow = primary_ids - shadow_ids
    extra_in_shadow = shadow_ids - primary_ids

    divergent = []
    for id_ in primary_ids & shadow_ids:
        if not rows_equal(primary[id_], shadow[id_]):
            divergent.append({
                "id": id_,
                "primary": normalize_row(primary[id_]),
                "shadow": normalize_row(shadow[id_]),
            })

    return {
        "table": table,
        "primary_count": len(primary_ids),
        "shadow_count": len(shadow_ids),
        "missing_in_shadow": sorted(missing_in_shadow),
        "extra_in_shadow": sorted(extra_in_shadow),
        "divergent": divergent,
        "consistent": not (missing_in_shadow or extra_in_shadow or divergent),
    }


def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("--primary", required=True, type=Path)
    parser.add_argument("--shadow", required=True, type=Path)
    parser.add_argument("--tables", required=True, help="Comma-separated table names")
    parser.add_argument("--output", type=Path)
    args = parser.parse_args()

    tables = args.tables.split(",")
    results = {
        "date": datetime.now().isoformat(),
        "primary": str(args.primary),
        "shadow": str(args.shadow),
        "tables": [reconcile_table(t.strip(), args.primary, args.shadow) for t in tables],
    }

    print(json.dumps(results, indent=2, ensure_ascii=False))

    if args.output:
        args.output.parent.mkdir(parents=True, exist_ok=True)
        args.output.write_text(json.dumps(results, indent=2, ensure_ascii=False))

    inconsistent = [r for r in results["tables"] if not r.get("consistent", True)]
    if inconsistent:
        import sys
        sys.exit(1)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 提交**

```bash
git add scripts/reconcile_databases.py
git commit -m "chore(scripts): add database reconciliation script"
```

---

# 验收清单（合并完成后）

## 整体验收

- [ ] 所有 10 个切片 + 收尾阶段完成
- [ ] `db.py` 已删除（先备份）
- [ ] `Navicat/setup_database.py` 已删除
- [ ] 所有路由使用 SQLAlchemy ORM
- [ ] `READ_BACKEND_PERCENTAGE=100` 永久生效（feature flag 移除）
- [ ] 性能基准全量通过（p95 < 50ms, memory < 60MB）
- [ ] 用户报告 7 天内无 P0/P1
- [ ] `SLICE_STATUS.md` 标记全部完成
- [ ] CI 集成双写一致性测试

## 时间线汇总

| Milestone | 切片 | 工作日 | 日历时间 |
|-----------|------|-------|---------|
| M0 | 基础设施 | 2 | W1 |
| M1 | #1 认证 | 4 | W1-W2 |
| M2 | #2 偏好 | 2 | W2 |
| M3 | #3 统计读 | 3 | W3 |
| M4 | #4 统计写 | 3 | W3-W4 |
| M5 | #5 课程 | 3 | W4-W5 |
| M6 | #6 知识 | 2 | W5 |
| M7 | #7 焦点 | 2 | W5-W6 |
| M8 | #8 游戏化 | 2 | W6 |
| M9 | #9 聊天 | 5 | W6-W8 |
| M10 | #10 教室 | 4 | W8-W9 |
| M11 | 收尾 | 5 | W9-W10 |
| **总计** | | **35 工作日** | **约 10 周** |